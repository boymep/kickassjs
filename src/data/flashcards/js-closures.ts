import type { TopicFlashcards } from '../../types/flashcard';

export const jsClosuresFlashcards: TopicFlashcards = {
  topicId: 'js-closures',
  cards: [
    {
      id: 'jsc-f1',
      question: 'Что такое замыкание? Как оно работает?',
      answer:
        'Замыкание — это функция, которая сохраняет ссылку на переменные из своей внешней области видимости даже после того, как эта область завершила выполнение. Переменные не уничтожаются сборщиком мусора, пока на них есть хотя бы одна ссылка.',
      keyPoints: [
        'Лексическая область видимости: scope определяется при написании кода, не при вызове',
        'Замыкание — живые ссылки, не копии значений',
        'Внешняя функция может завершиться, а её переменные останутся в памяти',
        'Каждый вызов внешней функции создаёт независимое замыкание',
      ],
      code: `function makeCounter() {
  let count = 0;
  return {
    inc() { count++; },
    get() { return count; },
  };
}

const c1 = makeCounter(); // своё замыкание
const c2 = makeCounter(); // независимое
c1.inc(); c1.inc();
c1.get(); // 2
c2.get(); // 0`,
    },
    {
      id: 'jsc-f2',
      question: 'Почему var в цикле с setTimeout выводит неожиданный результат?',
      answer:
        'С var все итерации цикла разделяют одну переменную i. К моменту, когда setTimeout выполнится, цикл уже завершился и i имеет конечное значение. let создаёт отдельную переменную для каждой итерации.',
      keyPoints: [
        'var — функциональная область видимости, одна переменная на весь цикл',
        'let — блочная область видимости, новая переменная на каждую итерацию',
        'Замыкание хранит ссылку, а не снимок значения',
        'Решение через IIFE работало до появления let',
      ],
      code: `// ❌ var: выводит 3 3 3
for (var i = 0; i < 3; i++) {
  setTimeout(() => console.log(i), 0);
}

// ✅ let: выводит 0 1 2
for (let i = 0; i < 3; i++) {
  setTimeout(() => console.log(i), 0);
}`,
    },
    {
      id: 'jsc-f3',
      question: 'Как замыкания используются для инкапсуляции (приватное состояние)?',
      answer:
        'Замыкание создаёт «приватную» область видимости: переменные внутри внешней функции недоступны снаружи, но доступны возвращённым функциям. Это паттерн модуля — альтернатива классам до ES6.',
      keyPoints: [
        'Переменные в scope внешней функции — приватные',
        'Возвращённые функции — публичный API',
        'Паттерн IIFE для одноразового модуля',
        'Сейчас то же делают private #fields в классах',
      ],
      code: `const bankAccount = (() => {
  let balance = 0; // приватно
  return {
    deposit(n) { balance += n; },
    withdraw(n) { if (n <= balance) balance -= n; },
    getBalance() { return balance; },
  };
})();

bankAccount.deposit(100);
bankAccount.balance; // undefined — нет прямого доступа`,
    },
    {
      id: 'jsc-f4',
      question: 'Что такое частичное применение и как его реализовать через замыкание?',
      answer:
        'Частичное применение — фиксация части аргументов функции. Замыкание «запоминает» первые аргументы и возвращает функцию, ожидающую остальные. Это применение принципа каррирования.',
      keyPoints: [
        'bind — встроенный механизм частичного применения',
        'Замыкание вручную даёт больше контроля',
        'Полезно для: логирование с prefix, обработчики с предустановленным контекстом',
      ],
      code: `function makeMultiplier(factor) {
  return (n) => n * factor; // factor в замыкании
}

const double = makeMultiplier(2);
const triple = makeMultiplier(3);

double(5); // 10
triple(5); // 15`,
    },
    {
      id: 'jsc-f5',
      question: 'Как замыкания могут вызывать утечки памяти?',
      answer:
        'Замыкание держит весь scope внешней функции, включая объекты которые вам больше не нужны. Пока существует ссылка на замкнутую функцию — GC не может освободить эти данные.',
      keyPoints: [
        'Частая ловушка: EventListener не снят, а внутри ссылается на большой объект',
        'Замыкание держит весь scope, а не только используемые переменные',
        'Решение: явно удалять обработчики, присваивать null ненужным ссылкам',
        'WeakMap помогает при кешировании с объектами-ключами',
      ],
      code: `function attachHandler(element) {
  const bigData = loadMegabytes(); // держится пока element жив

  element.addEventListener('click', () => {
    console.log('clicked'); // bigData в замыкании!
  });
  // Нет removeEventListener — утечка!
}`,
    },
    {
      id: 'jsc-f6',
      question: 'Что такое мемоизация и как она реализуется через замыкание?',
      answer:
        'Мемоизация — кеширование результата функции по её аргументам. Замыкание хранит Map/объект с кешем между вызовами. При повторном вызове с теми же аргументами — возвращается кешированный результат.',
      keyPoints: [
        'Кеш живёт в замыкании — не сбрасывается между вызовами',
        'JSON.stringify для ключа работает для простых аргументов',
        'Не применяется к нечистым функциям (с сайд-эффектами)',
        'TTL (time-to-live) — расширение с истечением кеша',
      ],
      code: `function memoize(fn) {
  const cache = new Map();
  return (...args) => {
    const key = JSON.stringify(args);
    if (cache.has(key)) return cache.get(key);
    const result = fn(...args);
    cache.set(key, result);
    return result;
  };
}`,
    },
  ],
};

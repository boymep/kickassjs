import type { Lesson } from '../../types/lesson';
import { binarySearchQuiz } from '../quizzes/binary-search';

// Index existing quiz questions so we can pick them as checkpoints
// without duplicating content.
const Q = Object.fromEntries(binarySearchQuiz.questions.map((q) => [q.id, q]));

// Questions placed inside chapters as checkpoints.
// They must NOT also appear in finalQuiz.
const CHECKPOINT_IDS = new Set(['bs-q1', 'bs-q3', 'bs-q6', 'bs-q9', 'bs-q13', 'bs-q14']);

export const binarySearchLesson: Lesson = {
  topicId: 'binary-search',

  intro: {
    whyItMatters: `Бинарный поиск — алгоритм поиска в отсортированных данных за O(log n). На каждом шаге диапазон делится пополам, поэтому миллиону элементов хватает ~20 проверок, миллиарду — ~30.

Применяется везде, где есть упорядоченные данные: индексы баз (B-tree), \`git bisect\`, поиск позиции вставки. На собеседовании спрашивают почти всегда — задача простая, но легко сделать ошибку на единицу в границах. Цель темы — научиться писать его без багов и объяснять, почему он работает.`,
    estimatedMinutes: 35,
    interviewAngle:
      'На собеседовании интервьюера обычно интересует не сам шаблон, а рассуждение про инвариант: «target всегда в [left; right]», «каждая итерация строго сужает диапазон». Проговаривание инварианта вслух — половина решения.',
    prerequisites: [],
  },

  chapters: [
    // ─────────────────────────────────────────────────────────────
    {
      id: 'how-it-works',
      title: 'Как работает бинарный поиск',
      estimatedMinutes: 5,
      blocks: [
        {
          type: 'text',
          content:
            'Представьте игру: загадано число от 1 до 100, нужно угадать за минимум попыток. После каждой попытки сообщают «больше» или «меньше». Перебирать по порядку 1, 2, 3 — неэффективно: разумнее начать с **50**, потом сузить до 25 или 75, и так далее. Этот приём и есть бинарный поиск.',
        },
        {
          type: 'text',
          content:
            'Главное условие — данные должны быть **отсортированы**. Только так, проверив элемент в середине, мы можем точно сказать, в какой половине искать дальше. Если массив не отсортирован, бинарный поиск работать не будет — нет никаких гарантий, что нужный элемент справа, если \`arr[mid]\` оказался меньше.',
        },
        {
          type: 'visualization',
          content: '',
          vizId: 'binary-search',
        },
        {
          type: 'heading',
          content: 'Почему это так быстро',
        },
        {
          type: 'text',
          content:
            'Линейный поиск — обычный \`for\` по массиву — работает за **O(n)**: миллион элементов требует миллиона операций. Бинарный поиск работает за **O(log n)**, потому что на каждой итерации количество кандидатов делится пополам. Для миллиона элементов достаточно ~20 шагов, для миллиарда — ~30.',
        },
        {
          type: 'callout',
          calloutType: 'info',
          content:
            'log₂(1 000 000) ≈ 20, log₂(1 000 000 000) ≈ 30. То есть для массива в миллиард элементов бинарный поиск делает порядка тридцати проверок. Сравните с миллиардом проверок линейного поиска — разница в десятки миллионов раз.',
        },
        {
          type: 'heading',
          content: 'Пошаговый пример: ищем 7 в [1, 3, 5, 7, 9, 11]',
        },
        {
          type: 'list',
          content: `Шаг 1: \`left = 0\`, \`right = 5\`, \`mid = 2\`. \`arr[2] = 5\` меньше 7 → сдвигаем \`left = mid + 1 = 3\`.
Шаг 2: \`left = 3\`, \`right = 5\`, \`mid = 4\`. \`arr[4] = 9\` больше 7 → сдвигаем \`right = mid - 1 = 3\`.
Шаг 3: \`left = 3\`, \`right = 3\`, \`mid = 3\`. \`arr[3] = 7\` равно 7 — нашли! Возвращаем индекс \`3\`.`,
        },
        {
          type: 'text',
          content:
            'Обратите внимание на деталь: на втором шаге сдвигаем \`right = mid - 1\`, а не \`right = mid\`. Это часть **инварианта**, на котором держится алгоритм. Раз \`arr[mid] = 9\` строго больше \`target\`, индекс \`mid\` не может быть ответом — его можно отбросить. Так гарантируется, что цикл завершится. Подробно про инвариант — в следующей главе.',
        },
      ],
      checkpoint: [Q['bs-q1']!, Q['bs-q6']!, {
        type: 'ordering',
        id: 'bs-ord1',
        description: 'Расставьте шаги одной итерации бинарного поиска в правильном порядке',
        items: [
          'Вычислить mid = Math.floor((left + right) / 2)',
          'Сравнить arr[mid] с target',
          'Если равны — вернуть mid',
          'Если arr[mid] < target — сдвинуть left = mid + 1',
          'Если arr[mid] > target — сдвинуть right = mid - 1',
        ],
        explanation:
          'Сначала всегда вычисляем середину, потом сравниваем, потом принимаем решение о сдвиге. Менять порядок нельзя — например, нельзя сдвигать границы до того, как сравнили mid с target.',
      }],
    },

    // ─────────────────────────────────────────────────────────────
    {
      id: 'classic-template',
      title: 'Классический шаблон и инвариант',
      estimatedMinutes: 6,
      blocks: [
        {
          type: 'text',
          content:
            'Базовый шаблон бинарного поиска выглядит так. Ниже разберём каждую строку.',
        },
        {
          type: 'code',
          language: 'javascript',
          content: `function binarySearch(arr, target) {
  let left = 0;
  let right = arr.length - 1;

  while (left <= right) {
    const mid = Math.floor((left + right) / 2);

    if (arr[mid] === target) {
      return mid;
    } else if (arr[mid] < target) {
      left = mid + 1;     // ответ строго правее mid
    } else {
      right = mid - 1;    // ответ строго левее mid
    }
  }

  return -1; // не нашли
}`,
        },
        {
          type: 'heading',
          content: 'Что такое инвариант и зачем он нужен',
        },
        {
          type: 'text',
          content:
            '**Инвариант цикла** — это утверждение, которое истинно перед каждой итерацией и не нарушается после. Для нашего шаблона он звучит так: «если \`target\` существует в массиве, его индекс находится в **закрытом интервале** [left; right]».',
        },
        {
          type: 'text',
          content:
            'Проверим инвариант:\n- В начале цикла он истинен: \`left = 0\`, \`right = arr.length - 1\` — это весь массив.\n- На каждом шаге отбрасываются только те индексы, которые **гарантированно не равны** target. Если \`arr[mid] < target\`, то все элементы \`arr[0..mid]\` тоже меньше (массив отсортирован), значит ответа там нет — можно сдвинуть \`left\` за \`mid\`.\n- Когда цикл завершается (\`left > right\`), диапазон пуст. По инварианту, если ответ был — он не мог быть отброшен. Значит элемента нет.',
        },
        {
          type: 'callout',
          calloutType: 'tip',
          content:
            'На собеседовании полезно проговорить инвариант вслух: «Инвариант цикла — target лежит в [left; right]. Каждая итерация либо находит target, либо строго сужает диапазон. Значит цикл завершится, и ответ корректен».',
        },
        {
          type: 'heading',
          content: 'Почему именно <=, а не <',
        },
        {
          type: 'text',
          content:
            'У \`left <= right\` есть конкретная причина: используется **закрытый** интервал [left; right]. То есть индекс \`right\` всё ещё может быть ответом. Если поставить \`while (left < right)\`, цикл завершится при \`left === right\` — а это валидная позиция, которую можно пропустить.',
        },
        {
          type: 'text',
          content:
            'Существует другой стиль — с **полуоткрытым** интервалом [left; right), \`while (left < right)\` и сдвигом \`right = mid\` (без -1). Он тоже работает, но это другой шаблон со своими правилами. Главное — выбрать один стиль и придерживаться его. Смешивать «<=» из одного шаблона с «right = mid» из другого — гарантированный баг.',
        },
        {
          type: 'callout',
          calloutType: 'warning',
          content:
            'Самая частая ошибка — путать стили. «while (left <= right)» с «right = mid» легко зацикливается, а «while (left < right)» с «right = mid - 1» пропускает ответ. Учить оба шаблона наизусть не нужно — достаточно одного, который понятен.',
        },
        {
          type: 'heading',
          content: 'Поиск позиции вставки',
        },
        {
          type: 'text',
          content:
            'Часто нужен не сам индекс элемента, а **позиция для вставки**, чтобы массив остался отсортированным. Это то же, что \`std::lower_bound\` в C++ или \`_.sortedIndex\` в lodash.',
        },
        {
          type: 'code',
          language: 'javascript',
          content: `function searchInsert(arr, target) {
  let left = 0;
  let right = arr.length - 1;

  while (left <= right) {
    const mid = Math.floor((left + right) / 2);

    if (arr[mid] === target) return mid;
    else if (arr[mid] < target) left = mid + 1;
    else right = mid - 1;
  }

  // После цикла всегда left === right + 1.
  // left указывает на первый элемент >= target
  // (или на arr.length, если все меньше).
  return left;
}`,
        },
        {
          type: 'callout',
          calloutType: 'info',
          content:
            'Этот факт стоит запомнить: после завершения цикла классического шаблона всегда `left === right + 1`. `right` указывает на последний элемент строго меньше target, `left` — на первый элемент больший или равный. Это и есть «инвариант на выходе».',
        },
      ],
    },

    // ─────────────────────────────────────────────────────────────
    {
      id: 'bounds',
      title: 'Левая и правая границы при дубликатах',
      estimatedMinutes: 7,
      blocks: [
        {
          type: 'text',
          content:
            'Классический шаблон работает, пока в массиве нет повторов. Но при дубликатах возникает вопрос: какой именно индекс будет возвращён? Ответ — **любой из подходящих**, в зависимости от того, где окажется \`mid\` на последней итерации. На собеседовании этого обычно недостаточно — спрашивают конкретно про первое или последнее вхождение.',
        },
        {
          type: 'callout',
          calloutType: 'warning',
          content:
            'Типичная ловушка: дан массив [1, 3, 3, 3, 3, 5], нужно найти **первое** вхождение тройки. Классический шаблон вернёт индекс какой-то тройки, но не обязательно первой. На тестах с одним вхождением всё работает, на тестах с несколькими — падает.',
        },
        {
          type: 'heading',
          content: 'Левая граница (lower_bound): первое вхождение',
        },
        {
          type: 'text',
          content:
            'Идея: не возвращать индекс сразу при равенстве. Раз нужен **самый левый** target, при нахождении одного вхождения надо продолжить поиск левее — там может быть такой же элемент.',
        },
        {
          type: 'code',
          language: 'javascript',
          content: `function searchLeftBound(arr, target) {
  let left = 0;
  let right = arr.length - 1;

  while (left <= right) {
    const mid = Math.floor((left + right) / 2);

    if (arr[mid] >= target) {
      right = mid - 1;    // НЕ возвращаемся при равенстве — идём левее
    } else {
      left = mid + 1;
    }
  }

  // left — первый индекс с arr[left] >= target.
  // Если такого элемента нет — вернёт arr.length.
  return left < arr.length && arr[left] === target ? left : -1;
}`,
        },
        {
          type: 'text',
          content:
            'Главное отличие от классического шаблона — **объединение** веток «равно» и «больше». Если \`arr[mid] >= target\`, right сдвигается влево без \`return\`. Цикл проходит по всем дубликатам и останавливается на самом левом.',
        },
        {
          type: 'heading',
          content: 'Почему после цикла left указывает на первое вхождение',
        },
        {
          type: 'text',
          content:
            'Возьмём \`arr = [1, 3, 3, 3, 5]\`, \`target = 3\` и проследим вручную:',
        },
        {
          type: 'code',
          language: 'javascript',
          content: `// Шаг 1: left=0, right=4, mid=2, arr[2]=3 >= 3 → right=1
// Шаг 2: left=0, right=1, mid=0, arr[0]=1 < 3  → left=1
// Шаг 3: left=1, right=1, mid=1, arr[1]=3 >= 3 → right=0
//
// Цикл завершён: left=1, right=0  (left === right + 1)
// left=1 — это индекс первого вхождения тройки ✓`,
        },
        {
          type: 'callout',
          calloutType: 'tip',
          content:
            'После «левого» цикла `left` всегда указывает на первый элемент `>= target`. Если таких элементов нет, `left === arr.length`. На этом инварианте строятся `lower_bound`, `upper_bound` и поиск позиции вставки.',
        },
        {
          type: 'heading',
          content: 'Правая граница: последнее вхождение',
        },
        {
          type: 'text',
          content:
            'Симметричная задача — найти **последний** target. Здесь подход другой: при равенстве запоминаем результат и идём правее. Этот вариант часто пишут так:',
        },
        {
          type: 'code',
          language: 'javascript',
          content: `function searchRightBound(arr, target) {
  let left = 0;
  let right = arr.length - 1;
  let result = -1;

  while (left <= right) {
    const mid = Math.floor((left + right) / 2);

    if (arr[mid] === target) {
      result = mid;
      left = mid + 1;     // запомнили — и продолжаем правее
    } else if (arr[mid] < target) {
      left = mid + 1;
    } else {
      right = mid - 1;
    }
  }

  return result;
}`,
        },
        {
          type: 'heading',
          content: 'Подсчёт количества за O(log n)',
        },
        {
          type: 'text',
          content:
            'Имея `lower_bound` и `upper_bound`, можно посчитать количество вхождений за O(log n): \`count = upper_bound(target) - lower_bound(target)\`. На собеседовании это частый follow-up после задачи «найдите первое вхождение».',
        },
        {
          type: 'callout',
          calloutType: 'info',
          content:
            'lower_bound возвращает позицию первого элемента >= target. upper_bound — позицию первого элемента строго > target. Разница ровно в одном знаке `>` vs `>=` в условии, но эта мелочь меняет смысл целиком.',
        },
      ],
      playground: {
        starterCode: `// Проследите, что вернёт поиск левой границы числа 4 в [1, 4, 4, 4, 7].
// Подсказка: ответ — индекс первого вхождения четвёрки.

const arr = [1, 4, 4, 4, 7];
const target = 4;

let left = 0;
let right = arr.length - 1;

while (left <= right) {
  const mid = Math.floor((left + right) / 2);
  if (arr[mid] >= target) right = mid - 1;
  else left = mid + 1;
}

console.log(left);`,
        expectedOutput: '1',
        description:
          'После цикла `left` должен указывать на индекс 1 — позицию первой четвёрки. Запустите код и посмотрите, что получится.',
      },
      checkpoint: [Q['bs-q9']!, {
        type: 'match-pairs',
        id: 'bs-mp1',
        description: 'Сопоставьте условие с его смыслом',
        pairs: [
          { left: 'left > right', right: 'Диапазон пуст — элемент не найден' },
          { left: 'arr[mid] === target', right: 'Нашли элемент — возвращаем индекс' },
          { left: 'arr[mid] < target', right: 'target правее — сдвигаем left = mid + 1' },
          { left: 'arr[mid] > target', right: 'target левее — сдвигаем right = mid - 1' },
        ],
        explanation:
          'Каждое условие соответствует одному из четырёх состояний поиска. Главное — сдвиги `+1` и `-1`: они гарантируют, что диапазон строго сокращается, а значит цикл завершится.',
      }],
    },

    // ─────────────────────────────────────────────────────────────
    {
      id: 'pitfalls',
      title: 'Подводные камни',
      estimatedMinutes: 6,
      blocks: [
        {
          type: 'text',
          content:
            'Бинарный поиск ломается незаметно: код выглядит правильно, проходит большинство тестов — но падает на массиве из одного элемента или когда target стоит на границе. Ниже три класса ошибок, которые встречаются чаще всего.',
        },
        {
          type: 'heading',
          content: 'Ошибка №1: off-by-one на границах',
        },
        {
          type: 'text',
          content:
            'Самая частая ошибка. Сравните два варианта — отличаются они на один символ:',
        },
        {
          type: 'code',
          language: 'javascript',
          content: `// Закрытый интервал [left; right] — нужно <= и -1
while (left <= right) {
  ...
  left  = mid + 1;
  right = mid - 1;
}

// Полуоткрытый интервал [left; right) — нужно < и без -1 в right
while (left < right) {
  ...
  left  = mid + 1;
  right = mid;        // без -1, потому что right эксклюзивен
}`,
        },
        {
          type: 'callout',
          calloutType: 'warning',
          content:
            'Знаки в условии и сдвиги границ всегда идут «комплектом»: «<=» вместе с «mid ± 1»; «<» вместе с «mid». Смешивать нельзя — будет либо бесконечный цикл, либо пропущенный ответ.',
        },
        {
          type: 'heading',
          content: 'Ошибка №2: бесконечный цикл',
        },
        {
          type: 'text',
          content:
            'Любая итерация **должна** уменьшать диапазон хотя бы на единицу. Если ветка сдвига не двигает границы — цикл зависнет. Типичный пример:',
        },
        {
          type: 'code',
          language: 'javascript',
          content: `// АНТИПАТТЕРН: зависает на двух элементах
while (left < right) {
  const mid = Math.floor((left + right) / 2);
  if (arr[mid] === target) return mid;
  if (arr[mid] < target) left = mid;     // ← забыли +1
  else right = mid;
}
// При left=0, right=1: mid=0 (округление вниз).
// Если arr[0] < target, выполняем left = mid = 0.
// Ничего не изменилось — следующая итерация повторит то же самое.`,
        },
        {
          type: 'callout',
          calloutType: 'tip',
          content:
            'Полезная привычка — мысленно подставить массив из двух элементов и проследить пару итераций. Если в какой-то ветке границы не двигаются — это будущий бесконечный цикл, и лучше заметить это до запуска кода.',
        },
        {
          type: 'heading',
          content: 'Ошибка №3: переполнение при вычислении mid',
        },
        {
          type: 'text',
          content:
            'Запись \`(left + right) / 2\` работает в JavaScript, потому что Number — это 64-битный double и точно представляет целые числа до 2⁵³. В Java, C, C++ \`int\` 32-битный, и при больших \`left + right\` сумма переполняется. В стандартной библиотеке JDK эта ошибка прожила 9 лет — её нашли только в 2006 году.',
        },
        {
          type: 'code',
          language: 'javascript',
          content: `// Работает в JS, но непортабельно
const mid = Math.floor((left + right) / 2);

// Портабельный вариант — работает в любом языке
const mid = left + Math.floor((right - left) / 2);`,
        },
        {
          type: 'callout',
          calloutType: 'info',
          content:
            'На собеседовании по JavaScript достаточно написать первый вариант и упомянуть второй: в JS переполнения нет, но портабельная запись важна, если код перенесут на язык с фиксированными целыми.',
        },
        {
          type: 'heading',
          content: 'Граничные случаи, которые проверит интервьюер',
        },
        {
          type: 'list',
          content: `**Пустой массив**: \`binarySearch([], 5)\` — цикл не должен начаться, должен вернуть -1.
**Один элемент, есть совпадение**: \`binarySearch([5], 5)\` — должно вернуть 0.
**Один элемент, нет совпадения**: \`binarySearch([5], 4)\` — должно вернуть -1.
**target меньше всех**: \`binarySearch([1,2,3], 0)\` — должно вернуть -1.
**target больше всех**: \`binarySearch([1,2,3], 100)\` — должно вернуть -1.
**target на левой границе**: \`binarySearch([1,2,3], 1)\` — должно вернуть 0.
**target на правой границе**: \`binarySearch([1,2,3], 3)\` — должно вернуть 2.`,
        },
        {
          type: 'callout',
          calloutType: 'tip',
          content:
            'Полезно проговаривать эти случаи вслух **до** написания кода — это демонстрирует, что граничные ситуации продуманы заранее, и помогает не упустить их в реализации.',
        },
      ],
      playground: {
        starterCode: `// Перед вами шаблон с полуоткрытым интервалом [left; right).
// Найдите индекс первого true в массиве. Запустите и посмотрите.

const arr = [false, false, true, true, true];
let left = 0;
let right = arr.length;

while (left < right) {
  const mid = Math.floor((left + right) / 2);
  if (arr[mid]) right = mid;       // без -1, потому что [left; right)
  else left = mid + 1;
}

console.log(left);`,
        expectedOutput: '2',
        description:
          'Этот шаблон работает с полуоткрытым интервалом. После цикла `left === right`, и эта позиция указывает на первый `true`. Обратите внимание: `right = mid` без `-1`.',
      },
    },

    // ─────────────────────────────────────────────────────────────
    {
      id: 'binary-search-on-answer',
      title: 'Бинарный поиск по ответу',
      estimatedMinutes: 7,
      blocks: [
        {
          type: 'text',
          content:
            'Бинарный поиск можно применять не только к элементам массива, но и к **самому числу-ответу**. Этот приём называется «бинпоиск по ответу» и часто оказывается ключом к задачам на минимизацию или максимизацию.',
        },
        {
          type: 'heading',
          content: 'Когда это работает',
        },
        {
          type: 'text',
          content:
            'Три условия:\n- Ответ — это **число** из ограниченного диапазона [lo; hi].\n- Есть функция \`check(x)\` — «подходит ли значение x?»\n- Этот предикат **монотонен**: если \`check(x)\` ложно, то для всех меньших x он тоже ложен; если \`check(x)\` истинно — для всех больших он тоже истинен.',
        },
        {
          type: 'text',
          content:
            'Монотонность — главное свойство. Без неё бинарный поиск неприменим: нет гарантии, что отбрасываемая половина «не содержит ответа». Если в задаче угадывается монотонный предикат — даже не самый очевидный — это сигнал применить бинпоиск.',
        },
        {
          type: 'callout',
          calloutType: 'tip',
          content:
            'Как распознать в задаче: формулировки вроде «найдите минимальное X, при котором…», «какая минимальная скорость…», «какая минимальная грузоподъёмность…» — почти всегда бинпоиск по ответу. Это редко указывается напрямую — приём нужно заметить самостоятельно.',
        },
        {
          type: 'visualization',
          content: '',
          vizId: 'binary-search-answer',
        },
        {
          type: 'heading',
          content: 'Канонический шаблон',
        },
        {
          type: 'code',
          language: 'javascript',
          content: `function binarySearchOnAnswer(lo, hi, check) {
  // Ищем минимальный x ∈ [lo; hi], при котором check(x) === true.
  // check должен быть монотонным: ...false, false, true, true, true...
  while (lo < hi) {
    const mid = Math.floor((lo + hi) / 2);

    if (check(mid)) {
      hi = mid;          // подходит — пробуем уменьшить
    } else {
      lo = mid + 1;      // не подходит — увеличиваем
    }
  }

  return lo;
}`,
        },
        {
          type: 'text',
          content:
            'Здесь используется **полуоткрытый интервал** [lo; hi), потому что ищется точка перехода, а не конкретный индекс в массиве. После цикла \`lo === hi\` — это минимальное значение x, при котором \`check\` возвращает true.',
        },
        {
          type: 'heading',
          content: 'Классическая задача: Koko ест бананы (LeetCode 875)',
        },
        {
          type: 'text',
          content:
            'Дано: кучи бананов \`piles\` и время \`h\` часов. За один час Коко ест \`k\` бананов из выбранной кучи (если в куче меньше — съедает остаток и переходит к следующей через час). Найдите **минимальную** скорость \`k\`, при которой Коко всё съест за \`h\` часов.',
        },
        {
          type: 'code',
          language: 'javascript',
          content: `function minEatingSpeed(piles, h) {
  // check(k) — успеет ли Коко при скорости k бананов в час
  const check = (k) =>
    piles.reduce((acc, p) => acc + Math.ceil(p / k), 0) <= h;

  let lo = 1;
  let hi = Math.max(...piles);

  while (lo < hi) {
    const mid = Math.floor((lo + hi) / 2);
    if (check(mid)) hi = mid;
    else lo = mid + 1;
  }

  return lo;
}`,
        },
        {
          type: 'text',
          content:
            'Сложность — **O(n · log(max(piles)))**. Линейный перебор скоростей дал бы O(n · max(piles)) — часто на пару порядков медленнее.',
        },
        {
          type: 'heading',
          content: 'Почему это монотонно',
        },
        {
          type: 'text',
          content:
            'Если Коко успевает при скорости \`k\`, то и при \`k + 1\` тоже успеет — она ест быстрее. Если не успевает при \`k\`, то и при \`k - 1\` не успеет. Это и есть монотонность. Часто её можно вывести из здравого смысла, но на собеседовании монотонность стоит проговаривать вслух — это часть обоснования решения.',
        },
        {
          type: 'heading',
          content: 'Другие классические задачи',
        },
        {
          type: 'list',
          content: `**Ship Within Days (LC 1011)**: минимальная грузоподъёмность для перевозки за D дней.
**Split Array Largest Sum (LC 410)**: разбить массив на K частей, минимизируя максимальную сумму части.
**git bisect**: ищет коммит с регрессией. \`check(commit)\` — это запуск тестов. Чистый бинпоиск по ответу.
**Поиск версии библиотеки**, в которой появился баг — то же самое.
**Квадратный корень из числа** за O(log x): ищем максимальное \`m\`, при котором \`m * m <= x\`.`,
        },
      ],
      checkpoint: [Q['bs-q14']!, Q['bs-q13']!, {
        type: 'ordering',
        id: 'bs-ord2',
        description: 'Расставьте шаги решения задачи «бинпоиск по ответу» в правильном порядке',
        items: [
          'Сформулировать, какое именно число мы ищем (ответ)',
          'Определить диапазон возможных ответов [lo; hi]',
          'Написать функцию check(x): «подходит ли значение x»',
          'Убедиться, что check монотонен',
          'Применить шаблон бинарного поиска по [lo; hi]',
        ],
        explanation:
          'Сначала надо понять, что именно мы ищем, и в каких границах. Затем — написать предикат check. Только после этого имеет смысл проверять монотонность и запускать бинпоиск. Перепрыгивать к коду без этих шагов — частая причина неверных решений.',
      }],
    },

    // ─────────────────────────────────────────────────────────────
    {
      id: 'advanced',
      title: 'Применения и тонкости',
      estimatedMinutes: 5,
      blocks: [
        {
          type: 'text',
          content:
            'Когда базовый шаблон уверенно пишется, интервьюер может усложнить задачу: массив повёрнут, в массиве есть пик, нужно найти корень. Все эти задачи — вариации одной идеи: на каждом шаге половина диапазона подчиняется простому свойству, и поиск продолжается в ней.',
        },
        {
          type: 'heading',
          content: 'Поиск в повёрнутом массиве',
        },
        {
          type: 'text',
          content:
            'Отсортированный массив могут циклически сдвинуть: \`[4, 5, 6, 7, 0, 1, 2]\`. Бинарный поиск по нему всё ещё работает за O(log n) — нужно лишь добавить одно наблюдение: **в любой момент одна из половин относительно \`mid\` гарантированно отсортирована**.',
        },
        {
          type: 'code',
          language: 'javascript',
          content: `function searchRotated(nums, target) {
  let left = 0;
  let right = nums.length - 1;

  while (left <= right) {
    const mid = Math.floor((left + right) / 2);
    if (nums[mid] === target) return mid;

    // Какая половина отсортирована?
    if (nums[left] <= nums[mid]) {
      // Левая отсортирована
      if (target >= nums[left] && target < nums[mid]) right = mid - 1;
      else left = mid + 1;
    } else {
      // Правая отсортирована
      if (target > nums[mid] && target <= nums[right]) left = mid + 1;
      else right = mid - 1;
    }
  }
  return -1;
}`,
        },
        {
          type: 'callout',
          calloutType: 'tip',
          content:
            'Ключевое наблюдение — одна из половин всегда отсортирована. Дальше задача сводится к классическому бинпоиску в этой половине. Запоминать код наизусть не нужно — достаточно понять идею.',
        },
        {
          type: 'heading',
          content: 'Поиск пика',
        },
        {
          type: 'text',
          content:
            'Если массив сначала строго возрастает, потом убывает — пик находится бинпоиском за O(log n). Сравниваем \`arr[mid]\` и \`arr[mid + 1]\`: если \`>\` — мы на спуске, пик слева или в \`mid\`; иначе — на подъёме, пик строго правее.',
        },
        {
          type: 'heading',
          content: 'Целочисленный корень за O(log x)',
        },
        {
          type: 'code',
          language: 'javascript',
          content: `function mySqrt(x) {
  if (x < 2) return x;

  let left = 1;
  let right = Math.floor(x / 2);

  while (left <= right) {
    const mid = Math.floor((left + right) / 2);
    const sq = mid * mid;

    if (sq === x) return mid;
    if (sq < x) left = mid + 1;
    else right = mid - 1;
  }

  return right; // целая часть корня
}`,
        },
        {
          type: 'text',
          content:
            'Это бинпоиск по ответу в чистом виде: ищется максимальное \`m\`, при котором \`m * m <= x\`. Таким же образом находится кубический корень или обратная функция любой монотонной функции — например, минимальная пропускная способность сервера для заданной нагрузки.',
        },
        {
          type: 'heading',
          content: 'Где встречается в реальной жизни',
        },
        {
          type: 'list',
          content: `**Индексы баз данных** (B-tree, B+tree) — это многоуровневый бинарный поиск. Каждый \`SELECT WHERE id = ?\` использует его.
**\`git bisect\`** — находит коммит с регрессией за O(log n) запусков тестов.
**Sorted Set в Redis** (ZRANGEBYSCORE, ZRANGEBYLEX) — внутри тоже бинпоиск.
**\`_.sortedIndex\` в lodash** — поиск позиции вставки.
**Range-запросы во временных рядах** (Prometheus, InfluxDB) — на основе бинпоиска по таймстемпам.`,
        },
        {
          type: 'callout',
          calloutType: 'info',
          content:
            'Если в задаче встречается формулировка «поиск», «минимум», «максимум» — и данные хоть как-то упорядочены — стоит попробовать бинарный поиск. Чаще всего он либо решает задачу, либо подсказывает направление.',
        },
      ],
    },
  ],

  // Все остальные вопросы квиза — те, что не использованы как checkpoint
  finalQuiz: binarySearchQuiz.questions.filter((q) => !CHECKPOINT_IDS.has(q.id)),

  cheatsheet: `### Шпаргалка по бинарному поиску

**Когда применять**
- Массив отсортирован и есть произвольный доступ по индексу (Array, не Linked List)
- Ответ — число из ограниченного диапазона + есть монотонный предикат \`check(x)\`

**Сложность**
- Время: O(log n)
- Память: O(1) (итеративно) или O(log n) (рекурсивно)

**Классический шаблон — закрытый интервал [left; right]**
\`\`\`js
while (left <= right) {
  const mid = Math.floor((left + right) / 2);
  if (arr[mid] === target) return mid;
  if (arr[mid] < target) left = mid + 1;
  else right = mid - 1;
}
return -1;
\`\`\`

**Левая граница (lower_bound)**
- Не возвращать сразу при равенстве, идти левее
- \`if (arr[mid] >= target) right = mid - 1\`
- После цикла \`left\` — индекс первого элемента \`>= target\`

**Бинпоиск по ответу — полуоткрытый [lo; hi)**
\`\`\`js
while (lo < hi) {
  const mid = Math.floor((lo + hi) / 2);
  if (check(mid)) hi = mid;
  else lo = mid + 1;
}
return lo;
\`\`\`

**Правила, которые надо помнить**
- Знаки в условии и сдвиги границ — «комплектом»: \`<=\` ↔ \`mid ± 1\`, \`<\` ↔ \`mid\`
- Каждая итерация должна **строго** сужать диапазон — иначе бесконечный цикл
- Портабельная формула: \`mid = left + Math.floor((right - left) / 2)\`

**Граничные случаи на собесе**
- Пустой массив
- Массив из одного элемента (есть/нет target)
- target меньше всех / больше всех
- target на левой/правой границе массива
- Дубликаты (для классики результат неопределён, нужно lower/upper_bound)`,

  nextTopics: [
    {
      slug: 'two-pointers',
      reason:
        'Два указателя — естественное обобщение идеи: оба алгоритма работают на упорядоченных данных и сводят квадратичные задачи к линейным или логарифмическим.',
    },
    {
      slug: 'sliding-window',
      reason:
        'Скользящее окно — частный случай двух указателей. После бинпоиска удобно закрепить мышление инвариантами на массивах.',
    },
  ],
};

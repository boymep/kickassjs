import type { Lesson } from '../../types/lesson';
import { binarySearchQuiz } from '../quizzes/binary-search';

// Index existing quiz questions so we can pick them as checkpoints
// without duplicating content.
const Q = Object.fromEntries(binarySearchQuiz.questions.map((q) => [q.id, q]));

// Questions placed inside chapters as checkpoints.
// They must NOT also appear in finalQuiz.
const CHECKPOINT_IDS = new Set(['bs-q1', 'bs-q3', 'bs-q6', 'bs-q9', 'bs-q13', 'bs-q14']);

export const binarySearchLesson: Lesson = {
  topicId: 'binary-search',

  intro: {
    whyItMatters: `Бинарный поиск ищет элемент в отсортированном массиве за O(log n). Миллиону элементов хватает двадцати проверок, миллиарду — тридцати. Под капотом он почти везде: индексы баз данных, \`git bisect\`, любой \`lower_bound\` в стандартной библиотеке.

На собеседовании его спрашивают почти всегда, и почти всегда у кандидата возникает off-by-one. Задача формально простая, но скользкая: легко получить бесконечный цикл там, где логика кажется очевидной. Цель урока — научиться писать бинпоиск уверенно и объяснять, почему он работает, а не запоминать шаблон механически.`,
    estimatedMinutes: 35,
    interviewAngle:
      'Интервьюеру важен не сам шаблон, а рассуждение про инвариант: «target всегда в [left; right], каждая итерация строго сужает диапазон». Если проговаривать инвариант до написания кода, большинство багов отлавливается ещё на этапе обсуждения.',
    prerequisites: [],
  },

  chapters: [
    // ─────────────────────────────────────────────────────────────
    {
      id: 'how-it-works',
      title: 'Как работает бинарный поиск',
      estimatedMinutes: 5,
      blocks: [
        {
          type: 'text',
          content:
            'Знакомая игра в «больше-меньше»: загадано число от 1 до 100, после каждой попытки сообщают, мало или много. Перебирать 1, 2, 3 — глупо; разумнее начать с 50, потом сузить до 25 или 75, и так далее. Бинарный поиск работает ровно по этой схеме, только с массивом вместо листка бумаги.',
        },
        {
          type: 'text',
          content:
            'Главное условие — массив должен быть отсортирован. Только тогда, сравнив элемент в середине с искомым, мы можем уверенно отбросить ровно одну половину: если \`arr[mid] < target\`, то ответ точно справа. На неотсортированных данных такой гарантии нет — там придётся перебирать всё подряд.',
        },
        {
          type: 'visualization',
          content: '',
          vizId: 'binary-search',
        },
        {
          type: 'heading',
          content: 'Почему это так быстро',
        },
        {
          type: 'text',
          content:
            'Линейный поиск — обычный \`for\` по массиву — работает за O(n): миллион элементов требует миллиона операций. Бинарный поиск работает за O(log n): на каждой итерации количество кандидатов делится пополам. Для миллиона достаточно ~20 шагов, для миллиарда — ~30.',
        },
        {
          type: 'callout',
          calloutType: 'info',
          content:
            'log₂(1 000 000) ≈ 20, log₂(1 000 000 000) ≈ 30. Бинарный поиск по миллиарду элементов делает порядка тридцати проверок против миллиарда у линейного — разница в десятки миллионов раз.',
        },
        {
          type: 'heading',
          content: 'Пошаговый пример: ищем 7 в [1, 3, 5, 7, 9, 11]',
        },
        {
          type: 'list',
          content: `Шаг 1: \`left = 0\`, \`right = 5\`, \`mid = 2\`. \`arr[2] = 5\` меньше 7 → сдвигаем \`left = mid + 1 = 3\`.
Шаг 2: \`left = 3\`, \`right = 5\`, \`mid = 4\`. \`arr[4] = 9\` больше 7 → сдвигаем \`right = mid - 1 = 3\`.
Шаг 3: \`left = 3\`, \`right = 3\`, \`mid = 3\`. \`arr[3] = 7\` равно 7 — нашли. Возвращаем индекс \`3\`.`,
        },
        {
          type: 'text',
          content:
            'Обратите внимание на деталь: на втором шаге сдвигаем \`right = mid - 1\`, а не \`right = mid\`. Раз \`arr[mid] = 9\` строго больше target, индекс \`mid\` ответом быть не может — его можно отбросить. Эта мелочь гарантирует, что цикл завершится; разбор того, как и почему — в следующей главе.',
        },
        {
          type: 'heading',
          content: 'А что если элемента нет',
        },
        {
          type: 'text',
          content:
            'Возьмём тот же массив и поищем число 6 — его там нет. Проследим, что произойдёт:',
        },
        {
          type: 'list',
          content: `Шаг 1: \`left = 0\`, \`right = 5\`, \`mid = 2\`. \`arr[2] = 5 < 6\` → \`left = 3\`.
Шаг 2: \`left = 3\`, \`right = 5\`, \`mid = 4\`. \`arr[4] = 9 > 6\` → \`right = 3\`.
Шаг 3: \`left = 3\`, \`right = 3\`, \`mid = 3\`. \`arr[3] = 7 > 6\` → \`right = 2\`.
Цикл завершён: \`left = 3\`, \`right = 2\`. Условие \`left <= right\` нарушено, возвращаем \`-1\`.`,
        },
        {
          type: 'callout',
          calloutType: 'info',
          content:
            'Запомните это состояние на выходе: после цикла классического шаблона всегда `left === right + 1`. `right` указывает на последний элемент строго меньше target, `left` — на первый элемент больше или равный. На этом факте дальше строится поиск позиции вставки и поиск границ.',
        },
      ],
      checkpoint: [Q['bs-q1']!, Q['bs-q6']!, {
        type: 'ordering',
        id: 'bs-ord1',
        description: 'Расставьте шаги одной итерации бинарного поиска в правильном порядке',
        items: [
          'Вычислить mid = Math.floor((left + right) / 2)',
          'Сравнить arr[mid] с target',
          'Если равны — вернуть mid',
          'Если arr[mid] < target — сдвинуть left = mid + 1',
          'Если arr[mid] > target — сдвинуть right = mid - 1',
        ],
        explanation:
          'Сначала всегда вычисляем середину, потом сравниваем, потом принимаем решение о сдвиге. Менять порядок нельзя — например, нельзя сдвигать границы до того, как сравнили mid с target.',
      }],
    },

    // ─────────────────────────────────────────────────────────────
    {
      id: 'classic-template',
      title: 'Классический шаблон и инвариант',
      estimatedMinutes: 6,
      blocks: [
        {
          type: 'text',
          content:
            'Базовый шаблон бинарного поиска. Ниже разберём, почему каждая строка именно такая.',
        },
        {
          type: 'code',
          language: 'javascript',
          content: `function binarySearch(arr, target) {
  let left = 0;
  let right = arr.length - 1;

  while (left <= right) {
    const mid = Math.floor((left + right) / 2);

    if (arr[mid] === target) {
      return mid;
    } else if (arr[mid] < target) {
      left = mid + 1;     // ответ строго правее mid
    } else {
      right = mid - 1;    // ответ строго левее mid
    }
  }

  return -1; // не нашли
}`,
        },
        {
          type: 'heading',
          content: 'Что такое инвариант и зачем он нужен',
        },
        {
          type: 'text',
          content:
            'Инвариант цикла — это утверждение, истинное перед каждой итерацией и не нарушенное после неё. Для нашего шаблона он формулируется так: «если target вообще есть в массиве, его индекс находится в закрытом интервале [left; right]».',
        },
        {
          type: 'text',
          content:
            'Проверим инвариант руками:\n- В начале \`left = 0\`, \`right = arr.length - 1\` — это весь массив, утверждение тривиально истинно.\n- На каждом шаге отбрасываются только те индексы, которые гарантированно не равны target. Если \`arr[mid] < target\`, то все элементы \`arr[0..mid]\` тоже меньше (массив отсортирован) — ответа там нет, безопасно сдвинуть \`left\` за \`mid\`.\n- Когда цикл завершается (\`left > right\`), диапазон пуст. По инварианту, если ответ и был — он не мог быть отброшен. Значит элемента нет.',
        },
        {
          type: 'text',
          content:
            'Практическая польза такого мышления — мгновенный поиск багов. Допустим, вы случайно написали \`right = mid\` вместо \`right = mid - 1\`. Инвариант формально остался истинным, но шаг \`mid → right\` уже не сокращает диапазон: при \`left === right\` цикл зависнет. Привычка проговаривать инвариант ловит такие огрехи до запуска кода.',
        },
        {
          type: 'heading',
          content: 'Почему именно <=, а не <',
        },
        {
          type: 'text',
          content:
            'У \`left <= right\` есть конкретная причина: используется закрытый интервал [left; right]. Индекс \`right\` — это всё ещё валидная позиция ответа. Если поставить \`while (left < right)\`, цикл завершится при \`left === right\` и пропустит последнюю проверку.',
        },
        {
          type: 'text',
          content:
            'Существует второй стиль — с полуоткрытым интервалом [left; right), \`while (left < right)\` и сдвигом \`right = mid\` без -1. Он тоже корректен, но это другой шаблон со своими правилами. Дальше в уроке оба ещё встретятся — каждый со своей причиной. Главное правило: внутри одной функции стили смешивать нельзя.',
        },
        {
          type: 'callout',
          calloutType: 'warning',
          content:
            'Самая частая ошибка — комбинировать «<=» из одного стиля с «right = mid» из другого. Такой код либо зацикливается, либо пропускает ответ. Учить оба наизусть не обязательно: достаточно одного, который вам понятен.',
        },
        {
          type: 'heading',
          content: 'Поиск позиции вставки',
        },
        {
          type: 'text',
          content:
            'Часто нужен не индекс самого элемента, а позиция, куда его надо вставить, чтобы массив остался отсортированным. Это аналог \`std::lower_bound\` из C++ и \`_.sortedIndex\` из lodash.',
        },
        {
          type: 'code',
          language: 'javascript',
          content: `function searchInsert(arr, target) {
  let left = 0;
  let right = arr.length - 1;

  while (left <= right) {
    const mid = Math.floor((left + right) / 2);

    if (arr[mid] === target) return mid;
    else if (arr[mid] < target) left = mid + 1;
    else right = mid - 1;
  }

  return left;
}`,
        },
        {
          type: 'text',
          content:
            'Почему здесь \`return left\`, а не \`-1\`? Когда цикл завершается без совпадения, мы знаем (мы это вывели в прошлой главе): \`left === right + 1\`. \`right\` указывает на последний элемент строго меньше target, значит \`left\` — на первый элемент больше или равный, ровно туда и нужно вставлять. Если target больше всех элементов массива, \`left\` дорастёт до \`arr.length\` — тоже корректно: вставлять в самый конец.',
        },
      ],
    },

    // ─────────────────────────────────────────────────────────────
    {
      id: 'bounds',
      title: 'Левая и правая границы при дубликатах',
      estimatedMinutes: 7,
      blocks: [
        {
          type: 'text',
          content:
            'Классический шаблон работает, пока в массиве нет повторов. При дубликатах возникает вопрос: какой индекс вернётся? Ответ — любой из подходящих, в зависимости от того, где окажется \`mid\` на последней итерации. На собеседовании этого обычно недостаточно: спрашивают конкретно про первое или последнее вхождение.',
        },
        {
          type: 'callout',
          calloutType: 'warning',
          content:
            'Типичная ловушка: дан массив [1, 3, 3, 3, 3, 5], нужно найти первое вхождение тройки. Классика вернёт индекс какой-то тройки, но не обязательно первой. На тестах с одним вхождением всё проходит, на тестах с несколькими — падает. Очень обидный баг, потому что код выглядит правильно.',
        },
        {
          type: 'text',
          content:
            'Очевидный костыль — найти любое вхождение, а потом линейно идти влево. Работает, но убивает асимптотику: на массиве из миллиона одинаковых элементов это снова O(n). Нам нужно решение, которое остаётся O(log n).',
        },
        {
          type: 'heading',
          content: 'Левая граница (lower_bound): первое вхождение',
        },
        {
          type: 'text',
          content:
            'Идея: не возвращать индекс сразу при равенстве. Раз нужен самый левый target, при нахождении совпадения надо продолжить поиск левее — там может быть такой же элемент.',
        },
        {
          type: 'code',
          language: 'javascript',
          content: `function searchLeftBound(arr, target) {
  let left = 0;
  let right = arr.length - 1;

  while (left <= right) {
    const mid = Math.floor((left + right) / 2);

    if (arr[mid] >= target) {
      right = mid - 1;    // НЕ возвращаемся при равенстве — идём левее
    } else {
      left = mid + 1;
    }
  }

  // left — первый индекс с arr[left] >= target.
  // Если такого элемента нет — вернёт arr.length.
  return left < arr.length && arr[left] === target ? left : -1;
}`,
        },
        {
          type: 'text',
          content:
            'Главное отличие от классики — объединение веток «равно» и «больше». Если \`arr[mid] >= target\`, right сдвигается влево без \`return\`. Цикл проходит по всем дубликатам и останавливается ровно на самом левом.',
        },
        {
          type: 'heading',
          content: 'Почему после цикла left указывает на первое вхождение',
        },
        {
          type: 'text',
          content:
            'Возьмём \`arr = [1, 3, 3, 3, 5]\`, \`target = 3\` и проследим:',
        },
        {
          type: 'code',
          language: 'javascript',
          content: `// Шаг 1: left=0, right=4, mid=2, arr[2]=3 >= 3 → right=1
// Шаг 2: left=0, right=1, mid=0, arr[0]=1 < 3  → left=1
// Шаг 3: left=1, right=1, mid=1, arr[1]=3 >= 3 → right=0
//
// Цикл завершён: left=1, right=0  (left === right + 1)
// left=1 — это индекс первого вхождения тройки ✓`,
        },
        {
          type: 'callout',
          calloutType: 'tip',
          content:
            'После «левого» цикла `left` всегда указывает на первый элемент `>= target`. Если таких нет — `left === arr.length`. На этом инварианте строятся `lower_bound`, `upper_bound` и поиск позиции вставки.',
        },
        {
          type: 'heading',
          content: 'Правая граница: последнее вхождение',
        },
        {
          type: 'text',
          content:
            'Симметричная задача — найти последний target. Шаблон зеркальный: объединяем ветки «равно» и «меньше», сдвигаем \`left\` вправо. Идея и инвариант ровно те же, просто отражены.',
        },
        {
          type: 'code',
          language: 'javascript',
          content: `function searchRightBound(arr, target) {
  let left = 0;
  let right = arr.length - 1;

  while (left <= right) {
    const mid = Math.floor((left + right) / 2);

    if (arr[mid] <= target) {
      left = mid + 1;     // равно или меньше — идём правее
    } else {
      right = mid - 1;
    }
  }

  // right — последний индекс с arr[right] <= target.
  // Если такого нет — right === -1.
  return right >= 0 && arr[right] === target ? right : -1;
}`,
        },
        {
          type: 'text',
          content:
            'Симметрия с левой границей видна сразу: те же четыре строки тела цикла, только \`>=\` поменялось на \`<=\`, а \`right = mid - 1\` — на \`left = mid + 1\`. Удобно запомнить один приём, а не два разных рецепта.',
        },
        {
          type: 'heading',
          content: 'Подсчёт количества за O(log n)',
        },
        {
          type: 'text',
          content:
            'Имея `lower_bound` и `upper_bound`, можно посчитать количество вхождений за O(log n): \`count = upper_bound(target) - lower_bound(target)\`. Частый follow-up после задачи «найдите первое вхождение».',
        },
        {
          type: 'callout',
          calloutType: 'info',
          content:
            'lower_bound возвращает позицию первого элемента >= target. upper_bound — позицию первого элемента строго > target. Разница ровно в одном знаке `>` vs `>=` в условии, но эта мелочь меняет смысл целиком.',
        },
      ],
      playground: {
        starterCode: `// Проследите, что вернёт поиск левой границы числа 4 в [1, 4, 4, 4, 7].
// Подсказка: ответ — индекс первого вхождения четвёрки.

const arr = [1, 4, 4, 4, 7];
const target = 4;

let left = 0;
let right = arr.length - 1;

while (left <= right) {
  const mid = Math.floor((left + right) / 2);
  if (arr[mid] >= target) right = mid - 1;
  else left = mid + 1;
}

console.log(left);`,
        expectedOutput: '1',
        description:
          'После цикла `left` должен указывать на индекс 1 — позицию первой четвёрки. Запустите код и посмотрите, что получится.',
      },
      checkpoint: [Q['bs-q9']!, {
        type: 'match-pairs',
        id: 'bs-mp1',
        description: 'Сопоставьте условие с его смыслом',
        pairs: [
          { left: 'left > right', right: 'Диапазон пуст — элемент не найден' },
          { left: 'arr[mid] === target', right: 'Нашли элемент — возвращаем индекс' },
          { left: 'arr[mid] < target', right: 'target правее — сдвигаем left = mid + 1' },
          { left: 'arr[mid] > target', right: 'target левее — сдвигаем right = mid - 1' },
        ],
        explanation:
          'Каждое условие соответствует одному из четырёх состояний поиска. Главное — сдвиги `+1` и `-1`: они гарантируют, что диапазон строго сокращается, а значит цикл завершится.',
      }],
    },

    // ─────────────────────────────────────────────────────────────
    {
      id: 'pitfalls',
      title: 'Подводные камни',
      estimatedMinutes: 6,
      blocks: [
        {
          type: 'text',
          content:
            'Бинарный поиск ломается незаметно: код выглядит правильно, проходит большинство тестов — и падает на массиве из одного элемента или когда target стоит на границе. Ниже три класса ошибок, которые встречаются чаще всего.',
        },
        {
          type: 'heading',
          content: 'Off-by-one в границах',
        },
        {
          type: 'text',
          content:
            'Сравните два варианта — отличаются они на один символ:',
        },
        {
          type: 'code',
          language: 'javascript',
          content: `// Закрытый интервал [left; right] — нужно <= и -1
while (left <= right) {
  ...
  left  = mid + 1;
  right = mid - 1;
}

// Полуоткрытый интервал [left; right) — нужно < и без -1 в right
while (left < right) {
  ...
  left  = mid + 1;
  right = mid;        // без -1, потому что right эксклюзивен
}`,
        },
        {
          type: 'callout',
          calloutType: 'warning',
          content:
            'Знаки в условии и сдвиги границ всегда идут «комплектом»: «<=» вместе с «mid ± 1»; «<» вместе с «mid». Смешивать нельзя — либо бесконечный цикл, либо пропущенный ответ.',
        },
        {
          type: 'heading',
          content: 'Цикл, который никогда не заканчивается',
        },
        {
          type: 'text',
          content:
            'Каждая итерация должна сокращать диапазон хотя бы на единицу. Если ветка сдвига не двигает границы — цикл зависает. Типичный пример:',
        },
        {
          type: 'code',
          language: 'javascript',
          content: `// АНТИПАТТЕРН: зависает на двух элементах
while (left < right) {
  const mid = Math.floor((left + right) / 2);
  if (arr[mid] === target) return mid;
  if (arr[mid] < target) left = mid;     // ← забыли +1
  else right = mid;
}
// При left=0, right=1: mid=0 (округление вниз).
// Если arr[0] < target, выполняем left = mid = 0.
// Ничего не изменилось — следующая итерация повторит то же самое.`,
        },
        {
          type: 'text',
          content:
            'Полезная привычка: перед запуском мысленно подставить массив из двух элементов и пройти пару итераций. Если в какой-то ветке границы не двигаются, будущий бесконечный цикл виден ещё до выполнения.',
        },
        {
          type: 'heading',
          content: 'Переполнение при вычислении mid',
        },
        {
          type: 'text',
          content:
            'Запись \`(left + right) / 2\` в JavaScript безопасна: Number — это 64-битный double, целые числа точны до 2⁵³. В Java, C, C++ \`int\` 32-битный, и при больших \`left + right\` сумма переполняется. Эта ошибка прожила в стандартной библиотеке JDK девять лет — её нашли только в 2006-м.',
        },
        {
          type: 'code',
          language: 'javascript',
          content: `// Работает в JS, но непортабельно
const mid = Math.floor((left + right) / 2);

// Портабельный вариант — работает в любом языке
const mid = left + Math.floor((right - left) / 2);`,
        },
        {
          type: 'callout',
          calloutType: 'info',
          content:
            'На JavaScript-собеседовании оба варианта корректны. Если кандидат сразу пишет портабельную форму и поясняет причину — это плюс: видно, что человек думает про код шире одного языка. По умолчанию имеет смысл писать именно её.',
        },
        {
          type: 'heading',
          content: 'Граничные случаи, которые проверит интервьюер',
        },
        {
          type: 'list',
          content: `Пустой массив: \`binarySearch([], 5)\` — цикл не должен начаться, должен вернуть -1.
Один элемент, есть совпадение: \`binarySearch([5], 5)\` — должно вернуть 0.
Один элемент, нет совпадения: \`binarySearch([5], 4)\` — должно вернуть -1.
target меньше всех: \`binarySearch([1,2,3], 0)\` — -1.
target больше всех: \`binarySearch([1,2,3], 100)\` — -1.
target на левой границе: \`binarySearch([1,2,3], 1)\` — 0.
target на правой границе: \`binarySearch([1,2,3], 3)\` — 2.`,
        },
        {
          type: 'text',
          content:
            'Проговаривать эти случаи вслух до того, как пишете код, — отдельный сигнал зрелости. Интервьюер видит: человек подумал о краях, а не просто скопировал шаблон.',
        },
      ],
      playground: {
        starterCode: `// Перед вами шаблон с полуоткрытым интервалом [left; right).
// Найдите индекс первого true в массиве. Запустите и посмотрите.

const arr = [false, false, true, true, true];
let left = 0;
let right = arr.length;

while (left < right) {
  const mid = Math.floor((left + right) / 2);
  if (arr[mid]) right = mid;       // без -1, потому что [left; right)
  else left = mid + 1;
}

console.log(left);`,
        expectedOutput: '2',
        description:
          'Этот шаблон работает с полуоткрытым интервалом. После цикла `left === right`, и эта позиция указывает на первый `true`. Обратите внимание: `right = mid` без `-1`.',
      },
    },

    // ─────────────────────────────────────────────────────────────
    {
      id: 'binary-search-on-answer',
      title: 'Бинарный поиск по ответу',
      estimatedMinutes: 7,
      blocks: [
        {
          type: 'text',
          content:
            'Бинарный поиск применим не только к индексам массива, но и к самому числу-ответу. Этот приём называется «бинпоиск по ответу» и часто оказывается ключом к задачам на минимизацию или максимизацию. Покажу сразу на классической задаче, потом обобщим в шаблон.',
        },
        {
          type: 'heading',
          content: 'Задача: Koko ест бананы (LeetCode 875)',
        },
        {
          type: 'text',
          content:
            'Условие. Есть кучи бананов \`piles\` и срок \`h\` часов. За один час Коко съедает \`k\` бананов из выбранной кучи (если в куче меньше — съедает остаток и до конца часа стоит). Найдите минимальную скорость \`k\`, при которой Коко всё съест за \`h\` часов.',
        },
        {
          type: 'text',
          content:
            'Лобовое решение: перебрать \`k\` от 1 до \`max(piles)\`, для каждого \`k\` посчитать, успевает ли Коко. Работает, но медленно: \`O(n · max(piles))\`. При кучах по миллиарду уже неприемлемо.',
        },
        {
          type: 'text',
          content:
            'Ключевое наблюдение: если Коко успевает при скорости \`k\`, то и при \`k + 1\` тоже успеет — она просто ест быстрее. И наоборот: если не успевает при \`k\`, то и при \`k - 1\` не успеет. Картина по оси скоростей выглядит так: …нет, нет, нет, ДА, ДА, ДА… — то есть строго монотонная. Раз так — можно бинарным поиском искать точку перехода вместо перебора.',
        },
        {
          type: 'code',
          language: 'javascript',
          content: `function minEatingSpeed(piles, h) {
  // check(k) — успеет ли Коко при скорости k бананов в час
  const check = (k) =>
    piles.reduce((acc, p) => acc + Math.ceil(p / k), 0) <= h;

  let lo = 1;
  let hi = Math.max(...piles);

  while (lo < hi) {
    const mid = Math.floor((lo + hi) / 2);
    if (check(mid)) hi = mid;      // подходит — пробуем меньше
    else lo = mid + 1;             // не подходит — увеличиваем
  }

  return lo;
}`,
        },
        {
          type: 'text',
          content:
            'Сложность стала O(n · log(max(piles))) — на пару порядков лучше лобового решения. Монотонность стоит проговорить вслух: это половина обоснования, без неё бинарный поиск был бы неприменим.',
        },
        {
          type: 'heading',
          content: 'Обобщение в шаблон',
        },
        {
          type: 'text',
          content:
            'Узор повторяется во многих задачах. Чтобы применять «бинпоиск по ответу», нужны три условия:\n- Ответ — это число из ограниченного диапазона [lo; hi].\n- Есть функция \`check(x)\` — «подходит ли значение x?»\n- Этот предикат монотонен: если \`check(x)\` ложно, то для всех меньших x он тоже ложен; если \`check(x)\` истинно — для всех больших он тоже истинен.',
        },
        {
          type: 'callout',
          calloutType: 'tip',
          content:
            'Триггерные формулировки в задачах: «найдите минимальное X, при котором…», «какая минимальная скорость…», «какая минимальная грузоподъёмность…». Это почти всегда бинпоиск по ответу. В условии задачи это редко указано напрямую — приём надо заметить самому.',
        },
        {
          type: 'visualization',
          content: '',
          vizId: 'binary-search-answer',
        },
        {
          type: 'code',
          language: 'javascript',
          content: `function binarySearchOnAnswer(lo, hi, check) {
  // Ищем минимальный x ∈ [lo; hi], при котором check(x) === true.
  // check должен быть монотонным: ...false, false, true, true, true...
  while (lo < hi) {
    const mid = Math.floor((lo + hi) / 2);

    if (check(mid)) {
      hi = mid;          // подходит — пробуем уменьшить
    } else {
      lo = mid + 1;      // не подходит — увеличиваем
    }
  }

  return lo;
}`,
        },
        {
          type: 'text',
          content:
            'Здесь используется полуоткрытый интервал [lo; hi) — не из-за моды, а потому что ищется точка перехода, а не конкретный индекс в массиве. После цикла \`lo === hi\` — это минимальное значение x, при котором \`check\` возвращает true. С закрытым интервалом пришлось бы добавлять переменную \`result\` и больше условий — получилось бы громоздко.',
        },
        {
          type: 'heading',
          content: 'Где ещё это встречается',
        },
        {
          type: 'list',
          content: `Ship Within Days (LC 1011): минимальная грузоподъёмность для перевозки за D дней.
Split Array Largest Sum (LC 410): разбить массив на K частей, минимизируя максимальную сумму части.
\`git bisect\`: ищет коммит с регрессией. \`check(commit)\` — это запуск тестов. Чистый бинпоиск по ответу.
Поиск версии библиотеки, в которой появился баг — то же самое.
Квадратный корень из числа за O(log x): ищем максимальное \`m\`, при котором \`m * m <= x\`.`,
        },
      ],
      checkpoint: [Q['bs-q14']!, Q['bs-q13']!],
    },

    // ─────────────────────────────────────────────────────────────
    {
      id: 'advanced',
      title: 'Применения и тонкости',
      estimatedMinutes: 5,
      blocks: [
        {
          type: 'text',
          content:
            'Когда базовый шаблон уверенно пишется, интервьюер усложняет: массив повёрнут, нужно найти целочисленный корень. Все такие задачи — вариации одной идеи: на каждом шаге половина диапазона подчиняется простому свойству, и поиск продолжается в ней.',
        },
        {
          type: 'heading',
          content: 'Поиск в повёрнутом массиве',
        },
        {
          type: 'text',
          content:
            'Отсортированный массив могут повернуть в точке-«пивоте»: \`[0, 1, 2, 4, 5, 6, 7]\` → \`[4, 5, 6, 7, 0, 1, 2]\`. Бинарный поиск по такому массиву всё ещё работает за O(log n) — нужно лишь одно дополнительное наблюдение: в любой момент одна из половин относительно \`mid\` гарантированно отсортирована.',
        },
        {
          type: 'code',
          language: 'javascript',
          content: `function searchRotated(nums, target) {
  let left = 0;
  let right = nums.length - 1;

  while (left <= right) {
    const mid = Math.floor((left + right) / 2);
    if (nums[mid] === target) return mid;

    // Какая половина отсортирована?
    if (nums[left] <= nums[mid]) {
      // Левая отсортирована
      if (target >= nums[left] && target < nums[mid]) right = mid - 1;
      else left = mid + 1;
    } else {
      // Правая отсортирована
      if (target > nums[mid] && target <= nums[right]) left = mid + 1;
      else right = mid - 1;
    }
  }
  return -1;
}`,
        },
        {
          type: 'text',
          content:
            'Разберём на массиве \`[4, 5, 6, 7, 0, 1, 2]\`, ищем \`target = 0\`. На первой итерации \`left = 0\`, \`right = 6\`, \`mid = 3\`. \`nums[mid] = 7\`. Проверяем \`nums[left] <= nums[mid]\`: \`4 <= 7\`, значит левая половина \`[4, 5, 6, 7]\` отсортирована. Лежит ли target в её диапазоне? \`0 >= 4\` — нет, значит target в правой половине, сдвигаем \`left = mid + 1 = 4\`. Дальше уже работает обычный бинпоиск в \`[0, 1, 2]\`.',
        },
        {
          type: 'callout',
          calloutType: 'tip',
          content:
            'Ключевое наблюдение — одна половина всегда отсортирована. Сравнение `nums[left] <= nums[mid]` именно с `<=`, а не `<`, потому что при `left === mid` (массив из одного элемента в левой части) левая половина тоже формально «отсортирована». Запоминать код наизусть не надо — достаточно держать в голове эту идею и выводить ветки на месте.',
        },
        {
          type: 'heading',
          content: 'Целочисленный корень за O(log x)',
        },
        {
          type: 'code',
          language: 'javascript',
          content: `function mySqrt(x) {
  if (x < 2) return x;

  let left = 1;
  let right = Math.floor(x / 2);

  while (left <= right) {
    const mid = Math.floor((left + right) / 2);
    const sq = mid * mid;

    if (sq === x) return mid;
    if (sq < x) left = mid + 1;
    else right = mid - 1;
  }

  return right; // целая часть корня
}`,
        },
        {
          type: 'text',
          content:
            'Это бинпоиск по ответу в чистом виде: ищется максимальное \`m\`, при котором \`m * m <= x\`. Таким же приёмом находится кубический корень или обратная функция любой монотонной функции — например, минимальная пропускная способность сервера для заданной нагрузки.',
        },
        {
          type: 'heading',
          content: 'Где встречается в реальной жизни',
        },
        {
          type: 'list',
          content: `Индексы баз данных (B-tree, B+tree) — это многоуровневый бинарный поиск. Каждый \`SELECT WHERE id = ?\` использует его.
\`git bisect\` — находит коммит с регрессией за O(log n) запусков тестов.
Sorted Set в Redis (ZRANGEBYSCORE, ZRANGEBYLEX) — внутри тоже бинпоиск.
\`_.sortedIndex\` в lodash — поиск позиции вставки.
Range-запросы во временных рядах (Prometheus, InfluxDB) — на основе бинпоиска по таймстемпам.`,
        },
        {
          type: 'text',
          content:
            'Эвристика на собес и на практику: если в задаче есть «поиск», «минимум», «максимум» — и данные хоть как-то упорядочены — попробуйте бинарный поиск. Чаще всего он либо решает задачу, либо хотя бы подсказывает, куда копать.',
        },
      ],
    },
  ],

  // Все остальные вопросы квиза — те, что не использованы как checkpoint
  finalQuiz: binarySearchQuiz.questions.filter((q) => !CHECKPOINT_IDS.has(q.id)),

  cheatsheet: `### Шпаргалка по бинарному поиску

**Когда применять**
- Массив отсортирован и есть произвольный доступ по индексу (Array, не Linked List)
- Ответ — число из ограниченного диапазона + есть монотонный предикат \`check(x)\`

**Сложность**
- Время: O(log n)
- Память: O(1) (итеративно) или O(log n) (рекурсивно)

**Классический шаблон — закрытый интервал [left; right]**
\`\`\`js
while (left <= right) {
  const mid = Math.floor((left + right) / 2);
  if (arr[mid] === target) return mid;
  if (arr[mid] < target) left = mid + 1;
  else right = mid - 1;
}
return -1;
\`\`\`

**Левая граница (lower_bound)**
- Не возвращать сразу при равенстве, идти левее
- \`if (arr[mid] >= target) right = mid - 1\`
- После цикла \`left\` — индекс первого элемента \`>= target\`

**Бинпоиск по ответу — полуоткрытый [lo; hi)**
\`\`\`js
while (lo < hi) {
  const mid = Math.floor((lo + hi) / 2);
  if (check(mid)) hi = mid;
  else lo = mid + 1;
}
return lo;
\`\`\`

**Правила, которые надо помнить**
- Знаки в условии и сдвиги границ — «комплектом»: \`<=\` ↔ \`mid ± 1\`, \`<\` ↔ \`mid\`
- Каждая итерация должна **строго** сужать диапазон — иначе бесконечный цикл
- Портабельная формула: \`mid = left + Math.floor((right - left) / 2)\`

**Граничные случаи на собеседовании**
- Пустой массив
- Массив из одного элемента (есть/нет target)
- target меньше всех / больше всех
- target на левой/правой границе массива
- Дубликаты (для классики результат неопределён, нужно lower/upper_bound)`,

  nextTopics: [
    {
      slug: 'two-pointers',
      reason:
        'Два указателя — естественное обобщение идеи: оба алгоритма работают на упорядоченных данных и сводят квадратичные задачи к линейным или логарифмическим.',
    },
    {
      slug: 'sliding-window',
      reason:
        'Скользящее окно — частный случай двух указателей. После бинпоиска удобно закрепить мышление инвариантами на массивах.',
    },
  ],
};

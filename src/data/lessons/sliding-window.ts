import type { Lesson } from '../../types/lesson';
import { slidingWindowQuiz } from '../quizzes/sliding-window';

const Q = Object.fromEntries(slidingWindowQuiz.questions.map((q) => [q.id, q]));

// Questions used as in-chapter checkpoints — не должны дублироваться в finalQuiz.
const CHECKPOINT_IDS = new Set(['sw-q2', 'sw-q4', 'sw-q5', 'sw-q6', 'sw-q10']);

export const slidingWindowLesson: Lesson = {
  topicId: 'sliding-window',

  intro: {
    whyItMatters: `Скользящее окно превращает квадратичный перебор подмассивов и подстрок в линейный O(n). Вместо того чтобы для каждого подотрезка пересчитывать агрегат с нуля, мы держим одно окно и обновляем агрегат инкрементально: добавили элемент справа, убрали слева.

Эта техника закрывает целое семейство задач вида «непрерывный подотрезок с условием»: максимальная сумма подмассива длины k, длиннейшая подстрока без повторов, минимальное окно подстроки, окно с не более чем K уникальных символов. На собеседовании это один из трёх базовых паттернов наряду с бинарным поиском и двумя указателями.`,
    estimatedMinutes: 28,
    interviewAngle:
      'Интервьюер проверяет три вещи. Первое — классифицирует ли кандидат задачу (фиксированное окно или динамическое). Второе — может ли объяснить амортизированную O(n)-сложность через «каждый элемент входит в окно и выходит из него ровно один раз». Третье — видит ли границы применимости: отрицательные числа в задачах на сумму, подпоследовательности вместо подмассивов. Если все три пункта проговорить — дальше уже сложно ошибиться даже в коде.',
    prerequisites: [{ slug: 'two-pointers', title: 'Два указателя' }],
  },

  chapters: [
    // ─────────────────────────────────────────────────────────────
    {
      id: 'idea',
      title: 'Идея скользящего окна',
      estimatedMinutes: 4,
      blocks: [
        {
          type: 'text',
          content:
            'Простая задача для разогрева: найти максимальную сумму подмассива длины 3 в \`[2, 1, 5, 1, 3, 2]\`. Лобовое решение — перебрать все подмассивы длины 3, сложить элементы каждого, найти максимум. Это O(n·k): для каждого из n стартов делаем k сложений.',
        },
        {
          type: 'text',
          content:
            'Заметим, что соседние окна почти совпадают: \`[2,1,5]\` и \`[1,5,1]\` отличаются на одно число слева и одно справа. Не нужно пересчитывать всю сумму — достаточно прибавить новый элемент и вычесть выпавший. Это и есть «скольжение окна»: один проход вместо n·k.',
        },
        {
          type: 'text',
          content:
            'Скользящее окно — паттерн для задач, где ищется непрерывный подмассив или подстрока с заданным свойством (максимальная сумма, уникальные символы, ограничение по частотам). Поддерживается одно окно \`[left; right]\`, края которого сдвигаются по мере необходимости.',
        },
        {
          type: 'text',
          content:
            'Главное правило техники — оба указателя движутся только вправо. Каждый элемент входит в окно ровно один раз и выходит из него ровно один раз — отсюда амортизированная O(n) сложность. Главный вопрос при решении: когда расширять окно, а когда сжимать.',
        },
        {
          type: 'visualization',
          content: '',
          vizId: 'sliding-window',
        },
        { type: 'heading', content: 'Два варианта окна' },
        {
          type: 'list',
          content: `Фиксированное окно: длина \`k\` задана условием. Окно сдвигается строго на один элемент за итерацию.
Динамическое окно: длина меняется в зависимости от условия. Расширяется правым указателем и сжимается левым, пока условие выполняется или нарушается.
Агрегат окна: сумма, количество уникальных, максимум, частоты символов в \`Map\` или \`Set\` — то, что обновляется дёшево при сдвиге границ.`,
        },
        {
          type: 'callout',
          calloutType: 'info',
          content:
            'Скользящее окно — частный случай двух указателей, где оба движутся в одну сторону. Если указатели идут навстречу — это уже не скользящее окно, а сходящиеся указатели из прошлой темы.',
        },
      ],
      checkpoint: [Q['sw-q5']!, {
        type: 'ordering',
        id: 'sw-ord1',
        description: 'Расставьте шаги динамического скользящего окна в правильном порядке',
        items: [
          'Инициализировать left = 0, right = 0, агрегат и результат',
          'Расширить окно: добавить arr[right] в агрегат',
          'Пока окно нарушает условие — сжать: убрать arr[left], left++',
          'Обновить результат (длина, сумма и т. д.)',
          'right++ для следующей итерации',
        ],
        explanation:
          'Шаблон: расширить право → сжать слева до валидного состояния → обновить ответ. Оба указателя двигаются только вправо, поэтому суммарное число итераций — O(n), а не O(n²).',
      }],
    },

    // ─────────────────────────────────────────────────────────────
    {
      id: 'fixed-window',
      title: 'Фиксированное окно',
      estimatedMinutes: 5,
      blocks: [
        {
          type: 'text',
          content:
            'Фиксированное окно — самый простой вариант: размер \`k\` задан заранее. Алгоритм в две фазы:\n- набирается первое окно — суммируются первые \`k\` элементов;\n- сдвигается вправо: добавляется \`arr[i]\`, убирается \`arr[i - k]\`.\n\nКаждый сдвиг — O(1) вместо пересчёта всей суммы.',
        },
        {
          type: 'visualization',
          content: '',
          vizId: 'fixed-window',
        },
        {
          type: 'code',
          language: 'javascript',
          content: `function maxSumSubarray(arr, k) {
  if (arr.length < k) return 0;

  // Фаза 1: сумма первых k элементов
  let sum = 0;
  for (let i = 0; i < k; i++) {
    sum += arr[i];
  }
  let maxSum = sum;

  // Фаза 2: сдвиг окна на 1 вправо за O(1)
  for (let i = k; i < arr.length; i++) {
    sum = sum + arr[i] - arr[i - k];
    maxSum = Math.max(maxSum, sum);
  }

  return maxSum;
}`,
        },
        { type: 'heading', content: 'Почему именно arr[i - k]' },
        {
          type: 'text',
          content:
            'Когда \`i\` дошло до значения \`i\`, окно содержит индексы \`[i - k + 1; i]\`. Элемент \`arr[i - k]\` — тот, что только что покинул окно: он был левой границей до сдвига. Запись \`arr[i - k + 1]\` или \`arr[i - 1]\` — классическая off-by-one ошибка.',
        },
        {
          type: 'callout',
          calloutType: 'warning',
          content:
            'Если сомневаетесь — нарисуйте маленький пример (массив из 4 элементов, окно длины 2) и подпишите, какой индекс покидает окно при первом сдвиге. Тридцать секунд на бумаге экономят двадцать минут отладки.',
        },
        {
          type: 'text',
          content:
            'Пример: \`maxSumSubarray([2, 1, 5, 1, 3, 2], 3)\`. Окна: \`[2,1,5]=8\`, \`[1,5,1]=7\`, \`[5,1,3]=9\`, \`[1,3,2]=6\`. Ответ — 9.',
        },
        { type: 'heading', content: 'Где применяется' },
        {
          type: 'list',
          content: `Размер окна явно задан в условии: «среди всех подмассивов длины k…».
Средние, максимумы, суммы по подмассивам фиксированной длины.
Скользящие метрики в реальном времени: «среднее за последние 60 секунд».
Подсчёт уникальных в окне фиксированной длины (с \`Map\` или \`Set\`).`,
        },
      ],
      checkpoint: [Q['sw-q2']!],
    },

    // ─────────────────────────────────────────────────────────────
    {
      id: 'dynamic-window',
      title: 'Динамическое окно',
      estimatedMinutes: 7,
      blocks: [
        {
          type: 'text',
          content:
            'Динамическое окно — размер меняется в зависимости от условия. Шаблон один:\n- расширить право (\`right++\`, добавить \`arr[right]\` в агрегат);\n- сжать слева, пока окно нарушает (или удовлетворяет) условию;\n- обновить ответ.\n\nОба указателя движутся только вправо, поэтому, несмотря на вложенный \`while\`, сложность остаётся O(n).',
        },
        {
          type: 'visualization',
          content: '',
          vizId: 'dynamic-window',
        },
        { type: 'heading', content: 'Универсальный шаблон' },
        {
          type: 'code',
          language: 'javascript',
          content: `function dynamicWindow(arr) {
  let left = 0;
  let agg = 0;          // агрегат: сумма, счётчик и т. д.
  let answer = 0;       // ответ: длина, количество, экстремум

  for (let right = 0; right < arr.length; right++) {
    // 1. Расширяем окно
    agg += arr[right];

    // 2. Сжимаем, пока окно нарушает условие
    while (/* условие */ false) {
      agg -= arr[left];
      left++;
    }

    // 3. Обновляем ответ для текущего окна
    answer = Math.max(answer, right - left + 1);
  }

  return answer;
}`,
        },
        {
          type: 'callout',
          calloutType: 'tip',
          content:
            'Запомните одну вещь, и дальше большинство задач становятся механическими: при минимальной длине ответ обновляется внутри \`while\` (перед сдвигом \`left\`), при максимальной — после \`while\` (когда окно снова валидно). Дальше два примера, каждый покрывает один из этих случаев.',
        },
        { type: 'heading', content: 'Пример: кратчайший подмассив с суммой ≥ target' },
        {
          type: 'code',
          language: 'javascript',
          content: `function minSubarrayLen(target, nums) {
  let left = 0;
  let sum = 0;
  let minLen = Infinity;

  for (let right = 0; right < nums.length; right++) {
    sum += nums[right];

    // Сжимаем, пока окно ВСЁ ЕЩЁ удовлетворяет условию,
    // фиксируя минимальную длину
    while (sum >= target) {
      minLen = Math.min(minLen, right - left + 1);
      sum -= nums[left];
      left++;
    }
  }

  return minLen === Infinity ? 0 : minLen;
}`,
        },
        {
          type: 'text',
          content:
            'Здесь \`minLen\` обновляется внутри \`while\` — каждое валидное окно мы сначала фиксируем в ответе, потом сжимаем, теряя валидность. Если фиксировать после цикла, мы упустим момент, когда окно было самым коротким.',
        },
        { type: 'heading', content: 'Пример: длиннейшая подстрока без повторов' },
        {
          type: 'code',
          language: 'javascript',
          content: `function lengthOfLongestSubstring(s) {
  const set = new Set();
  let left = 0;
  let maxLen = 0;

  for (let right = 0; right < s.length; right++) {
    // Сжимаем, пока условие НАРУШЕНО (есть дубликат)
    while (set.has(s[right])) {
      set.delete(s[left]);
      left++;
    }
    set.add(s[right]);
    // После цикла окно валидно — обновляем ответ
    maxLen = Math.max(maxLen, right - left + 1);
  }
  return maxLen;
}`,
        },
        {
          type: 'text',
          content:
            'Здесь зеркальная картина: пока окно сломано (есть дубликат), сжимаем; вышли из \`while\` — окно снова валидно и его длину можно сравнить с максимумом. Если бы мы обновляли \`maxLen\` внутри \`while\`, мы бы считали длину невалидного окна.',
        },
        { type: 'heading', content: 'Почему сложность остаётся O(n)' },
        {
          type: 'text',
          content:
            'Внешний цикл делает \`n\` итераций. Внутренний \`while\` суммарно — тоже не более \`n\`: \`left\` инициализирован нулём, никогда не уменьшается и ограничен сверху \`n\`. Итого ~2n операций — амортизированная O(n). Это та же логика, что и в анализе двух указателей: оба индекса движутся в одну сторону и не возвращаются.',
        },
      ],
      playground: {
        starterCode: `// Реализуйте: длину длиннейшей подстроки без повторов.
// Ожидаемый вывод для "abcabcbb" — 3 (подстрока "abc").

function lengthOfLongestSubstring(s) {
  const set = new Set();
  let left = 0;
  let maxLen = 0;

  for (let right = 0; right < s.length; right++) {
    // сжимаем окно при дубликате, затем добавляем s[right]
    // и обновляем maxLen
  }

  return maxLen;
}

console.log(lengthOfLongestSubstring("abcabcbb"));`,
        expectedOutput: '3',
        description:
          'Допишите шаблон с использованием \`Set\`. Сжатие здесь — «пока окно не валидно». Ответ обновляется после \`while\`.',
      },
      checkpoint: [Q['sw-q4']!, Q['sw-q10']!],
    },

    // ─────────────────────────────────────────────────────────────
    {
      id: 'when-applies',
      title: 'Когда техника работает',
      estimatedMinutes: 5,
      blocks: [
        {
          type: 'text',
          content:
            'Скользящее окно применимо, когда расширение и сжатие окна меняют агрегат в предсказуемую сторону. Для задач на сумму это значит — массив из неотрицательных чисел. Добавили положительное — сумма выросла, убрали — уменьшилась. Порядок строгий, жадные сжатия корректны.',
        },
        { type: 'heading', content: 'Сигналы «здесь скользящее окно»' },
        {
          type: 'list',
          content: `В условии — непрерывный подмассив или подстрока (не подпоследовательность).
Спрашивают минимум, максимум или количество среди подотрезков с агрегатным условием.
Наивное решение даёт O(n·k) или O(n²), а ожидается O(n).
Условие на агрегат: сумма, количество уникальных, максимум, частоты символов.`,
        },
        { type: 'heading', content: 'Свойство монотонности' },
        {
          type: 'text',
          content:
            'Динамическое окно работает, когда расширение окна вправо монотонно меняет агрегат в одну сторону, а сжатие слева — в противоположную. Если это свойство нарушено, жадное сжатие пропустит оптимальное окно.',
        },
        { type: 'heading', content: 'Когда техника не работает' },
        {
          type: 'list',
          content: `Подпоследовательность вместо подмассива. Элементы подпоследовательности не обязаны быть соседними — окно не подойдёт.
Отрицательные числа в задачах на сумму. Удаление отрицательного элемента увеличивает сумму — монотонность ломается. Используются префиксные суммы с хеш-таблицей или монотонная очередь (deque).
Произвольная переставимость элементов в окне. Скользящее окно сохраняет порядок; если порядок не важен и допустима сортировка — это другая задача.
Окно зависит от глобального состояния всего массива. Если для каждого положения окна нужно знать что-то о всём массиве, инкрементальное обновление невозможно.`,
        },
        {
          type: 'callout',
          calloutType: 'warning',
          content:
            'Типичная ловушка — «найдите кратчайший подмассив с суммой ≥ target» на массиве с отрицательными числами. Стандартный шаблон даст неверный ответ. Корректное решение — префиксные суммы и монотонная очередь, это отдельная тема.',
        },
      ],
      checkpoint: [Q['sw-q6']!, {
        type: 'match-pairs',
        id: 'sw-mp1',
        description: 'Сопоставьте тип окна с условием его применения',
        pairs: [
          { left: 'Фиксированное окно', right: 'Размер окна задан заранее (k элементов)' },
          { left: 'Динамическое окно', right: 'Ищем min / max окно, удовлетворяющее условию' },
          { left: 'Скользящее окно неприменимо', right: 'Задача про подпоследовательность, а не подмассив' },
          { left: 'Скользящее окно неприменимо', right: 'Отрицательные числа в задачах на сумму' },
        ],
        explanation:
          'Фиксированное окно: «максимальная сумма k подряд идущих элементов». Динамическое: «минимальная подстрока, содержащая все символы». Подпоследовательности и отрицательные числа ломают монотонность — нужны другие техники.',
      }],
    },

    // ─────────────────────────────────────────────────────────────
    {
      id: 'pitfalls',
      title: 'Подводные камни',
      estimatedMinutes: 5,
      blocks: [
        {
          type: 'text',
          content:
            'Ошибки в скользящем окне делятся на три категории: неверная формула сдвига, забытое сжатие или забытый сдвиг \`right\`. Все три тихие — код не падает, а просто даёт неверный ответ на некоторых тестах. Самое надёжное — прогнать алгоритм руками на примере из 5–6 элементов.',
        },
        { type: 'heading', content: 'Off-by-one в формуле сдвига' },
        {
          type: 'code',
          language: 'javascript',
          content: `// АНТИПАТТЕРН: вычитается не тот элемент
for (let i = k; i < arr.length; i++) {
  sum = sum + arr[i] - arr[i - k + 1]; // ❌ off-by-one
  maxSum = Math.max(maxSum, sum);
}

// КОРРЕКТНО:
for (let i = k; i < arr.length; i++) {
  sum = sum + arr[i] - arr[i - k];     // ✓
  maxSum = Math.max(maxSum, sum);
}`,
        },
        {
          type: 'callout',
          calloutType: 'warning',
          content:
            'Когда \`i\` стало \`k\`, окно — это индексы \`[1; k]\`. Элемент, который только что вышел из окна, имеет индекс \`0 = i - k\`, а не \`1 = i - k + 1\`.',
        },
        { type: 'heading', content: 'Забытое сжатие окна' },
        {
          type: 'code',
          language: 'javascript',
          content: `// АНТИПАТТЕРН: сравниваем длину, но не двигаем left
function lengthOfLongestSubstring(s) {
  const set = new Set();
  let left = 0, maxLen = 0;
  for (let right = 0; right < s.length; right++) {
    if (set.has(s[right])) {
      // ❌ забыли while + set.delete + left++
    }
    set.add(s[right]);
    maxLen = Math.max(maxLen, set.size);
  }
  return maxLen;
}
// Для "abcabcbb" вернёт 3, но логика сломается на "pwwkew" → 4 вместо 3.`,
        },
        { type: 'heading', content: 'Забытый сдвиг right' },
        {
          type: 'code',
          language: 'javascript',
          content: `// АНТИПАТТЕРН: while с условием на right без инкремента
let left = 0, right = 0, sum = 0;
while (right < arr.length) {
  sum += arr[right];
  if (sum >= target) {
    sum -= arr[left];
    left++;
  }
  // ❌ забыли right++ → бесконечный цикл
}`,
        },
        {
          type: 'callout',
          calloutType: 'tip',
          content:
            'Шаблон \`for (let right = 0; ...; right++)\` защищает от этой ошибки автоматически. Если пишете через \`while\` — ручной \`right++\` легко пропустить, и тогда программа уходит в бесконечный цикл.',
        },
        { type: 'heading', content: 'Граничные случаи' },
        {
          type: 'list',
          content: `Пустой массив: \`arr.length === 0\` — большинство шаблонов корректно вернут 0 или \`Infinity\`, но это стоит проверить явно.
\`k > arr.length\`: окно не помещается. Возвращайте 0 или \`null\`, главное — не \`NaN\`.
\`k === 0\`: бессмысленный размер окна — лучше явно выбрасывать ошибку.`,
        },
      ],
      playground: {
        starterCode: `// Найдите и исправьте баг. Ожидается: 9 (сумма [5,1,3]).

function maxSumSubarray(arr, k) {
  let sum = 0;
  for (let i = 0; i < k; i++) sum += arr[i];
  let maxSum = sum;
  for (let i = k; i < arr.length; i++) {
    sum = sum + arr[i] - arr[i - k + 1]; // тут что-то не так
    maxSum = Math.max(maxSum, sum);
  }
  return maxSum;
}

console.log(maxSumSubarray([2, 1, 5, 1, 3, 2], 3));`,
        expectedOutput: '9',
        description:
          'Off-by-one: вычитается не тот элемент, который покинул окно. Замените \`arr[i - k + 1]\` на \`arr[i - k]\`.',
      },
    },

    // ─────────────────────────────────────────────────────────────
    {
      id: 'patterns',
      title: 'Канонические паттерны',
      estimatedMinutes: 5,
      blocks: [
        {
          type: 'text',
          content:
            'Большинство задач на скользящее окно — вариации трёх семей: фиксированное окно с формулой сдвига, динамическое окно на минимум и динамическое окно на максимум. Понимание этих трёх шаблонов закрывает большую часть собеседовательных задач.',
        },
        { type: 'heading', content: 'Максимальная сумма подмассива длины k' },
        {
          type: 'text',
          content:
            'Базовый паттерн фиксированного окна. Тренирует формулу сдвига и обработку случая \`arr.length < k\`. Реализация — выше в главе про фиксированное окно.',
        },
        { type: 'heading', content: 'Длиннейшая подстрока без повторов' },
        {
          type: 'text',
          content:
            'LeetCode 3, динамическое окно с \`Set\`. Ускоряется заменой \`Set\` на \`Map\` «символ → индекс»: при дубликате \`left\` сразу прыгает к \`map.get(s[right]) + 1\` вместо пошагового сдвига.',
        },
        {
          type: 'code',
          language: 'javascript',
          content: `function lengthOfLongestSubstring(s) {
  const map = new Map(); // символ → последний индекс
  let left = 0, maxLen = 0;

  for (let right = 0; right < s.length; right++) {
    if (map.has(s[right])) {
      // Math.max защищает от «отката» left назад,
      // если старое вхождение уже за пределами окна.
      left = Math.max(left, map.get(s[right]) + 1);
    }
    map.set(s[right], right);
    maxLen = Math.max(maxLen, right - left + 1);
  }
  return maxLen;
}`,
        },
        { type: 'heading', content: 'Минимальное окно подстроки' },
        {
          type: 'text',
          content:
            'LeetCode 76, hard. Используются две \`Map\`: \`need\` (что нужно) и счётчик \`have\` (сколько символов уже в нужном количестве). Когда \`have === required\` — окно валидно, можно сжимать слева. Частая задача на собеседованиях в крупных компаниях.',
        },
        { type: 'heading', content: 'Длиннейшая подстрока с не более чем K уникальных' },
        {
          type: 'text',
          content:
            'Динамическое окно с ограничением «не более K уникальных». \`Map\` хранит частоту, при \`map.size > K\` окно сжимается слева. Ключ удаляется из \`Map\`, когда счётчик опустился до нуля.',
        },
        { type: 'heading', content: 'Подмассивы с произведением меньше K' },
        {
          type: 'text',
          content:
            'LeetCode 713. Для каждого \`right\` число подмассивов с произведением \`< K\`, заканчивающихся в \`right\`, равно \`right - left + 1\`. Это общая идея для подсчёта подмассивов в окне.',
        },
        {
          type: 'callout',
          calloutType: 'info',
          content:
            'Три «семьи»: (1) фиксированное окно — формула сдвига; (2) динамическое на минимум — сжимаем, пока валидно; (3) динамическое на максимум — сжимаем, пока невалидно. Большинство задач сводятся к комбинации этих трёх шаблонов.',
        },
      ],
    },
  ],

  // Все вопросы квиза, не использованные как checkpoint
  finalQuiz: slidingWindowQuiz.questions.filter((q) => !CHECKPOINT_IDS.has(q.id)),

  cheatsheet: `### Шпаргалка по скользящему окну

**Когда применять**
- Непрерывный подмассив или подстрока (не подпоследовательность)
- Минимум / максимум / количество среди подотрезков с агрегатным условием
- Для задач на сумму — массив из неотрицательных чисел

**Сложность**
- Время: O(n) амортизированно
- Память: O(1) для скалярных агрегатов; O(k) или O(|alphabet|) для Set / Map

**Фиксированное окно**
\`\`\`js
let sum = 0;
for (let i = 0; i < k; i++) sum += arr[i];
let maxSum = sum;
for (let i = k; i < arr.length; i++) {
  sum = sum + arr[i] - arr[i - k];
  maxSum = Math.max(maxSum, sum);
}
\`\`\`
- Формула сдвига: \`+arr[i] -arr[i - k]\`
- Не \`arr[i - k + 1]\` и не \`arr[i - 1]\` — это off-by-one

**Динамическое окно**
\`\`\`js
let left = 0, agg = 0, answer = 0;
for (let right = 0; right < arr.length; right++) {
  agg += arr[right];
  while (/* условие */) {
    agg -= arr[left];
    left++;
  }
  answer = Math.max(answer, right - left + 1);
}
\`\`\`
- Минимальная длина → ответ внутри \`while\`, перед \`left++\`
- Максимальная длина → ответ после \`while\`, когда окно валидно

**Когда техника не работает**
- Подпоследовательность вместо подмассива
- Отрицательные числа в задачах на сумму → префиксные суммы и монотонная очередь
- Окно зависит от глобального состояния массива

**Граничные случаи**
- Пустой массив
- \`k > n\`, \`k === 0\`, \`k === n\`
- Все элементы одинаковые, дубликаты
- Target недостижим`,

  nextTopics: [
    {
      slug: 'hash-map',
      reason:
        'Скользящее окно часто комбинируется с хеш-таблицей для подсчёта частот символов или элементов в окне (длиннейшая подстрока без повторов, минимальное окно подстроки).',
    },
    {
      slug: 'two-pointers',
      reason:
        'Скользящее окно — частный случай двух указателей. Полезно вернуться и закрепить общие идеи на классических задачах с двумя указателями.',
    },
  ],
};

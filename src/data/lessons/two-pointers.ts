import type { Lesson } from '../../types/lesson';
import { twoPointersQuiz } from '../quizzes/two-pointers';

const Q = Object.fromEntries(twoPointersQuiz.questions.map((q) => [q.id, q]));

// Questions used as in-chapter checkpoints — не должны дублироваться в finalQuiz.
const CHECKPOINT_IDS = new Set(['tp-q1', 'tp-q3', 'tp-q5', 'tp-q11', 'tp-q14']);

export const twoPointersLesson: Lesson = {
  topicId: 'two-pointers',

  intro: {
    whyItMatters: `Техника двух указателей — приём, который превращает алгоритмы с вложенными циклами O(n²) в линейные O(n) без дополнительной памяти. Идея простая: вместо обхода всех пар двумя циклами используются два индекса, каждый из которых движется только в одну сторону.

Применяется в задачах на пары и тройки в отсортированных массивах (Two Sum II, 3Sum), in-place-операциях (удаление дубликатов, перемещение нулей), проверке палиндрома, слиянии отсортированных последовательностей. На собеседовании техника спрашивается часто — наряду с бинарным поиском и хеш-таблицами это один из базовых приёмов.`,
    estimatedMinutes: 30,
    interviewAngle:
      'Интервьюера интересует не сам шаблон, а понимание, почему сдвиг указателя не теряет ни одного потенциального ответа. Это утверждение опирается на монотонность — отсортированность массива или явный физический инвариант задачи. Проговаривание этого вслух показывает понимание, а не заучивание.',
    prerequisites: [{ slug: 'binary-search', title: 'Бинарный поиск' }],
  },

  chapters: [
    // ─────────────────────────────────────────────────────────────
    {
      id: 'how-it-works',
      title: 'Как работают два указателя',
      estimatedMinutes: 5,
      blocks: [
        {
          type: 'text',
          content:
            '**Два указателя** — паттерн обхода массива двумя индексами вместо вложенного цикла. Вложенный цикл даёт O(n²); два указателя — **O(n)**. Главное правило: каждый указатель движется только в одну сторону и не возвращается назад. Именно это гарантирует линейную сложность.',
        },
        {
          type: 'text',
          content:
            'Сигнал применить паттерн: отсортированный массив плюс задача на пары, либо требование сделать что-то in-place (без выделения нового массива). На собеседовании эту догадку обычно ждут от кандидата — не как подсказку.',
        },
        {
          type: 'visualization',
          content: '',
          vizId: 'two-pointers',
        },
        { type: 'heading', content: 'Два варианта техники' },
        {
          type: 'list',
          content: `**Сходящиеся (converging)**: \`left\` стартует в начале, \`right\` — в конце, указатели движутся навстречу друг другу. Применяется на отсортированных массивах и в задачах симметрии (палиндром).
**Параллельные (slow/fast)**: оба указателя идут в одну сторону. \`fast\` читает каждый элемент, \`slow\` отмечает позицию для записи. Применяется для in-place-модификаций и обнаружения цикла в связном списке.`,
        },
        {
          type: 'callout',
          calloutType: 'info',
          content:
            'Ключевое условие применимости — монотонность. У сходящихся указателей это отсортированность массива: сдвиг одного указателя гарантированно отбрасывает все «безнадёжные» пары. У параллельных — то, что элементы обрабатываются строго в порядке возрастания индекса.',
        },
        {
          type: 'text',
          content:
            'Сложность: **O(n)** по времени и **O(1)** по дополнительной памяти. Каждый указатель движется не более n раз в одном направлении, поэтому суммарное число итераций ограничено линейно.',
        },
      ],
      checkpoint: [Q['tp-q1']!, Q['tp-q5']!, {
        type: 'ordering',
        id: 'tp-ord1',
        description: 'Расставьте шаги поиска пары с суммой = target в отсортированном массиве',
        items: [
          'Поставить lo = 0, hi = arr.length - 1',
          'Вычислить sum = arr[lo] + arr[hi]',
          'Если sum === target — пара найдена',
          'Если sum < target — lo++',
          'Если sum > target — hi--',
        ],
        explanation:
          'Два указателя работают на отсортированном массиве. sum < target — левый элемент слишком мал, сдвигается lo. sum > target — правый слишком большой, сдвигается hi. Итог: O(n) вместо O(n²).',
      }],
    },

    // ─────────────────────────────────────────────────────────────
    {
      id: 'converging-pointers',
      title: 'Сходящиеся указатели',
      estimatedMinutes: 6,
      blocks: [
        {
          type: 'text',
          content:
            '**Сходящиеся указатели** — `left` стартует в начале, `right` — в конце. Они движутся навстречу, сужая пространство поиска. На каждом шаге смотрим на пару и решаем, кого из них двигать. Условие выхода — \`left < right\`: при равенстве указатели встретились.',
        },
        {
          type: 'visualization',
          content: '',
          vizId: 'converging-pointers',
        },
        { type: 'heading', content: 'Шаблон: пара с заданной суммой' },
        {
          type: 'code',
          language: 'javascript',
          content: `function twoSumSorted(nums, target) {
  let left = 0;
  let right = nums.length - 1;

  while (left < right) {
    const sum = nums[left] + nums[right];

    if (sum === target) return [left, right];
    if (sum < target) left++;   // нужно число побольше
    else right--;               // нужно число поменьше
  }

  return [-1, -1];
}`,
        },
        { type: 'heading', content: 'Почему сдвиг указателя корректен' },
        {
          type: 'text',
          content:
            'При \`sum < target\` элемент \`nums[left]\` не может быть частью ответа ни с одним элементом из диапазона \`(left; right]\`: массив отсортирован, и любая такая пара даст сумму ещё меньше. Значит \`left\` можно сдвинуть, не теряя ни одной потенциальной пары. Симметрично для \`sum > target\` и \`right\`.',
        },
        {
          type: 'callout',
          calloutType: 'tip',
          content:
            'Проговаривание этого рассуждения на собеседовании — главное, что отличает понимание алгоритма от заучивания шаблона. Каждый сдвиг указателя должен иметь логическое обоснование.',
        },
        { type: 'heading', content: 'Шаблон: проверка палиндрома' },
        {
          type: 'code',
          language: 'javascript',
          content: `function isPalindrome(s) {
  let left = 0;
  let right = s.length - 1;

  while (left < right) {
    if (s[left] !== s[right]) return false;
    left++;
    right--;
  }

  return true;
}`,
        },
        {
          type: 'text',
          content:
            'Тот же приём применяется в задачах Container With Most Water и Trapping Rain Water. Монотонность там опирается не на сортировку, а на физический инвариант: более низкая стенка ограничивает объём воды.',
        },
      ],
      checkpoint: [Q['tp-q11']!, {
        type: 'match-pairs',
        id: 'tp-mp1',
        description: 'Сопоставьте условие со сдвигом в Two Sum II',
        pairs: [
          { left: 'sum === target', right: 'Пара найдена — возвращаем индексы' },
          { left: 'sum < target', right: 'Нужно число побольше → left++' },
          { left: 'sum > target', right: 'Нужно число поменьше → right--' },
          { left: 'left >= right', right: 'Пространство поиска исчерпано' },
        ],
        explanation:
          'Каждое сравнение опирается на отсортированность массива. Сдвиг одного указателя отбрасывает все «безнадёжные» пары с этим элементом, не теряя ни одной потенциально подходящей.',
      }],
    },

    // ─────────────────────────────────────────────────────────────
    {
      id: 'parallel-pointers',
      title: 'Параллельные указатели (slow/fast)',
      estimatedMinutes: 6,
      blocks: [
        {
          type: 'text',
          content:
            '**Параллельные указатели** — оба идут в одну сторону, но с разной ролью:\n- \`fast\` читает всё подряд\n- \`slow\` отмечает позицию для записи следующего «нужного» элемента\n\nПозволяет фильтровать или модифицировать массив **in-place**, без выделения нового. Инвариант: \`slow\` никогда не опережает \`fast\`.',
        },
        {
          type: 'visualization',
          content: '',
          vizId: 'parallel-pointers',
        },
        { type: 'heading', content: 'Шаблон: удаление дубликатов in-place' },
        {
          type: 'code',
          language: 'javascript',
          content: `function removeDuplicates(nums) {
  if (nums.length === 0) return 0;

  let slow = 0;

  for (let fast = 1; fast < nums.length; fast++) {
    if (nums[fast] !== nums[slow]) {
      slow++;
      nums[slow] = nums[fast];
    }
  }

  return slow + 1; // длина уникального префикса
}`,
        },
        {
          type: 'callout',
          calloutType: 'warning',
          content:
            'Типичная ошибка — вернуть \`slow\`, а не \`slow + 1\`. Это индекс последнего уникального элемента, а длина массива на единицу больше.',
        },
        {
          type: 'text',
          content:
            'Инвариант цикла: после каждой итерации \`nums[0..slow]\` содержит все уникальные значения из \`nums[0..fast]\` исходного массива в порядке первого появления. Поскольку массив отсортирован, новый уникальный элемент всегда больше \`nums[slow]\`.',
        },
        { type: 'heading', content: 'Шаблон: слияние двух отсортированных массивов' },
        {
          type: 'code',
          language: 'javascript',
          content: `function merge(a, b) {
  const result = [];
  let i = 0;
  let j = 0;

  while (i < a.length && j < b.length) {
    if (a[i] <= b[j]) result.push(a[i++]);
    else result.push(b[j++]);
  }

  while (i < a.length) result.push(a[i++]);
  while (j < b.length) result.push(b[j++]);

  return result;
}`,
        },
        {
          type: 'text',
          content:
            'Это базовая операция merge sort. Каждый элемент посещается ровно один раз — итого O(n + m) времени и O(n + m) памяти (под результат). Если слияние нужно сделать in-place в массив \`a\` с запасом — алгоритм проходится с конца.',
        },
      ],
      playground: {
        starterCode: `// Задача: переместите все нули в конец массива, сохранив порядок
// ненулевых элементов. Решите за O(n) и O(1) дополнительной памяти.

function moveZeroes(nums) {
  // используйте slow/fast:
  // slow — позиция для следующего ненулевого элемента
  // fast — текущая позиция чтения
  return nums;
}

console.log(JSON.stringify(moveZeroes([0, 1, 0, 3, 12])));`,
        expectedOutput: '[1,3,12,0,0]',
        description:
          'Подсказка: при \`nums[fast] !== 0\` сделайте swap \`nums[slow]\` и \`nums[fast]\`, затем сдвиньте \`slow\`. После цикла все ненулевые элементы окажутся в начале, нули — в конце.',
      },
      checkpoint: [Q['tp-q3']!, Q['tp-q14']!, {
        type: 'multi-select',
        id: 'tp-ms1',
        description: 'Для каких задач подходит паттерн двух указателей?',
        options: [
          'Поиск пары с заданной суммой в отсортированном массиве',
          'Разворот строки или массива in-place',
          'Удаление дубликатов из отсортированного массива',
          'Поиск подстроки в строке',
          'Проверка палиндрома',
        ],
        correctIndices: [0, 1, 2, 4],
        explanation:
          'Два указателя подходят для пар в отсортированных данных, разворота, удаления дубликатов и проверки палиндрома. Поиск подстроки решается другим паттерном — скользящим окном или KMP.',
      }],
    },

    // ─────────────────────────────────────────────────────────────
    {
      id: 'when-to-apply',
      title: 'Когда применять и чем отличается от sliding window',
      estimatedMinutes: 5,
      blocks: [
        {
          type: 'text',
          content:
            'Два указателя подходят не для всех задач. Главный сигнал — отсортированный массив плюс задача на пары или тройки. Либо требование сделать что-то на месте, без аллокации нового массива. Если массив не отсортирован и сортировать нельзя — обычно подходит хеш-таблица.',
        },
        { type: 'heading', content: 'Сигналы в формулировке задачи' },
        {
          type: 'list',
          content: `Массив **отсортирован** или может быть отсортирован без потери индексов.
Нужна **пара** или **тройка** элементов с заданным свойством (сумма, разность, произведение).
Требуется **in-place**-модификация без дополнительной памяти.
Упоминаются **палиндром**, **слияние**, **разделение** или **симметрия**.`,
        },
        { type: 'heading', content: 'Чем два указателя отличаются от скользящего окна' },
        {
          type: 'text',
          content:
            'Скользящее окно — частный случай параллельных указателей, где \`left\` и \`right\` ограничивают **подмассив**, а внутри окна поддерживается агрегат: сумма, счётчик, частоты символов. Two-pointers в общем смысле не требует состояния внутри отрезка — каждый шаг решается локально, сравнением \`arr[left]\` и \`arr[right]\` или \`arr[fast]\` и \`arr[slow]\`.',
        },
        {
          type: 'callout',
          calloutType: 'tip',
          content:
            'Если задача звучит как «найдите подмассив максимальной длины с условием X» или «минимальное окно, удовлетворяющее Y» — это скользящее окно. Если «найдите пару / тройку» или «сделайте in-place» — это два указателя.',
        },
        { type: 'heading', content: 'Когда техника не работает' },
        {
          type: 'list',
          content: `Массив **не отсортирован**, а сортировка теряет нужные индексы. Пример: классический Two Sum (LeetCode 1) требует исходные индексы. Решение — хеш-таблица за O(n).
Свойство **немонотонно**: сдвиг указателя может пропустить ответ. Здесь нужны другие техники — динамическое программирование, рекурсия, BFS/DFS.
Требуется **возврат к пройденным элементам**. Two-pointers идёт строго вперёд или к центру и не возвращается назад.`,
        },
      ],
    },

    // ─────────────────────────────────────────────────────────────
    {
      id: 'pitfalls',
      title: 'Подводные камни',
      estimatedMinutes: 5,
      blocks: [
        {
          type: 'text',
          content:
            'Ошибки в двух указателях обычно одного типа: неверное условие выхода, забытый сдвиг указателя или некорректная инициализация. Код компилируется, проходит часть тестов — и падает только на крайних случаях. Полезная привычка — прогонять алгоритм вручную на массиве из двух элементов: большинство багов всплывает сразу.',
        },
        { type: 'heading', content: 'Off-by-one в условии цикла' },
        {
          type: 'text',
          content:
            'Для сходящихся указателей стандартное условие — \`while (left < right)\`. Запись \`left <= right\` приводит к тому, что один и тот же элемент учитывается дважды (в Two Sum получится пара \`[i, i]\`).',
        },
        {
          type: 'code',
          language: 'javascript',
          content: `// АНТИПАТТЕРН: при left === right элемент посчитается с самим собой
while (left <= right) {
  const sum = nums[left] + nums[right];
  // при left === right здесь sum === 2 * nums[left]
}`,
        },
        { type: 'heading', content: 'Забытый сдвиг указателя' },
        {
          type: 'code',
          language: 'javascript',
          content: `// АНТИПАТТЕРН: бесконечный цикл
while (left < right) {
  if (nums[left] + nums[right] === target) return [left, right];
  if (nums[left] + nums[right] < target) {
    // забыли left++
  } else {
    right--;
  }
}`,
        },
        {
          type: 'callout',
          calloutType: 'warning',
          content:
            'Правило: каждая ветка цикла должна **строго** сдвигать хотя бы один указатель. Это инвариант, гарантирующий завершение цикла за O(n).',
        },
        { type: 'heading', content: 'Дубликаты в задачах с тройками' },
        {
          type: 'text',
          content:
            'В 3Sum после нахождения подходящей тройки нужно пропустить все дубликаты \`nums[left]\` и \`nums[right]\` — иначе одна и та же тройка попадёт в ответ несколько раз.',
        },
        {
          type: 'code',
          language: 'javascript',
          content: `if (sum === 0) {
  result.push([nums[i], nums[left], nums[right]]);
  while (left < right && nums[left]  === nums[left + 1])  left++;
  while (left < right && nums[right] === nums[right - 1]) right--;
  left++;
  right--;
}`,
        },
        { type: 'heading', content: 'Выход за границу массива' },
        {
          type: 'callout',
          calloutType: 'warning',
          content:
            'В алгоритмах с параллельными указателями (например, merge) забытая проверка \`i < a.length\` приведёт к чтению \`undefined\`. В JavaScript это не выбросит ошибку, но даст некорректное сравнение и тихий баг.',
        },
      ],
      playground: {
        starterCode: `// Найдите и исправьте ошибку. Ожидаемый вывод: true.

function isPalindrome(s) {
  let left = 0;
  let right = s.length;     // ← подсказка: что-то не так

  while (left < right) {
    if (s[left] !== s[right]) return false;
    left++;
    right--;
  }
  return true;
}

console.log(isPalindrome('level'));`,
        expectedOutput: 'true',
        description:
          'Указатель \`right\` должен начинаться с \`s.length - 1\`, а не \`s.length\`. Иначе на первой итерации сравнивается \`s[0]\` с \`s[length]\`, что даёт \`undefined\` и ломает логику.',
      },
    },

    // ─────────────────────────────────────────────────────────────
    {
      id: 'advanced',
      title: 'Усложнения: 3Sum и голландский флаг',
      estimatedMinutes: 5,
      blocks: [
        {
          type: 'text',
          content:
            '3Sum — задача, где два указателя применяются внутри внешнего цикла. После сортировки массива фиксируется первый элемент тройки, а к остатку применяются сходящиеся указатели. Итог: O(n²) вместо O(n³). Один из самых частых алгоритмических вопросов на собеседованиях.',
        },
        { type: 'heading', content: '3Sum через сортировку и два указателя' },
        {
          type: 'text',
          content:
            'Задача: найти все уникальные тройки \`(a, b, c)\` в массиве, такие что \`a + b + c = 0\`. Наивное решение — три вложенных цикла, O(n³). С сортировкой и двумя указателями — O(n²).',
        },
        {
          type: 'code',
          language: 'javascript',
          content: `function threeSum(nums) {
  nums.sort((a, b) => a - b);
  const result = [];

  for (let i = 0; i < nums.length - 2; i++) {
    if (i > 0 && nums[i] === nums[i - 1]) continue; // пропускаем дубль

    let left = i + 1;
    let right = nums.length - 1;

    while (left < right) {
      const sum = nums[i] + nums[left] + nums[right];
      if (sum === 0) {
        result.push([nums[i], nums[left], nums[right]]);
        while (left < right && nums[left]  === nums[left + 1])  left++;
        while (left < right && nums[right] === nums[right - 1]) right--;
        left++;
        right--;
      } else if (sum < 0) left++;
      else right--;
    }
  }

  return result;
}`,
        },
        {
          type: 'callout',
          calloutType: 'info',
          content:
            'Сложность: сортировка O(n log n), внешний цикл O(n), внутренние два указателя O(n). Итого O(n²). Сортировка поглощается общей сложностью и не ухудшает асимптотику.',
        },
        { type: 'heading', content: 'Голландский флаг: три указателя за один проход' },
        {
          type: 'text',
          content:
            'Алгоритм Дейкстры сортирует массив из трёх различных значений (0, 1, 2) за O(n) времени и O(1) памяти. Используются три указателя: \`low\` — граница нулей, \`mid\` — текущий элемент, \`high\` — граница двоек.',
        },
        {
          type: 'code',
          language: 'javascript',
          content: `function sortColors(nums) {
  let low = 0;
  let mid = 0;
  let high = nums.length - 1;

  while (mid <= high) {
    if (nums[mid] === 0) {
      [nums[low], nums[mid]] = [nums[mid], nums[low]];
      low++;
      mid++;
    } else if (nums[mid] === 1) {
      mid++;
    } else {
      [nums[mid], nums[high]] = [nums[high], nums[mid]];
      high--; // mid не сдвигаем — пришедший элемент ещё не обработан
    }
  }
}`,
        },
        {
          type: 'callout',
          calloutType: 'tip',
          content:
            'При swap с \`high\` указатель \`mid\` не сдвигается: элемент, пришедший с правого конца, ещё не классифицирован — он может быть нулём, единицей или другой двойкой. Этот нюанс часто проверяется на собеседовании.',
        },
      ],
      checkpoint: [{
        type: 'ordering',
        id: 'tp-ord2',
        description: 'Расставьте шаги решения 3Sum в правильном порядке',
        items: [
          'Отсортировать массив',
          'Зафиксировать nums[i], пропустить дубликаты по i',
          'Применить сходящиеся указатели к подмассиву (i; n)',
          'При нахождении тройки — пропустить дубликаты по left и right',
          'Сдвинуть left и right после успешного шага',
        ],
        explanation:
          'Сортировка нужна, чтобы предикат «сумма меньше/больше нуля» стал монотонным. Пропуск дубликатов на обоих уровнях — единственный способ избежать повторяющихся троек в ответе.',
      }],
    },
  ],

  // Все вопросы квиза, не использованные как checkpoint
  finalQuiz: twoPointersQuiz.questions.filter((q) => !CHECKPOINT_IDS.has(q.id)),

  cheatsheet: `### Шпаргалка по двум указателям

**Когда применять**
- Отсортированный массив + задача на пару или тройку
- In-place-операции (удаление дубликатов, перемещение элементов)
- Проверка палиндрома, слияние отсортированных последовательностей

**Сложность**
- Время: O(n)
- Память: O(1)

**Сходящиеся указатели**
\`\`\`js
let left = 0, right = nums.length - 1;
while (left < right) {
  const sum = nums[left] + nums[right];
  if (sum === target) return [left, right];
  if (sum < target) left++;
  else right--;
}
\`\`\`
- Условие цикла строго \`<\` — иначе элемент сравнится сам с собой
- Сдвиг указателя обоснован отсортированностью

**Параллельные (slow/fast)**
\`\`\`js
let slow = 0;
for (let fast = 1; fast < nums.length; fast++) {
  if (nums[fast] !== nums[slow]) {
    slow++;
    nums[slow] = nums[fast];
  }
}
return slow + 1;
\`\`\`
- \`fast\` читает, \`slow\` пишет
- Длина результата — \`slow + 1\`, а не \`slow\`

**3Sum: сортировка + два указателя за O(n²)**
- Сортируйте массив
- Фиксируйте \`nums[i]\`, пропускайте дубликаты по \`i\`
- Применяйте сходящиеся указатели к остатку
- Пропускайте дубликаты по \`left\` и \`right\` после успеха

**Отличие от скользящего окна**
- Two-pointers — пара/тройка, in-place. Состояние между указателями не хранится.
- Sliding window — подмассив/подстрока с агрегатом (сумма, счётчик).

**Когда техника не работает**
- Массив не отсортирован и нужны исходные индексы → хеш-таблица
- Свойство немонотонно → DP, BFS/DFS
- Нужен возврат к пройденным элементам → стек, рекурсия

**Граничные случаи на собеседовании**
- Пустой массив, один элемент
- Все элементы одинаковые
- Дубликаты в массиве (особенно для 3Sum)
- target меньше всех сумм / больше всех сумм`,

  nextTopics: [
    {
      slug: 'sliding-window',
      reason:
        'Скользящее окно — частный случай параллельных указателей. Естественное продолжение: учимся поддерживать агрегат внутри окна.',
    },
    {
      slug: 'hash-map',
      reason:
        'Хеш-таблица — основная альтернатива двум указателям, когда массив не отсортирован или нужны исходные индексы. Вместе закрывают большинство задач на пары и тройки.',
    },
  ],
};

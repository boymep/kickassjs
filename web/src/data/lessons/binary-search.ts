import type { Lesson } from '../../types/lesson';
import { binarySearchQuiz } from '../quizzes/binary-search';

// Index existing quiz questions for reuse as checkpoints.
const Q = Object.fromEntries(binarySearchQuiz.questions.map((q) => [q.id, q]));

// Questions used as in-chapter checkpoints (must NOT appear in finalQuiz).
const CHECKPOINT_IDS = new Set(['bs-q1', 'bs-q3', 'bs-q6', 'bs-q9', 'bs-q14']);

export const binarySearchLesson: Lesson = {
  topicId: 'binary-search',

  intro: {
    whyItMatters: `Бинарный поиск — один из самых востребованных алгоритмов в практике фронтенд-разработчика. Он лежит в основе \`Array.prototype.indexOf\` для отсортированных коллекций, метода \`sortedIndex\` из lodash, индексов в SQL-базах и команды \`git bisect\`, которая ищет коммит с регрессией за O(log n) шагов вместо O(n). На бэкенде бинарный поиск используется в B-tree-индексах, в \`std::lower_bound\` и \`std::upper_bound\` C++, в реализации range-запросов и в задачах планирования (поиск минимально достаточного ресурса).

На собеседованиях бинарный поиск — это лакмусовая бумажка: он формально простой, но реализовать его без багов с первого раза удаётся не каждому кандидату. Интервьюер проверяет не знание шаблона, а внимание к границам: где \`<\`, а где \`<=\`; куда сдвигать \`right\` — на \`mid\` или на \`mid - 1\`; что вернуть, если элемент не найден; как избежать переполнения при вычислении \`mid\`.

Освоив тему, вы научитесь распознавать «скрытые» бинпоиски в реальных задачах: «найдите минимальную скорость, при которой...», «найдите версию, в которой появился баг», «найдите позицию вставки в отсортированный список». Это превращает квадратичные решения в логарифмические и часто решает задачу собеседования за несколько минут вместо получаса.`,
    estimatedMinutes: 32,
    interviewAngle:
      'Бинарный поиск спрашивают почти на каждом алгоритмическом собеседовании. Сильный кандидат не пишет шаблон по памяти, а аргументирует выбор инвариантов: какой полуинтервал поиска используется, почему граница сдвигается именно так, как доказывается завершение цикла.',
    prerequisites: [],
  },

  resources: {
    videos: [
      {
        source: 'youtube',
        id: 'GU7DpgHINWQ',
        title: 'Binary Search tutorial (C++ and Python)',
        channel: 'Errichto Algorithms',
        language: 'en',
        durationSec: 19 * 60,
        description: 'Подробный разбор шаблона с инвариантами от гроссмейстера соревновательного программирования.',
      },
      {
        source: 'youtube',
        id: 'MFhxShGxHWc',
        title: 'Binary Search Algorithm in 100 Seconds',
        channel: 'Fireship',
        language: 'en',
        durationSec: 100,
        description: 'Короткое визуальное введение для тех, кто впервые сталкивается с темой.',
      },
    ],
    links: [
      {
        url: 'https://cp-algorithms.com/num_methods/binary_search.html',
        title: 'Binary search — cp-algorithms',
        source: 'article',
        language: 'en',
        note: 'Каноническая глава e-maxx с разбором инвариантов и обобщением на бинпоиск по ответу.',
      },
      {
        url: 'https://neetcode.io/courses/dsa-for-beginners/8',
        title: 'Binary Search — NeetCode DSA course',
        source: 'article',
        language: 'en',
        note: 'Бесплатный курс с задачами, разобранными по шаблону.',
      },
      {
        url: 'https://developer.mozilla.org/ru/docs/Web/JavaScript/Reference/Global_Objects/Array/indexOf',
        title: 'Array.prototype.indexOf — MDN',
        source: 'mdn',
        language: 'ru',
        note: 'Линейный поиск в стандартной библиотеке: полезно сравнить с логарифмическим бинпоиском.',
      },
      {
        url: 'https://lodash.com/docs/4.17.15#sortedIndex',
        title: '_.sortedIndex — Lodash',
        source: 'article',
        language: 'en',
        note: 'Реализация поиска позиции вставки на бинарном поиске — пример прикладного использования.',
      },
      {
        url: 'https://learn.javascript.ru/array-methods',
        title: 'Методы массивов — learn.javascript.ru',
        source: 'learn-js',
        language: 'ru',
        note: 'Глава Ильи Кантора по работе с массивами — нужный фундамент перед изучением алгоритма.',
      },
    ],
  },

  chapters: [
    {
      id: 'how-it-works',
      title: 'Как работает бинарный поиск',
      estimatedMinutes: 5,
      blocks: [
        {
          type: 'text',
          content:
            '**Бинарный поиск** — это алгоритм поиска элемента в отсортированном массиве за время O(log n). На каждой итерации область поиска делится пополам: средний элемент сравнивается с искомым, и одна из половин отбрасывается.',
        },
        {
          type: 'visualization',
          content: '',
          vizId: 'binary-search',
        },
        { type: 'heading', content: 'Пошаговый пример: поиск числа 7 в массиве [1, 3, 5, 7, 9, 11]' },
        {
          type: 'list',
          content: `- Шаг 1: \`left = 0\`, \`right = 5\`, \`mid = 2\`. \`arr[2] = 5 < 7\` — сдвигаем \`left = mid + 1 = 3\`.
- Шаг 2: \`left = 3\`, \`right = 5\`, \`mid = 4\`. \`arr[4] = 9 > 7\` — сдвигаем \`right = mid - 1 = 3\`.
- Шаг 3: \`left = 3\`, \`right = 3\`, \`mid = 3\`. \`arr[3] = 7 === 7\` — нашли. Возвращаем индекс \`3\`.`,
        },
        {
          type: 'callout',
          calloutType: 'info',
          content:
            'На каждой итерации область поиска сокращается вдвое. Для массива из n элементов потребуется не более log₂(n) шагов: 32 шага хватает на массив из 4 миллиардов элементов.',
        },
      ],
      flashcardIds: ['bs-f1', 'bs-f2'],
      checkpoint: [Q['bs-q1']!, Q['bs-q6']!],
    },

    {
      id: 'classic-template',
      title: 'Классический шаблон',
      estimatedMinutes: 6,
      blocks: [
        {
          type: 'text',
          content:
            'Базовый шаблон ищет точное вхождение элемента \`target\` в отсортированном массиве. Если элемента нет — возвращается \`-1\`. Обратите внимание на инвариант цикла: на каждой итерации искомый элемент, если он есть, лежит в полуинтервале \`[left; right]\`.',
        },
        {
          type: 'code',
          language: 'javascript',
          content: `function binarySearch(arr, target) {
  let left = 0;
  let right = arr.length - 1;

  while (left <= right) {
    const mid = Math.floor((left + right) / 2);

    if (arr[mid] === target) {
      return mid;
    } else if (arr[mid] < target) {
      left = mid + 1;     // искомый элемент строго правее mid
    } else {
      right = mid - 1;    // искомый элемент строго левее mid
    }
  }

  return -1; // элемент не найден
}`,
        },
        {
          type: 'callout',
          calloutType: 'tip',
          content:
            'Запишите три условия выхода в одну формулу: «\`left = mid + 1\` или \`right = mid - 1\` обязательно сужает диапазон не менее чем на единицу». Это гарантирует завершение цикла.',
        },
        { type: 'heading', content: 'Поиск позиции вставки (lower_bound)' },
        {
          type: 'text',
          content:
            'Если элемент может повторяться или его может не быть, обычно нужна **позиция вставки**: индекс, в который следует поместить \`target\`, чтобы массив остался отсортированным. Это аналог \`std::lower_bound\` из C++.',
        },
        {
          type: 'code',
          language: 'javascript',
          content: `function searchInsert(arr, target) {
  let left = 0;
  let right = arr.length - 1;

  while (left <= right) {
    const mid = Math.floor((left + right) / 2);

    if (arr[mid] === target) return mid;
    else if (arr[mid] < target) left = mid + 1;
    else right = mid - 1;
  }

  // После цикла left === right + 1.
  // left — первый элемент >= target (или arr.length, если все меньше).
  return left;
}`,
        },
      ],
      flashcardIds: ['bs-f3', 'bs-f4', 'bs-f5'],
      checkpoint: [Q['bs-q3']!],
    },

    {
      id: 'bounds',
      title: 'Левая и правая границы',
      estimatedMinutes: 7,
      blocks: [
        {
          type: 'text',
          content:
            'Когда элемент встречается в массиве несколько раз, обычный шаблон возвращает «случайный» из найденных индексов — тот, на котором цикл успел сравнить \`mid\` с \`target\`. Чтобы найти **первое** или **последнее** вхождение, нужны слегка иные инварианты.',
        },
        { type: 'heading', content: 'Левая граница: первое вхождение target' },
        {
          type: 'code',
          language: 'javascript',
          content: `function searchLeftBound(arr, target) {
  let left = 0;
  let right = arr.length - 1;

  while (left <= right) {
    const mid = Math.floor((left + right) / 2);

    if (arr[mid] >= target) {
      right = mid - 1;    // не останавливаемся на найденном — ищем левее
    } else {
      left = mid + 1;
    }
  }

  // left — индекс первого элемента >= target.
  return left < arr.length && arr[left] === target ? left : -1;
}`,
        },
        {
          type: 'callout',
          calloutType: 'tip',
          content:
            'Ключевое отличие от классического шаблона: при \`arr[mid] === target\` вы не возвращаете \`mid\`, а сдвигаете \`right = mid - 1\`. Цикл «пробегает» по всем дубликатам и останавливается на самом левом.',
        },
        { type: 'heading', content: 'Почему left указывает на первое вхождение' },
        {
          type: 'text',
          content:
            'Когда цикл \`while (left <= right)\` завершается, **всегда** выполняется \`left === right + 1\`: указатели «перепрыгнули» друг через друга. \`right\` указывает на последний элемент строго меньше \`target\`, а \`left\` — на первый элемент больший или равный \`target\`. Это инвариант, который удобно держать в голове.',
        },
        {
          type: 'code',
          language: 'javascript',
          content: `// Пример: arr = [1, 3, 3, 3, 5], target = 3
//
// Шаг 1: left=0, right=4, mid=2, arr[2]=3 >= 3 → right=1
// Шаг 2: left=0, right=1, mid=0, arr[0]=1 < 3  → left=1
// Шаг 3: left=1, right=1, mid=1, arr[1]=3 >= 3 → right=0
//
// Цикл завершён: left=1, right=0  (left === right + 1)
// left=1 — индекс первого вхождения 3 ✓`,
        },
        { type: 'heading', content: 'Правая граница: последнее вхождение target' },
        {
          type: 'code',
          language: 'javascript',
          content: `function searchRightBound(arr, target) {
  let left = 0;
  let right = arr.length - 1;
  let result = -1;

  while (left <= right) {
    const mid = Math.floor((left + right) / 2);

    if (arr[mid] === target) {
      result = mid;
      left = mid + 1;     // запоминаем и продолжаем правее
    } else if (arr[mid] < target) {
      left = mid + 1;
    } else {
      right = mid - 1;
    }
  }

  return result;
}`,
        },
      ],
      playground: {
        starterCode: `// Проследите выполнение поиска левой границы числа 4 в [1, 4, 4, 4, 7].
// На какой индекс будет указывать left после завершения цикла?
// Подсказка: пройдите по итерациям руками и подсчитайте.

const arr = [1, 4, 4, 4, 7];
const target = 4;

let left = 0;
let right = arr.length - 1;

while (left <= right) {
  const mid = Math.floor((left + right) / 2);
  if (arr[mid] >= target) right = mid - 1;
  else left = mid + 1;
}

console.log(left);`,
        expectedOutput: '1',
        description:
          'Цель — убедиться, что после цикла \`left\` указывает на первое вхождение элемента 4, то есть на индекс 1. Запустите код или посчитайте вручную и проверьте, совпадает ли результат с ожидаемым.',
      },
      flashcardIds: ['bs-f6', 'bs-f7'],
      checkpoint: [Q['bs-q9']!],
    },

    {
      id: 'binary-search-on-answer',
      title: 'Бинарный поиск по ответу',
      estimatedMinutes: 7,
      blocks: [
        {
          type: 'visualization',
          content: '',
          vizId: 'binary-search-answer',
        },
        {
          type: 'text',
          content:
            'Бинарный поиск применим не только к массивам. Если ответ задачи — число из непрерывного диапазона, и существует **монотонный** предикат \`check(x)\`: все значения до некоторого порога не подходят, а после — подходят, — то этот порог можно искать бинарным поиском.',
        },
        { type: 'heading', content: 'Шаблон поиска по ответу' },
        {
          type: 'code',
          language: 'javascript',
          content: `function binarySearchOnAnswer(lo, hi, check) {
  // check(x) возвращает true, если x удовлетворяет условию.
  // Предикат должен быть монотонным: ...false, false, true, true, true...
  while (lo < hi) {
    const mid = Math.floor((lo + hi) / 2);

    if (check(mid)) {
      hi = mid;          // mid подходит — пробуем уменьшить
    } else {
      lo = mid + 1;      // mid не подходит — двигаемся вправо
    }
  }

  return lo; // минимальное значение, удовлетворяющее условию
}`,
        },
        {
          type: 'callout',
          calloutType: 'tip',
          content:
            'Признак задачи: «найдите минимальное (или максимальное) значение X, при котором выполняется условие Y». Часто пространство ответов огромно, а проверка одного варианта — линейная. Бинпоиск превращает O(answer · n) в O(n · log answer).',
        },
        { type: 'heading', content: 'Где встречается на практике' },
        {
          type: 'list',
          content: `- Минимальная грузоподъёмность для перевозки за D дней (LeetCode 1011).
- Минимальная скорость поедания бананов (LeetCode 875, Koko).
- Поиск регрессии в \`git bisect\` — то же самое, только check(commit) — это запуск тестов.
- Поиск версии библиотеки, в которой появился баг.
- Поиск максимального минимального промежутка (минимакс) в задачах распределения ресурсов.`,
        },
      ],
      flashcardIds: ['bs-f8', 'bs-f9'],
    },

    {
      id: 'pitfalls',
      title: 'Типичные ошибки',
      estimatedMinutes: 5,
      blocks: [
        { type: 'heading', content: 'Off-by-one: <= или <' },
        {
          type: 'text',
          content:
            'Самая частая ошибка — путаница между \`while (left <= right)\` и \`while (left < right)\`. Эти два варианта работают с разными интервалами и требуют разных правил сдвига границ.',
        },
        {
          type: 'list',
          content: `- \`while (left <= right)\` — закрытый интервал \`[left; right]\`. Сдвиг: \`left = mid + 1\` или \`right = mid - 1\`.
- \`while (left < right)\` — полуоткрытый интервал \`[left; right)\`. Сдвиг: \`left = mid + 1\` или \`right = mid\` (без -1).`,
        },
        {
          type: 'callout',
          calloutType: 'warning',
          content:
            'Если вы пишете \`while (left < right)\` и сдвигаете \`right = mid - 1\`, вы можете пропустить ответ. Если \`while (left <= right)\` и \`right = mid\`, цикл может зациклиться при \`left + 1 === right\`. Выберите один шаблон и придерживайтесь его.',
        },
        { type: 'heading', content: 'Переполнение при вычислении mid' },
        {
          type: 'code',
          language: 'javascript',
          content: `// В JavaScript числа — double-precision (53 бита мантиссы), переполнение
// случится только на массивах длиной > 2^53. На практике — не проблема.
//
// В Java/C/C++ запись (left + right) / 2 при больших индексах
// переполняет int32. Переносимый шаблон:
const mid = left + Math.floor((right - left) / 2);`,
        },
        {
          type: 'callout',
          calloutType: 'info',
          content:
            'На собеседовании по JavaScript достаточно объяснить, что в JS переполнения нет, но упомянуть переносимый вариант — это плюс к ответу. В коде на C++ или Java переполнение приводит к отрицательному \`mid\` и к выходу за границы массива.',
        },
        { type: 'heading', content: 'Бесконечный цикл' },
        {
          type: 'code',
          language: 'javascript',
          content: `// АНТИПАТТЕРН: цикл зависнет на двухэлементном диапазоне
while (left < right) {
  const mid = Math.floor((left + right) / 2);
  if (arr[mid] === target) return mid;
  if (arr[mid] < target) left = mid;     // ← здесь нужна +1
  else right = mid;
}
// При left=0, right=1: mid=0, если arr[0] < target,
// left = 0 — никаких изменений → бесконечный цикл.`,
        },
        {
          type: 'callout',
          calloutType: 'warning',
          content:
            'Правило: каждая ветка цикла должна **строго** сокращать диапазон. Если оставляете \`left = mid\`, переходите на \`while (left < right)\` и убедитесь, что вторая ветка пишет \`right = mid - 1\` или симметрично.',
        },
      ],
      playground: {
        starterCode: `// Найдите первый отрицательный индекс или индекс первого true.
// Перед вами буггованный шаблон. Запустите код мысленно: что выведется?

const arr = [false, false, true, true, true];
let left = 0;
let right = arr.length - 1;

while (left < right) {
  const mid = Math.floor((left + right) / 2);
  if (arr[mid]) right = mid;
  else left = mid + 1;
}

console.log(left);`,
        expectedOutput: '2',
        description:
          'Это корректный шаблон с полуоткрытым интервалом \`[left; right)\`. После завершения цикла \`left === right\`, и эта позиция указывает на первый \`true\`. Цель — убедиться, что вы понимаете, почему сдвиг \`right = mid\` (без \`-1\`) здесь корректен.',
      },
      flashcardIds: ['bs-f10', 'bs-f11'],
    },

    {
      id: 'advanced',
      title: 'Усложнения и приложения',
      estimatedMinutes: 5,
      blocks: [
        { type: 'heading', content: 'Поиск в повёрнутом массиве' },
        {
          type: 'text',
          content:
            'Отсортированный массив может быть циклически сдвинут: \`[4, 5, 6, 7, 0, 1, 2]\`. Бинарный поиск по нему всё ещё работает за O(log n): на каждом шаге одна из половин относительно \`mid\` гарантированно отсортирована, и можно решить, в какой половине искать \`target\`.',
        },
        { type: 'heading', content: 'Поиск пика' },
        {
          type: 'text',
          content:
            'Если в массиве сначала возрастающая последовательность, а затем убывающая, пик находится за O(log n). На каждом шаге сравниваются \`arr[mid]\` и \`arr[mid + 1]\`: если \`>\` — пик слева или в \`mid\`, иначе — справа.',
        },
        { type: 'heading', content: 'Корень из числа за O(log x)' },
        {
          type: 'code',
          language: 'javascript',
          content: `function mySqrt(x) {
  if (x < 2) return x;
  let left = 1;
  let right = Math.floor(x / 2);

  while (left <= right) {
    const mid = Math.floor((left + right) / 2);
    const sq = mid * mid;

    if (sq === x) return mid;
    if (sq < x) left = mid + 1;
    else right = mid - 1;
  }

  return right; // целая часть корня
}`,
        },
        {
          type: 'callout',
          calloutType: 'tip',
          content:
            'Корень — это «бинпоиск по ответу» в чистом виде: ищем максимальный \`m\`, для которого \`m * m <= x\`. Этот же подход применяется к кубическому корню и к обратной функции любой монотонной функции.',
        },
      ],
      checkpoint: [Q['bs-q14']!],
      flashcardIds: ['bs-f12'],
    },
  ],

  // Все вопросы из старого квиза, кроме тех, что ушли в checkpoint.
  finalQuiz: binarySearchQuiz.questions.filter((q) => !CHECKPOINT_IDS.has(q.id)),

  flashcards: [
    {
      id: 'bs-f1',
      question: 'Что такое бинарный поиск и какие у него предусловия?',
      answer:
        'Алгоритм поиска элемента в отсортированном массиве за O(log n). Требует две вещи: массив должен быть отсортирован и допускать произвольный доступ (Array, не Linked List).',
      keyPoints: [
        'Только отсортированный массив',
        'Сложность O(log n) по времени, O(1) по памяти',
        'На связных списках не работает: нет O(1) доступа по индексу',
      ],
    },
    {
      id: 'bs-f2',
      question: 'Почему сложность бинарного поиска — O(log n)?',
      answer:
        'На каждой итерации область поиска сокращается вдвое. После k итераций остаётся n / 2^k элементов. Цикл завершается, когда n / 2^k < 1, то есть k = log₂(n).',
      keyPoints: [
        'Деление пополам → логарифм',
        'log₂(10⁹) ≈ 30 итераций',
        'log₂(2⁵³) ≈ 53 — предел Number в JS',
      ],
    },
    {
      id: 'bs-f3',
      question: 'Как выглядит классический шаблон бинарного поиска?',
      answer:
        'Закрытый интервал [left; right], цикл while (left <= right), три ветки: равенство, меньше, больше. Сдвиги — на mid + 1 или mid - 1.',
      keyPoints: [
        '`left = 0`, `right = arr.length - 1`',
        '`while (left <= right)`',
        'Ветки: `mid + 1` или `mid - 1` — обязательно сужают диапазон',
      ],
      code: `let left = 0, right = arr.length - 1;
while (left <= right) {
  const mid = Math.floor((left + right) / 2);
  if (arr[mid] === target) return mid;
  if (arr[mid] < target) left = mid + 1;
  else right = mid - 1;
}
return -1;`,
      codeLanguage: 'javascript',
    },
    {
      id: 'bs-f4',
      question: 'Чем lower_bound отличается от upper_bound?',
      answer:
        'lower_bound возвращает индекс первого элемента >= target. upper_bound — индекс первого элемента строго > target. Разница — в знаке сравнения внутри ветки.',
      keyPoints: [
        'lower_bound: arr[mid] >= target → right = mid - 1',
        'upper_bound: arr[mid] > target → right = mid - 1',
        'Количество вхождений = upper_bound - lower_bound',
      ],
    },
    {
      id: 'bs-f5',
      question: 'Что означает позиция, на которой left останавливается после цикла?',
      answer:
        'left указывает на первый элемент массива, который >= target. Если все элементы меньше target, left === arr.length. Это и есть позиция для вставки, чтобы массив остался отсортированным.',
      keyPoints: [
        'После цикла всегда `left === right + 1`',
        '`left` ∈ [0; arr.length] — корректная позиция вставки',
        'Этот инвариант одинаков для классического шаблона и для lower_bound',
      ],
    },
    {
      id: 'bs-f6',
      question: 'Как найти первое вхождение элемента, если в массиве дубликаты?',
      answer:
        'При arr[mid] === target не возвращайте mid, а сдвигайте right = mid - 1. Цикл продолжит сужать диапазон влево и остановится на самом левом вхождении.',
      keyPoints: [
        'Не возвращать сразу при равенстве',
        'Объединить ветки: `arr[mid] >= target` → `right = mid - 1`',
        'После цикла проверить `arr[left] === target` — иначе элемент отсутствует',
      ],
    },
    {
      id: 'bs-f7',
      question: 'Как найти последнее вхождение элемента?',
      answer:
        'Заведите переменную result = -1. При arr[mid] === target обновите result = mid и сдвиньте left = mid + 1, продолжая искать правее. После цикла верните result.',
      keyPoints: [
        'Запоминать найденный индекс',
        'Продолжать искать правее: `left = mid + 1`',
        'Альтернатива — использовать upper_bound и вычесть 1',
      ],
    },
    {
      id: 'bs-f8',
      question: 'Что такое бинарный поиск по ответу?',
      answer:
        'Это применение бинпоиска не к массиву, а к диапазону возможных ответов. Работает, если есть монотонный предикат check(x): все значения до порога не подходят, после — подходят.',
      keyPoints: [
        'Признак: «найдите минимальное X, при котором ...»',
        'Сложность: O(log answer · time(check))',
        'Примеры: Koko Eating Bananas, Ship Within Days, git bisect',
      ],
    },
    {
      id: 'bs-f9',
      question: 'Как доказать, что бинпоиск по ответу применим к задаче?',
      answer:
        'Нужно проверить два свойства: (1) есть скалярный ответ X из ограниченного диапазона, (2) предикат check(X) монотонен — если check(X) === true, то и check(X+1) === true.',
      keyPoints: [
        'Монотонность — главное свойство',
        'Без монотонности шаблон неприменим',
        'Часто монотонность доказывается «здравым смыслом»: больший ресурс = легче выполнить условие',
      ],
    },
    {
      id: 'bs-f10',
      question: 'Чем опасна запись `mid = (left + right) / 2`?',
      answer:
        'В языках с фиксированными int32 (Java, C, C++) сумма left + right может переполниться при больших значениях, и mid окажется отрицательным. Переносимая запись: `left + Math.floor((right - left) / 2)`.',
      keyPoints: [
        'В JavaScript Number — double, переполнения нет до 2^53',
        'В Java/C переполнение — реальный баг (известно с 2006 года)',
        'В JS также не забывайте `Math.floor` — без него mid дробный',
      ],
    },
    {
      id: 'bs-f11',
      question: 'Когда возникает бесконечный цикл в бинпоиске?',
      answer:
        'Когда сдвиг границы не уменьшает диапазон. Классика — `while (left < right)` с `left = mid` (без +1) при двухэлементном диапазоне: mid = left, и если ветка идёт сюда, диапазон не меняется.',
      keyPoints: [
        'Каждая ветка должна строго сужать [left; right]',
        '`while (left < right)` + `left = mid` без +1 = ловушка',
        'Тестируйте на массивах из 1 и 2 элементов',
      ],
    },
    {
      id: 'bs-f12',
      question: 'Как искать в повёрнутом отсортированном массиве?',
      answer:
        'На каждом шаге одна из половин (от left до mid или от mid до right) гарантированно отсортирована. Сравнив target с её границами, решаем, в какой половине искать. Алгоритм работает за O(log n).',
      keyPoints: [
        'Условие отсортированности левой половины: `nums[left] <= nums[mid]`',
        'Если target в отсортированной половине — идём туда',
        'Иначе — в другую половину',
      ],
    },
  ],

  cheatsheet: `### Шпаргалка по бинарному поиску

- **Предусловие**: массив отсортирован, доступ по индексу — O(1).
- **Сложность**: O(log n) по времени, O(1) по памяти.
- **Классический шаблон**: \`while (left <= right)\`, сдвиг \`mid + 1\` / \`mid - 1\`.
- **lower_bound**: при \`arr[mid] >= target\` → \`right = mid - 1\`. После цикла \`left\` — позиция вставки.
- **upper_bound**: то же, но условие \`arr[mid] > target\`.
- **Бинпоиск по ответу**: применим, если есть монотонный предикат \`check(x)\`.
- **Переносимый mid**: \`left + Math.floor((right - left) / 2)\`.
- **Тесты**: пустой массив, один элемент, target меньше всех, target больше всех, дубликаты.`,

  interviewQA: [
    {
      id: 'bs-iq1',
      question: 'Объясните инвариант бинарного поиска и почему он гарантирует корректность.',
      shortAnswer:
        'Инвариант — утверждение «если ответ существует, он находится в диапазоне [left; right]». Каждая итерация сохраняет инвариант, отбрасывая только заведомо неподходящие элементы.',
      fullAnswer: `Инвариант цикла — это утверждение, истинное перед каждой итерацией. Для классического бинарного поиска инвариант звучит так: «если \`target\` есть в массиве, его индекс находится в закрытом интервале \`[left; right]\`».

Перед первой итерацией это очевидно: \`left = 0\`, \`right = arr.length - 1\` — весь массив. На каждой итерации мы вычисляем \`mid\` и рассматриваем три случая:

1. \`arr[mid] === target\` — нашли, возвращаем индекс.
2. \`arr[mid] < target\` — поскольку массив отсортирован, все элементы \`arr[0..mid]\` тоже меньше \`target\`. Сдвигаем \`left = mid + 1\`.
3. \`arr[mid] > target\` — симметрично, все \`arr[mid..end]\` больше. Сдвигаем \`right = mid - 1\`.

После сдвига инвариант сохраняется: мы отбросили только те элементы, которые точно не равны \`target\`. Цикл завершается, когда \`left > right\` — диапазон пуст. По инварианту, если ответ был в исходном массиве, мы бы его нашли. Раз диапазон пуст и ответа не было — элемента нет, возвращаем -1.

Формально это пример доказательства корректности по инварианту цикла: инвариант истинен на входе, сохраняется итерацией и на выходе даёт нужное свойство.`,
      followUps: [
        'Как вы сформулируете инвариант для шаблона с полуоткрытым интервалом `[left; right)`?',
        'Почему именно `mid + 1` и `mid - 1`, а не `mid`? Что произойдёт без `+1`?',
      ],
      relatedChapterId: 'classic-template',
    },
    {
      id: 'bs-iq2',
      question: 'Чем lower_bound отличается от upper_bound? Приведите пример, когда нужны оба.',
      shortAnswer:
        '\`lower_bound\` возвращает позицию первого элемента >= target, \`upper_bound\` — позицию первого элемента строго > target. Разница в одном знаке сравнения. Применение — подсчёт количества вхождений: \`upper - lower\`.',
      fullAnswer: `Оба алгоритма основаны на одном и том же шаблоне поиска левой границы. Разница только в условии в ветке:

- \`lower_bound\`: \`if (arr[mid] >= target) right = mid - 1; else left = mid + 1;\`
- \`upper_bound\`: \`if (arr[mid] > target)  right = mid - 1; else left = mid + 1;\`

После цикла \`left\` указывает на первый элемент, удовлетворяющий условию (или \`arr.length\`, если такого нет).

**Зачем нужны оба.** Допустим, массив отсортирован: \`[1, 2, 2, 2, 3, 4]\`. Сколько раз встречается 2? Линейный проход — O(n). Бинпоиск: \`upper_bound(arr, 2) - lower_bound(arr, 2) = 4 - 1 = 3\`. Это работает за O(log n) и часто используется в задачах на интервальные запросы.

**Связь с языками.** В C++ \`std::lower_bound\` и \`std::upper_bound\` — стандартные функции STL. В JavaScript их нет в стандартной библиотеке, но \`_.sortedIndex\` и \`_.sortedLastIndex\` из lodash реализуют их под капотом.

**Граничные случаи.** Если \`target\` меньше всех элементов, оба возвращают 0. Если больше всех — \`arr.length\`. Если \`target\` точно совпадает с уникальным элементом, разница ровно 1.`,
      followUps: [
        'Как реализовать `upper_bound` через `lower_bound` без копирования кода?',
        'Что произойдёт с обоими шаблонами, если массив содержит NaN?',
      ],
      relatedChapterId: 'bounds',
    },
    {
      id: 'bs-iq3',
      question: 'В чём опасность записи `mid = (left + right) / 2` и почему её часто упоминают на собеседованиях?',
      shortAnswer:
        'В языках с фиксированными целыми (C, Java, int32) сумма \`left + right\` может переполниться, дать отрицательный mid и привести к выходу за границы массива. В JavaScript этой проблемы нет, но переносимая запись — \`left + Math.floor((right - left) / 2)\`.',
      fullAnswer: `Эта ловушка вошла в учебники после того, как в 2006 году инженер Google Джошуа Блох опубликовал заметку «Extra, Extra — Read All About It: Nearly All Binary Searches and Mergesorts are Broken». В стандартной реализации \`Arrays.binarySearch\` JDK при \`left = 1_000_000_000\` и \`right = 2_000_000_000\` сумма переполнялась и приводила к \`ArrayIndexOutOfBoundsException\`.

В JavaScript \`Number\` — это IEEE 754 double, целые числа точно представимы до \`2^53 - 1\`. На массивах разумного размера переполнения не возникнет. Но интервьюеры всё равно ждут, что вы упомянете переносимый вариант:

\`\`\`js
const mid = left + Math.floor((right - left) / 2);
\`\`\`

Эта запись сначала вычисляет половину диапазона, потом прибавляет к \`left\`. Промежуточное значение никогда не превышает \`right - left\`, поэтому переполнения не происходит даже на 32-битных целых.

**Связанные грабли в JavaScript.**
- Без \`Math.floor\` \`mid\` будет дробным, и \`arr[mid]\` вернёт \`undefined\`. Для целочисленного деления используют \`| 0\`, \`>> 1\` или \`Math.floor\`.
- \`(left + right) >> 1\` работает быстрее, но даёт неверный результат при значениях больше \`2^31\` из-за того, что побитовые операторы в JS работают с 32-битными целыми.

На собеседовании достаточно сказать: «В JS переполнения не будет, но я предпочитаю переносимую запись на случай переноса алгоритма на другой язык».`,
      followUps: [
        'Чем `>> 1` отличается от `Math.floor(... / 2)` на отрицательных числах?',
        'Что вообще делает оператор `| 0` с числом?',
      ],
      relatedChapterId: 'pitfalls',
    },
    {
      id: 'bs-iq4',
      question: 'Когда применима техника «бинарный поиск по ответу»? Приведите конкретный пример.',
      shortAnswer:
        'Когда ответ задачи — число из ограниченного диапазона и существует монотонный предикат \`check(x)\`. Классический пример — Koko Eating Bananas: ищем минимальную скорость поедания, при которой обезьяна успевает за заданное число часов.',
      fullAnswer: `Условия применимости два:

1. **Скалярный ответ.** Задача должна спрашивать число (или дискретное значение, упорядочиваемое). Подходит «минимальная скорость», «минимальная грузоподъёмность», «максимальный минимальный промежуток». Не подходит «найдите путь» или «найдите перестановку».
2. **Монотонный предикат.** Должна существовать функция \`check(x)\`, которая возвращает \`true\`/\`false\`, и быть монотонной: если \`check(x) === true\`, то \`check(x + 1) === true\` (или симметрично).

**Пример: Koko Eating Bananas.** Дан массив \`piles\` (кучи бананов) и время \`h\` часов. За час Коко съедает \`k\` бананов из выбранной кучи (если в куче меньше — съедает остаток и переходит к следующей через час). Найдите минимальную \`k\`, при которой Коко успевает.

\`\`\`js
function minEatingSpeed(piles, h) {
  const check = (k) =>
    piles.reduce((acc, p) => acc + Math.ceil(p / k), 0) <= h;

  let lo = 1;
  let hi = Math.max(...piles);

  while (lo < hi) {
    const mid = Math.floor((lo + hi) / 2);
    if (check(mid)) hi = mid;
    else lo = mid + 1;
  }

  return lo;
}
\`\`\`

Сложность: O(n · log(max(piles))). Без бинпоиска — линейный перебор скоростей даёт O(n · max(piles)).

**Признаки в формулировке.** «Минимальное», «максимальное», «оптимальное» в сочетании с проверяемым условием. Если задача звучит как «можем ли мы при ресурсе X выполнить задачу за время T» — почти наверняка тут бинпоиск по ответу.`,
      followUps: [
        'Какие границы вы возьмёте для lo и hi и почему?',
        'Что делать, если предикат не строго монотонен (есть локальные минимумы)?',
      ],
      relatedChapterId: 'binary-search-on-answer',
    },
    {
      id: 'bs-iq5',
      question: 'Как доказать, что бинарный поиск работает за O(log n)?',
      shortAnswer:
        'На каждой итерации диапазон сокращается как минимум вдвое. Через k итераций остаётся не более n / 2^k элементов. Цикл завершается, когда диапазон пуст, то есть при k ≈ log₂(n).',
      fullAnswer: `Пусть \`T(n)\` — число итераций для диапазона из n элементов. Каждая итерация:

1. Вычисляет \`mid\` за O(1).
2. Сравнивает \`arr[mid]\` с \`target\` за O(1).
3. Сдвигает одну из границ. После сдвига длина диапазона не превосходит \`floor(n / 2)\` (точнее, \`floor((n - 1) / 2)\` для случая \`right = mid - 1\`).

Получаем рекуррентное соотношение: \`T(n) ≤ T(n / 2) + 1\`, \`T(0) = 0\`.

По мастер-теореме (или просто разворачивая рекуррентность): \`T(n) = O(log n)\`.

**Альтернативное рассуждение.** Назовём \`d\` = \`right - left + 1\` — длина диапазона. Изначально \`d ≤ n\`. После каждой итерации \`d\` уменьшается как минимум в 2 раза. Значит, после \`k = ⌈log₂(n)⌉\` итераций \`d ≤ 1\`, и цикл сделает максимум одну дополнительную итерацию.

**Почему это важно.** Логарифм растёт очень медленно. Для миллиарда элементов нужно ~30 итераций, для триллиона — ~40. Это позволяет применять бинпоиск в горячих путях (lookup в индексах БД, поиск версии в \`git bisect\`).

**Память.** Итеративная реализация использует O(1) дополнительной памяти. Рекурсивная — O(log n) на стек вызовов из-за глубины рекурсии. На собеседовании предпочтительнее итеративная версия.`,
      followUps: [
        'Какова сложность бинпоиска на массиве длиной 10⁶ в худшем случае?',
        'Как изменится анализ, если на каждом шаге сравнение стоит O(L), где L — длина строки?',
      ],
      relatedChapterId: 'how-it-works',
    },
    {
      id: 'bs-iq6',
      question: 'Какие граничные случаи нужно тестировать в реализации бинарного поиска?',
      shortAnswer:
        'Пустой массив, массив из одного элемента, target меньше всех, target больше всех, target равен крайним элементам, дубликаты в массиве. Эти случаи покрывают типичные off-by-one ошибки.',
      fullAnswer: `Опытный разработчик начинает с тест-плана, ещё не написав код. Минимальный набор:

1. **Пустой массив**: \`binarySearch([], 5)\`. Цикл не должен начаться, должен вернуться -1.
2. **Один элемент, совпадение**: \`binarySearch([5], 5) === 0\`.
3. **Один элемент, нет совпадения**: \`binarySearch([5], 4) === -1\`.
4. **target меньше всех**: \`binarySearch([1, 2, 3], 0) === -1\`. Проверяет, что не уйдём в отрицательный индекс.
5. **target больше всех**: \`binarySearch([1, 2, 3], 100) === -1\`. Проверяет выход \`left\` за границу.
6. **target равен левой границе**: \`binarySearch([1, 2, 3], 1) === 0\`.
7. **target равен правой границе**: \`binarySearch([1, 2, 3], 3) === 2\`.
8. **Дубликаты**: для классического шаблона результат не определён, для lower_bound должен быть индекс первого вхождения.

Эти восемь тестов ловят почти все ошибки реализации. На собеседовании я обычно проговариваю их вслух перед кодом — это показывает зрелость инженера и часто помогает поймать баг до запуска.

**Дополнительно** для бинпоиска по ответу:
- \`lo === hi\` на старте (диапазон из одного значения).
- Все элементы массива одинаковые.
- Предикат всегда true или всегда false.`,
      followUps: [
        'Что должна вернуть функция при `arr === null` или `target === undefined`? Как это специфицировать?',
        'Как написать property-based тест, который проверяет соответствие бинпоиска и линейного поиска?',
      ],
      relatedChapterId: 'pitfalls',
    },
  ],

  nextTopics: [
    {
      slug: 'two-pointers',
      reason:
        'Два указателя — естественное обобщение бинарного поиска: оба алгоритма работают на отсортированных данных и сводят квадратичные задачи к линейным.',
    },
    {
      slug: 'sliding-window',
      reason:
        'Скользящее окно — частный случай двух указателей. После бинпоиска удобно переходить к нему, чтобы закрепить навык работы с инвариантами на массивах.',
    },
  ],
};

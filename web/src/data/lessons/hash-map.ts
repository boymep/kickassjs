import type { Lesson } from '../../types/lesson';
import { hashMapQuiz } from '../quizzes/hash-map';

// Index existing quiz questions for reuse as checkpoints.
const Q = Object.fromEntries(hashMapQuiz.questions.map((q) => [q.id, q]));

// Questions used as in-chapter checkpoints (must NOT appear in finalQuiz).
const CHECKPOINT_IDS = new Set(['hm-q1', 'hm-q5', 'hm-q7', 'hm-q10', 'hm-q15']);

export const hashMapLesson: Lesson = {
  topicId: 'hash-map',

  intro: {
    whyItMatters: `Хеш-таблица — структура, без которой не обходится ни один день фронтенд-разработчика. \`Map\` для подсчёта частот, \`Set\` для дедубликации списка пользователей, объект-словарь как кэш ответов API, мемоизация селекторов в Redux — всё это разные обёртки над одной и той же идеей: получить значение по ключу за O(1) в среднем.

Под капотом в V8 \`Map\` устроен сложнее, чем кажется. На маленьких размерах движок использует компактное представление с открытой адресацией, на больших — переходит к классическому хранилищу с массивом бакетов и цепочками для разрешения коллизий. Хеш-функция строится из адреса объекта или из побайтового представления строки и числа. Из-за этого константа O(1) — амортизированная: иногда вставка тянет за собой ребилд таблицы и стоит O(n), но в среднем по большой серии операций цена остаётся константной.

С \`Object\` ситуация ещё интереснее: V8 хранит объекты с одинаковой формой — одинаковые ключи, заданные в одинаковом порядке — через быструю структуру, похожую на массив. Но при динамическом добавлении или удалении ключей движок переключается в медленный режим, где внутри уже обычная хеш-таблица. \`Map\` спроектирован именно для частых вставок и удалений, не имеет конфликтов с именами прототипа (\`__proto__\`, \`constructor\`) и принимает ключом любое значение, включая объекты и \`NaN\`.

На собеседовании хеш-таблицы — это **рабочий инструмент**: половина задач Easy и Medium на LeetCode сводятся к «положи в Map то, что уже видел». Освоив тему, вы будете распознавать «скрытые» хеш-таблицы за секунды и превращать наивные O(n²)-решения в O(n).`,
    estimatedMinutes: 30,
    interviewAngle:
      'Хеш-таблицы спрашивают на каждом алгоритмическом и почти каждом JS-собеседовании. Сильный кандидат не пересказывает API \`Map\`, а объясняет, **почему** O(1) — амортизированная константа, чем \`Map\` отличается от \`Object\` на уровне движка и в каких сценариях операции деградируют до O(n).',
    prerequisites: [],
  },

  resources: {
    videos: [
      {
        source: 'youtube',
        id: 'b4b8ktEV4Bg',
        title: 'Hashing Algorithms and Security — Computerphile',
        channel: 'Computerphile',
        language: 'en',
        durationSec: 8 * 60,
        description: 'Майк Паунд объясняет суть хеш-функций и их свойств — короткое введение для тех, кто хочет понять «почему вообще это работает».',
      },
      {
        source: 'youtube',
        id: 'KLlXCFG5TnA',
        title: 'Two Sum — Leetcode 1 — HashMap — NeetCode',
        channel: 'NeetCode',
        language: 'en',
        durationSec: 12 * 60,
        description: 'Каноничный разбор Two Sum через Map: как линейный проход с lookup-таблицей превращает O(n²) в O(n).',
      },
    ],
    links: [
      {
        url: 'https://developer.mozilla.org/ru/docs/Web/JavaScript/Reference/Global_Objects/Map',
        title: 'Map — MDN',
        source: 'mdn',
        language: 'ru',
        note: 'Полный API: get, set, has, delete, итерация, итератор entries.',
      },
      {
        url: 'https://developer.mozilla.org/ru/docs/Web/JavaScript/Reference/Global_Objects/Set',
        title: 'Set — MDN',
        source: 'mdn',
        language: 'ru',
        note: 'Уникальные значения и операции пересечения/объединения.',
      },
      {
        url: 'https://learn.javascript.ru/map-set',
        title: 'Map и Set — учебник learn.javascript.ru',
        source: 'learn-js',
        language: 'ru',
        note: 'Глава Ильи Кантора с подробным сравнением Map vs Object и примерами WeakMap.',
      },
      {
        url: 'https://v8.dev/blog/hash-code',
        title: 'Optimizing hash tables: hiding the hash code — V8 blog',
        source: 'v8-blog',
        language: 'en',
        note: 'Как V8 хранит хеш-код объектов и почему это важно для производительности Map.',
      },
      {
        url: 'https://cp-algorithms.com/string/string-hashing.html',
        title: 'String hashing — cp-algorithms',
        source: 'article',
        language: 'en',
        note: 'Полиномиальное хеширование строк — фундамент быстрого сравнения строк и работы Set<string>.',
      },
    ],
  },

  chapters: [
    {
      id: 'how-it-works',
      title: 'Как устроена хеш-таблица',
      estimatedMinutes: 6,
      blocks: [
        {
          type: 'text',
          content:
            '**Хеш-таблица** — структура данных, в которой ключ преобразуется в индекс массива через **хеш-функцию** \`h(key) → int\`. Значение кладётся в бакет с этим индексом. На запрос \`get(key)\` мы заново вычисляем индекс и читаем значение за O(1).',
        },
        {
          type: 'visualization',
          content: '',
          vizId: 'hash-map',
        },
        {
          type: 'text',
          content:
            '**Хорошая хеш-функция** обладает тремя свойствами: (1) детерминирована — одинаковые входы дают одинаковый хеш; (2) равномерно распределяет значения по диапазону, минимизируя коллизии; (3) считается за O(1) от размера ключа (для строки длины L — за O(L)).',
        },
        {
          type: 'callout',
          calloutType: 'info',
          content:
            'Коллизия — ситуация, когда разные ключи имеют один хеш. Полностью избежать коллизий невозможно (принцип Дирихле). Поэтому каждая хеш-таблица содержит механизм разрешения: цепочки (chaining) или открытая адресация (open addressing).',
        },
        { type: 'heading', content: 'Два способа разрешения коллизий' },
        {
          type: 'list',
          content: `- **Цепочки (separate chaining)**: каждый бакет — связный список (или массив) пар \`{key, value}\`. Простая реализация, устойчива к высокой загрузке. Используется в Java HashMap.
- **Открытая адресация (open addressing)**: при коллизии ищем следующий свободный бакет (linear probing, quadratic probing, double hashing). Лучше работает с кэшем процессора, но требует rehash при загрузке > 50–70%.`,
        },
        {
          type: 'text',
          content:
            'V8 использует гибридный подход: маленькие \`Map\` (до ~16 элементов) хранятся в компактном представлении, большие — в классической хеш-таблице. Когда элементов становится слишком много относительно размера таблицы (примерно 75% заполнения), таблица **перестраивается**: выделяется массив большего размера, все ключи перехешируются. Эта операция стоит O(n), но случается раз на n вставок — поэтому в среднем O(1) на операцию.',
        },
      ],
      flashcardIds: ['hm-f1', 'hm-f2', 'hm-f3'],
      checkpoint: [Q['hm-q5']!],
      docsLink: { url: 'https://learn.javascript.ru/map-set', title: 'Map и Set — learn.javascript.ru' },
    },

    {
      id: 'frequency-pattern',
      title: 'Подсчёт частот и lookup-таблицы',
      estimatedMinutes: 6,
      blocks: [
        {
          type: 'visualization',
          content: '',
          vizId: 'frequency-count',
        },
        {
          type: 'text',
          content:
            'Самый распространённый паттерн с хеш-таблицей — **подсчёт частот**. Идём по коллекции один раз и для каждого элемента увеличиваем счётчик в \`Map\`. Сложность — O(n) по времени и O(k) по памяти, где k — число уникальных элементов.',
        },
        {
          type: 'code',
          language: 'javascript',
          content: `function countFrequencies(items) {
  const freq = new Map();

  for (const item of items) {
    freq.set(item, (freq.get(item) || 0) + 1);
  }

  return freq;
}

// countFrequencies("abracadabra")
// Map(5) { 'a' => 5, 'b' => 2, 'r' => 2, 'c' => 1, 'd' => 1 }`,
        },
        {
          type: 'callout',
          calloutType: 'tip',
          content:
            'Идиома \`(map.get(key) || 0) + 1\` — стандартный способ инкремента. Если ключа нет, \`get\` вернёт \`undefined\`, оператор \`||\` подставит 0. Альтернатива — \`map.set(k, (map.get(k) ?? 0) + 1)\` через nullish coalescing, если допустимы значения 0 как валидные.',
        },
        { type: 'heading', content: 'Lookup-таблица: паттерн Two Sum' },
        {
          type: 'visualization',
          content: '',
          vizId: 'two-sum-lookup',
        },
        {
          type: 'text',
          content:
            'Когда задача звучит как «найдите пару элементов с суммой / разностью / произведением, равным X», почти всегда это бинпоиск (если массив отсортирован) или **хеш-таблица как lookup**. Идём одним проходом, в Map складываем уже виденные элементы и для текущего проверяем, есть ли «дополнение».',
        },
        {
          type: 'code',
          language: 'javascript',
          content: `function twoSum(nums, target) {
  const seen = new Map(); // value -> index

  for (let i = 0; i < nums.length; i++) {
    const complement = target - nums[i];

    if (seen.has(complement)) {
      return [seen.get(complement), i];
    }
    seen.set(nums[i], i);
  }

  return [];
}`,
        },
        {
          type: 'callout',
          calloutType: 'tip',
          content:
            'Главный инсайт: проверка \`seen.has(complement)\` идёт **до** записи текущего элемента. Это естественно решает кейс с дубликатами: \`twoSum([3, 3], 6)\` корректно возвращает \`[0, 1]\`, а не \`[0, 0]\`.',
        },
      ],
      flashcardIds: ['hm-f4', 'hm-f5', 'hm-f6'],
      checkpoint: [Q['hm-q1']!, Q['hm-q10']!],
      playground: {
        starterCode: `// Реализуйте hasDuplicate через Set за один проход.
// Верните true, если в массиве есть хотя бы один повтор, иначе false.

function hasDuplicate(arr) {
  // ваш код
}

console.log(hasDuplicate([1, 2, 3, 2]));
console.log(hasDuplicate([1, 2, 3, 4]));`,
        expectedOutput: 'true\nfalse',
        description:
          'Классический паттерн «уже видел»: добавляем элементы в Set по одному и проверяем, есть ли уже такой. Решение — O(n) по времени, O(n) по памяти.',
      },
    },

    {
      id: 'grouping',
      title: 'Группировка по ключу',
      estimatedMinutes: 5,
      blocks: [
        {
          type: 'text',
          content:
            'Когда в задаче встречается «**сгруппируйте** элементы по признаку X», берите \`Map\`, где ключ — это X, а значение — массив элементов. Признак может быть любым: длина строки, остаток от деления, отсортированный набор букв, нормализованный URL.',
        },
        {
          type: 'code',
          language: 'javascript',
          content: `function groupAnagrams(strs) {
  const groups = new Map();

  for (const s of strs) {
    // Все анаграммы дают одинаковый отсортированный ключ.
    const key = s.split('').sort().join('');

    if (!groups.has(key)) {
      groups.set(key, []);
    }
    groups.get(key).push(s);
  }

  return Array.from(groups.values());
}

// groupAnagrams(["eat","tea","tan","ate","nat","bat"])
// → [["eat","tea","ate"], ["tan","nat"], ["bat"]]`,
        },
        {
          type: 'callout',
          calloutType: 'info',
          content:
            'В JS есть встроенный метод \`Object.groupBy(items, fn)\` (ES2024) и \`Map.groupBy\` для случая, когда ключи — объекты. До массового внедрения шаблон выше остаётся универсальным.',
        },
        { type: 'heading', content: 'Альтернативный ключ через частоты' },
        {
          type: 'text',
          content:
            'Сортировка строки длины m стоит O(m log m). Если m большое, сократите ключ до строки из 26 чисел — частот букв. Это убирает логарифм и даёт O(m) на ключ.',
        },
        {
          type: 'code',
          language: 'javascript',
          content: `function anagramKey(s) {
  const counts = new Array(26).fill(0);
  for (const ch of s) counts[ch.charCodeAt(0) - 97]++;
  return counts.join(',');
}`,
        },
      ],
      flashcardIds: ['hm-f7'],
      checkpoint: [Q['hm-q7']!],
    },

    {
      id: 'map-vs-object',
      title: 'Map vs Object vs WeakMap',
      estimatedMinutes: 7,
      blocks: [
        {
          type: 'text',
          content:
            'В JS три «хеш-подобные» структуры — \`Object\`, \`Map\`, \`WeakMap\` — решают разные задачи. Понимать различия важно: на собеседовании это один из самых частых уточняющих вопросов.',
        },
        { type: 'heading', content: 'Object — статические словари' },
        {
          type: 'list',
          content: `- Ключи приводятся к строкам или символам: \`obj[1]\` и \`obj["1"]\` — один ключ.
- Имеет прототипные ключи: \`toString\`, \`constructor\` — риск коллизии. Лечится через \`Object.create(null)\`.
- Размер — \`Object.keys(obj).length\`, нет встроенного \`.size\`.
- Хорошо подходит для статических словарей и JSON-сериализации.`,
        },
        { type: 'heading', content: 'Map — динамические коллекции' },
        {
          type: 'list',
          content: `- Ключ может быть **любого** типа: число, объект, функция, \`NaN\`, \`null\`.
- Сохраняет порядок вставки.
- Имеет \`.size\` — O(1).
- Оптимизирован для частого \`set\`/\`delete\`.
- Не имеет прототипных ключей — не нужно бояться \`__proto__\`.`,
        },
        {
          type: 'code',
          language: 'javascript',
          content: `// Object теряет тип ключа
const obj = {};
obj[1] = 'one';
obj['1'] = 'string-one';
console.log(Object.keys(obj));    // ['1']  — один ключ
console.log(obj[1]);              // 'string-one'

// Map различает 1 и "1"
const map = new Map();
map.set(1, 'one');
map.set('1', 'string-one');
console.log(map.size);            // 2
console.log(map.get(1));          // 'one'`,
        },
        { type: 'heading', content: 'WeakMap — ассоциация без удержания' },
        {
          type: 'text',
          content:
            'У \`WeakMap\` ключи — **только объекты**, и они удерживаются **слабо**. Если на ключ-объект больше нет ссылок снаружи, GC его соберёт, а запись из WeakMap исчезнет. Это идеальная структура для «прикрепить к объекту приватные данные, не мешая GC».',
        },
        {
          type: 'code',
          language: 'javascript',
          content: `const cache = new WeakMap();

function processNode(domNode) {
  if (cache.has(domNode)) return cache.get(domNode);

  const result = expensiveCompute(domNode);
  cache.set(domNode, result);
  return result;
}

// Когда DOM-узел удаляют со страницы и теряют все ссылки,
// запись из cache исчезнет автоматически — утечки нет.`,
        },
        {
          type: 'callout',
          calloutType: 'warning',
          content:
            'У WeakMap **нет** \`.size\`, итераторов и метода \`clear\`. Это сделано специально: иначе видимость ключей мешала бы сборщику мусора. Если нужно перебрать все элементы — берите обычный Map и думайте про утечки сами.',
        },
      ],
      flashcardIds: ['hm-f8', 'hm-f9', 'hm-f10'],
      docsLink: { url: 'https://developer.mozilla.org/ru/docs/Web/JavaScript/Reference/Global_Objects/Map', title: 'Map — MDN (ru)' },
    },

    {
      id: 'pitfalls',
      title: 'Типичные ошибки',
      estimatedMinutes: 5,
      blocks: [
        { type: 'heading', content: 'Объект как ключ — проверка по ссылке' },
        {
          type: 'code',
          language: 'javascript',
          content: `const map = new Map();
map.set({ id: 1 }, 'A');
map.set({ id: 1 }, 'B');

console.log(map.size);                    // 2 — это два разных объекта
console.log(map.get({ id: 1 }));          // undefined — третий объект, нет в Map`,
        },
        {
          type: 'callout',
          calloutType: 'warning',
          content:
            'Map сравнивает ключи через **SameValueZero** — для объектов это сравнение по ссылке. Если нужен ключ по содержимому объекта, используйте сериализацию (\`JSON.stringify\`) или композитный ключ-строку.',
        },
        { type: 'heading', content: 'Object и числовые ключи' },
        {
          type: 'code',
          language: 'javascript',
          content: `const counts = {};
counts[1] = 'a';
counts['1'] = 'b';
console.log(Object.keys(counts)); // ['1'] — один ключ, "1" перезаписал 1

// Если важен тип ключа — используйте Map
const m = new Map();
m.set(1, 'a');
m.set('1', 'b');
console.log(m.size); // 2`,
        },
        { type: 'heading', content: 'Прототипные ключи у Object' },
        {
          type: 'code',
          language: 'javascript',
          content: `const obj = {};
console.log(obj.toString); // function — наследуется от Object.prototype

// Безопасный словарь — без прототипа
const dict = Object.create(null);
console.log(dict.toString); // undefined`,
        },
        { type: 'heading', content: 'Деградация O(1) → O(n)' },
        {
          type: 'text',
          content:
            'В худшем случае все ключи попадают в один бакет и операции деградируют до O(n). На практике в V8 это почти невозможно — хеш-функция randomized. Но если в задаче явно говорят про **adversarial input** (например, hash flooding атака), упомяните эту деградацию: интервьюер ждёт.',
        },
      ],
      flashcardIds: ['hm-f11', 'hm-f12'],
      playground: {
        starterCode: `// Перед вами буггованный код. Что выведет console.log?
// Запустите мысленно или в реальности.

const seen = new Map();
const a = { id: 1 };
const b = { id: 1 };

seen.set(a, 'first');
console.log(seen.get(b));`,
        expectedOutput: 'undefined',
        description:
          'Map сравнивает объектные ключи **по ссылке**, а не по содержимому. \`a\` и \`b\` — два разных объекта, поэтому \`seen.get(b)\` возвращает \`undefined\`. Это классическая ловушка на собеседованиях.',
      },
    },
  ],

  // Все вопросы из старого квиза, кроме тех, что ушли в checkpoint.
  finalQuiz: hashMapQuiz.questions.filter((q) => !CHECKPOINT_IDS.has(q.id)),

  flashcards: [
    {
      id: 'hm-f1',
      question: 'Что такое хеш-функция и какими свойствами она должна обладать?',
      answer:
        'Функция, отображающая ключ произвольной длины в число фиксированного диапазона. Хорошая хеш-функция: детерминирована, равномерно распределяет значения, считается за O(L) от длины ключа.',
      keyPoints: [
        'Детерминированность: h(x) === h(x) всегда',
        'Равномерность распределения — минимум коллизий',
        'Скорость: O(1) от размера таблицы, O(L) от длины ключа',
        'В криптографии добавляются: одностороннесть и устойчивость к коллизиям',
      ],
    },
    {
      id: 'hm-f2',
      question: 'Что такое коллизия и какие есть стратегии её разрешения?',
      answer:
        'Коллизия — когда разные ключи дают одинаковый хеш. Решается двумя способами: цепочки (бакет — связный список) и открытая адресация (ищем следующий свободный слот).',
      keyPoints: [
        'Цепочки (separate chaining): просто, устойчиво, used by Java HashMap',
        'Открытая адресация (linear/quadratic probing): кэш-эффективно, требует rehash',
        'V8 использует гибрид: компактное хранение для маленьких Map',
        'Полностью избежать коллизий нельзя (принцип Дирихле)',
      ],
    },
    {
      id: 'hm-f3',
      question: 'Почему сложность операций хеш-таблицы — O(1) амортизированно, а не строго?',
      answer:
        'Большинство операций действительно O(1), но при превышении load factor таблица ребилдится: выделяется массив большего размера и все ключи перехешируются — это O(n). На длинной серии операций средняя стоимость остаётся константной.',
      keyPoints: [
        'Ребилд (rehash) — O(n), но случается раз на n вставок',
        'Амортизированная стоимость = (сумма всех операций) / (число операций)',
        'В худшем случае одна операция может занять O(n)',
        'При adversarial input возможна деградация до O(n) на каждую операцию',
      ],
    },
    {
      id: 'hm-f4',
      question: 'Опишите паттерн «подсчёт частот» с помощью Map.',
      answer:
        'Один проход по коллекции, для каждого элемента: `map.set(item, (map.get(item) || 0) + 1)`. Сложность — O(n) по времени, O(k) по памяти (k — число уникальных).',
      keyPoints: [
        'Идиома `(map.get(k) || 0) + 1`',
        'Если допустимы 0 как валидные значения — используйте `??`',
        'Применяется в подсчёте символов, проверке анаграмм, подсчёте дубликатов',
        'O(n) по времени и памяти',
      ],
      code: `const freq = new Map();
for (const item of arr) {
  freq.set(item, (freq.get(item) || 0) + 1);
}`,
      codeLanguage: 'javascript',
    },
    {
      id: 'hm-f5',
      question: 'Опишите паттерн lookup-таблицы на примере Two Sum.',
      answer:
        'Идём по массиву одним проходом. Для текущего nums[i] вычисляем complement = target - nums[i] и проверяем, есть ли он в Map. Если есть — пара найдена. Иначе — записываем nums[i] в Map.',
      keyPoints: [
        'Превращает O(n²) в O(n)',
        'Проверка ДО записи — корректно работает с дубликатами',
        'Применим в "найдите пару с суммой/разностью X"',
        'Память O(n)',
      ],
      code: `function twoSum(nums, target) {
  const seen = new Map();
  for (let i = 0; i < nums.length; i++) {
    const c = target - nums[i];
    if (seen.has(c)) return [seen.get(c), i];
    seen.set(nums[i], i);
  }
  return [];
}`,
      codeLanguage: 'javascript',
    },
    {
      id: 'hm-f6',
      question: 'Когда стоит брать Set вместо Map?',
      answer:
        'Когда важно только наличие элемента, а не сопоставленное значение. Set удобнее для дедубликации, проверки уникальности, реализации множественных операций (пересечение, объединение).',
      keyPoints: [
        'Set — это Map<K, true>',
        'Дедубликация массива: `[...new Set(arr)]`',
        'Проверка наличия за O(1)',
        'В ES2025 — методы intersection, union, difference',
      ],
    },
    {
      id: 'hm-f7',
      question: 'Опишите паттерн «группировка по ключу».',
      answer:
        'Для каждого элемента вычисляем ключ группы и кладём элемент в соответствующий массив в Map. Универсально для группировки анаграмм, по длине, по остатку от деления, по любому свойству.',
      keyPoints: [
        '`if (!map.has(key)) map.set(key, [])`',
        '`map.get(key).push(item)`',
        'Ключ — отсортированные буквы для анаграмм',
        'Альтернатива — Object.groupBy (ES2024)',
      ],
      code: `function groupBy(items, keyFn) {
  const groups = new Map();
  for (const item of items) {
    const k = keyFn(item);
    if (!groups.has(k)) groups.set(k, []);
    groups.get(k).push(item);
  }
  return groups;
}`,
      codeLanguage: 'javascript',
    },
    {
      id: 'hm-f8',
      question: 'Чем Map отличается от обычного объекта в JavaScript?',
      answer:
        'Map: ключи любого типа, сохраняет порядок вставки, есть .size, нет прототипных ключей, оптимизирован под частые set/delete. Object: ключи только строки и символы, есть прототип, удобен для JSON.',
      keyPoints: [
        'Map: ключ любого типа vs Object: только string/Symbol',
        'Map.size O(1) vs Object.keys(o).length O(n)',
        'Map сохраняет порядок вставки гарантированно',
        'Object имеет прототипные ключи (toString, constructor)',
        '`Object.create(null)` — безопасный словарь',
      ],
    },
    {
      id: 'hm-f9',
      question: 'Зачем нужен WeakMap и чем он отличается от Map?',
      answer:
        'WeakMap слабо удерживает ключи: если на ключ-объект больше нет ссылок, GC соберёт его и запись исчезнет. Ключами могут быть только объекты. Нет .size, итераторов, .clear.',
      keyPoints: [
        'Ключи — только объекты',
        'Слабые ссылки → нет утечек памяти при привязке метаданных к DOM/объектам',
        'Нет .size, нет итерации (специально, чтобы не мешать GC)',
        'Используется для приватных полей, кэшей, ассоциаций "объект → метаданные"',
      ],
    },
    {
      id: 'hm-f10',
      question: 'Как Map сравнивает ключи?',
      answer:
        'По алгоритму SameValueZero: примитивы — по значению (но NaN === NaN считается true), объекты — по ссылке.',
      keyPoints: [
        'NaN === NaN в Map (а не как === в JS)',
        '+0 и -0 — один ключ',
        'Объекты сравниваются по ссылке, не по содержимому',
        '`{a:1}` ≠ `{a:1}` как ключи Map',
      ],
    },
    {
      id: 'hm-f11',
      question: 'Какие типичные ошибки бывают при работе с Object как со словарём?',
      answer:
        'Числовые ключи приводятся к строкам (1 и "1" — один ключ), наследуемые свойства из прототипа коллидируют с пользовательскими (toString), нет .size. Решение — Map или Object.create(null).',
      keyPoints: [
        'obj[1] и obj["1"] — один ключ',
        '`"toString" in obj` всегда true',
        'Защита: `Object.hasOwn(obj, key)` или Object.create(null)',
        'JSON.stringify теряет числовой тип ключей в Map тоже',
      ],
    },
    {
      id: 'hm-f12',
      question: 'Когда O(1) операций хеш-таблицы превращается в O(n)?',
      answer:
        'При превышении load factor (rehash — O(n) разово), при плохой хеш-функции и adversarial input (все ключи в один бакет), и при использовании Object с прототипными ключами без проверки `hasOwn`.',
      keyPoints: [
        'Rehash при росте таблицы — амортизируется',
        'Adversarial input — hash flooding атака',
        'Длинная цепочка коллизий → линейный поиск в бакете',
        'V8 использует randomized хеширование для защиты',
      ],
    },
  ],

  cheatsheet: `### Шпаргалка по хеш-таблицам

- **Сложность**: get/set/has/delete — O(1) **амортизированно**.
- **Рабочие лошади**: \`Map\` (любые ключи, порядок), \`Set\` (уникальность), \`WeakMap\` (слабые ссылки).
- **Подсчёт частот**: \`map.set(k, (map.get(k) || 0) + 1)\`.
- **Lookup (Two Sum)**: проверка \`map.has(complement)\` **до** записи текущего.
- **Группировка**: \`if (!m.has(k)) m.set(k, []); m.get(k).push(x)\` или \`Object.groupBy\`.
- **Map vs Object**: ключи любого типа, .size O(1), порядок вставки, нет прототипных коллизий.
- **WeakMap**: ключи только объекты, нет .size, нет итерации, нет утечек.
- **Объект как ключ Map** — сравнение по ссылке, не по содержимому.
- **Деградация до O(n)**: rehash при росте, hash flooding, плохая хеш-функция.`,

  interviewQA: [
    {
      id: 'hm-iq1',
      question: 'Что такое хеш-функция? Какими свойствами должна обладать хорошая хеш-функция?',
      shortAnswer:
        'Функция, отображающая ключ в число фиксированного диапазона. Должна быть детерминированной, равномерно распределять значения и считаться за O(L) от длины ключа.',
      fullAnswer: `**Хеш-функция** \`h: K → [0; M)\` сопоставляет каждому ключу из бесконечного множества K число из конечного диапазона. Это число — индекс бакета в массиве, где хранится значение.

**Три свойства хорошей хеш-функции:**

1. **Детерминированность.** \`h(x)\` всегда возвращает одно значение для одного входа. Без этого структура просто не работает: положили по ключу, вернулись — ничего не нашли.
2. **Равномерное распределение.** Для случайных входов вероятность попадания в любой бакет одинакова. Это минимизирует коллизии и удерживает среднюю длину цепочки около единицы.
3. **Скорость.** Хеш считается за O(L), где L — размер ключа. Для int — O(1), для строки длины L — O(L). Хеширование строки длины миллион стоит миллион операций, но это компенсируется тем, что сравнение строк — тоже O(L).

**В криптографии** добавляются ещё два требования:
- **Одностороннесть**: по \`h(x)\` нельзя восстановить \`x\`.
- **Устойчивость к коллизиям**: найти \`x ≠ y\` с \`h(x) === h(y)\` вычислительно сложно.

Криптографические хеш-функции (SHA-256, BLAKE3) дороже обычных и используются в безопасности, не в Map. В Map движок применяет быструю некриптографическую функцию вроде MurmurHash или собственный variant V8 с randomization для защиты от hash flooding.`,
      followUps: [
        'Что такое hash flooding и как от него защищаются?',
        'Почему модулирование по простому числу даёт лучшее распределение?',
      ],
      relatedChapterId: 'how-it-works',
    },
    {
      id: 'hm-iq2',
      question: 'Чем Map отличается от обычного объекта в JavaScript? Когда что использовать?',
      shortAnswer:
        'Map принимает ключи любого типа, гарантирует порядок вставки, имеет O(1) .size и не страдает от прототипных коллизий. Object — для статических конфигов и JSON, Map — для динамических словарей и нестроковых ключей.',
      fullAnswer: `**Object.**
- Ключи приводятся к строкам или символам. \`obj[1]\` и \`obj["1"]\` — один ключ.
- Наследует ключи прототипа: \`obj.toString\`, \`obj.constructor\`. На пустом объекте \`"toString" in obj === true\`. Защита — \`Object.create(null)\` или \`Object.hasOwn(obj, key)\`.
- Размер — \`Object.keys(obj).length\`, что стоит O(n).
- Хорошо сериализуется в JSON.

**Map.**
- Ключи **любого** типа: число, объект, функция, \`NaN\`, \`null\`. Сравнение через SameValueZero (\`NaN === NaN\` в Map работает).
- Гарантирует порядок вставки.
- \`.size\` — O(1).
- Оптимизирован для частого \`set\`/\`delete\` — V8 не переключается в slow dictionary mode, как с Object.
- Не сериализуется в JSON напрямую (нужен \`Array.from(map)\`).

**Когда что выбирать.**
- **Object**: статические словари, известные ключи, JSON, конфиги.
- **Map**: динамические словари, ключи неизвестны заранее, ключи нестроковые, частые добавления/удаления.

На собеседовании сильный кандидат добавляет: «\`Object\` после нескольких \`delete\` уходит в dictionary mode V8 и теряет оптимизации hidden classes — для часто меняющихся словарей \`Map\` будет быстрее на практике, не только в теории».`,
      followUps: [
        'Что такое hidden classes в V8?',
        'Как сериализовать Map в JSON и обратно?',
      ],
      relatedChapterId: 'map-vs-object',
    },
    {
      id: 'hm-iq3',
      question: 'Когда O(1) операций хеш-таблицы превращается в O(n)?',
      shortAnswer:
        'Три случая: rehash при росте таблицы (O(n) разово, амортизируется), плохая хеш-функция или adversarial input (все ключи в один бакет), и Object с прототипными коллизиями.',
      fullAnswer: `**1. Rehash при росте.** Когда load factor (отношение элементов к размеру таблицы) превышает порог (~0.75), таблица удваивается, и все ключи перехешируются. Эта операция стоит O(n), но случается раз на n вставок, поэтому амортизированно — O(1). На отдельной вставке это всё равно может быть видно: latency spike в profiling.

**2. Adversarial input (hash flooding).** Если злоумышленник знает хеш-функцию, он может подобрать ключи, которые все попадут в один бакет. Тогда добавление одного элемента — O(n), и общая сложность атаки — O(n²). В 2003 году так атаковали Perl, Python, Ruby. С тех пор все сериозные движки (включая V8) используют **randomized hashing**: хеш-функция инициализируется случайным seed при старте процесса.

**3. Object с прототипными коллизиями.** Если использовать \`Object\` как словарь без \`Object.create(null)\` или \`Object.hasOwn\`, ключи прототипа (\`toString\`, \`constructor\`) неявно «есть» в каждом объекте. Это не O(n), но даёт ложноположительные результаты в \`in\` и \`obj[key] !== undefined\`.

**4. Долгие коллизионные цепочки.** В реализации с chaining, если хеш-функция плохая, средняя длина цепочки растёт. Поиск в одной цепочке — O(длины цепочки). Java 8 вводит TreeMap-fallback: при цепочке > 8 элементов бакет превращается в красно-чёрное дерево, и операция деградирует до O(log n), а не O(n).

**5. WeakMap GC.** Удаление слабых ссылок происходит во время GC, и в этот момент возможна задержка. Но это не O(n) per operation — это пауза GC.`,
      followUps: [
        'Как именно V8 защищается от hash flooding?',
        'Что такое load factor и почему его выбирают около 0.75?',
      ],
      relatedChapterId: 'pitfalls',
    },
    {
      id: 'hm-iq4',
      question: 'Зачем нужен WeakMap? Приведите практический пример.',
      shortAnswer:
        'WeakMap слабо удерживает ключи-объекты: после потери всех внешних ссылок GC удаляет запись автоматически. Используется для привязки метаданных к DOM-узлам, приватных полей, кэшей с автоочисткой.',
      fullAnswer: `**Слабые ссылки.** Обычный \`Map\` удерживает ключи и значения сильно: пока запись в Map жива, объект-ключ не будет собран GC, даже если на него больше нет ссылок снаружи. Это одна из самых частых причин утечек памяти в SPA.

**WeakMap** удерживает ключи слабо. Когда на объект-ключ исчезают все внешние ссылки, GC может его собрать, и запись из WeakMap удалится автоматически.

**Практический пример: метаданные DOM.**

\`\`\`js
const handlers = new WeakMap();

function attach(el, handler) {
  handlers.set(el, handler);
  el.addEventListener('click', handler);
}

// Когда el удаляют со страницы и теряют ссылки,
// запись из handlers исчезнет автоматически — без явной чистки.
\`\`\`

**Другие применения:**

1. **Приватные поля до ES2022.** До \`#field\` приватность реализовывалась через WeakMap: ключ — \`this\`, значение — приватное состояние. Внешний код не имеет доступа к WeakMap → не может прочитать данные.
2. **Кэш по объекту.** \`memoize(fn)\` с ключом-объектом: пока объект жив, кэш живёт; когда объект исчез — кэш почистится.
3. **Маркировка обработанных узлов.** Алгоритмы обхода графа: \`visited.set(node, true)\` без удержания всех узлов в памяти.

**Ограничения WeakMap.**
- Ключи — **только объекты**. Ни строки, ни числа.
- Нет \`.size\`, нет \`.keys()\`, нет \`.entries()\`, нет \`for...of\`. Это сделано специально: если бы можно было перебрать все ключи, GC не мог бы их собрать.
- Нет \`.clear()\` — освобождение происходит автоматически.

В ES2021 добавлен \`WeakRef\` — ещё более низкоуровневый примитив для одиночной слабой ссылки.`,
      followUps: [
        'Чем WeakMap отличается от WeakRef?',
        'Можно ли реализовать `Set` поверх `WeakMap`?',
      ],
      relatedChapterId: 'map-vs-object',
    },
    {
      id: 'hm-iq5',
      question: 'Как реализовать Set поверх Map?',
      shortAnswer:
        'Set — это Map<K, true>, где значения нас не интересуют. Достаточно обернуть Map и игнорировать второй аргумент.',
      fullAnswer: `**Базовая реализация.**

\`\`\`js
class MySet {
  constructor(iterable = []) {
    this._map = new Map();
    for (const v of iterable) this.add(v);
  }

  add(value) {
    this._map.set(value, true);
    return this;
  }

  has(value) {
    return this._map.has(value);
  }

  delete(value) {
    return this._map.delete(value);
  }

  get size() {
    return this._map.size;
  }

  *[Symbol.iterator]() {
    for (const k of this._map.keys()) yield k;
  }
}
\`\`\`

**Почему это работает.** Set нужен для проверки наличия элемента. Map уже даёт O(1) на \`has\` по ключу. Значение — необязательная нагрузка; если положить \`true\`, \`null\` или \`undefined\`, разницы для проверки наличия нет.

**Реализация через Object.** Для строковых ключей можно сделать \`const set = Object.create(null); set[key] = true;\`. Минус: только строки, есть прототипные коллизии до \`Object.create(null)\`, нет .size O(1).

**В реальности.** Нативный \`Set\` в V8 устроен через \`OrderedHashTable<Key>\` — отдельная реализация хеш-таблицы без поля «значение». Это даёт небольшую экономию памяти по сравнению с \`Map<K, true>\`.

**На собеседовании** этот вопрос проверяет: понимаете ли вы, что Set — частный случай Map, и можете ли реализовать структуру через композицию. Вторая часть вопроса часто звучит как «реализуйте LRU-кэш на Map» — там \`Map\` используется не только за O(1), но и за гарантию порядка вставки, который позволяет отбрасывать самый старый элемент через \`map.keys().next()\`.`,
      followUps: [
        'Реализуйте LRU-кэш на Map. Какая сложность операций?',
        'Какая разница в потреблении памяти между Set из 1М строк и Map из 1М пар?',
      ],
      relatedChapterId: 'frequency-pattern',
    },
    {
      id: 'hm-iq6',
      question: 'Как Map сравнивает ключи? Почему `map.set({a:1}, 1); map.get({a:1})` возвращает undefined?',
      shortAnswer:
        'Map использует алгоритм SameValueZero. Для примитивов — сравнение по значению (NaN === NaN, +0 === -0). Для объектов — сравнение по ссылке. Два литерала `{a:1}` создают разные объекты с разными ссылками.',
      fullAnswer: `**SameValueZero** — это алгоритм сравнения, описанный в спецификации ECMAScript. Используется в \`Map\`, \`Set\`, \`Array.prototype.includes\`, \`String.prototype.replace\`. Особенности:

- \`x === y\` для большинства случаев.
- \`NaN === NaN\` считается **true** (в отличие от обычного \`===\`).
- \`+0 === -0\` считается **true**.

Для объектов SameValueZero сводится к сравнению ссылок: два литерала \`{a:1}\` и \`{a:1}\` — это два разных объекта в памяти, и Map видит их как разные ключи.

**Демонстрация.**

\`\`\`js
const m = new Map();
m.set({ a: 1 }, 'first');
m.set({ a: 1 }, 'second');
console.log(m.size);                 // 2 — два разных объекта
console.log(m.get({ a: 1 }));        // undefined — третий объект

const obj = { a: 1 };
m.set(obj, 'by-ref');
console.log(m.get(obj));             // 'by-ref' — та же ссылка ✓

console.log(m.has(NaN));             // false
m.set(NaN, 'not-a-number');
console.log(m.has(NaN));             // true ✓
\`\`\`

**Что делать, если нужно сравнение по содержимому.**

1. **Сериализация ключа в строку.** \`map.set(JSON.stringify(obj), value)\`. Минус: \`JSON.stringify({a:1, b:2})\` и \`{b:2, a:1}\` дадут разные строки — порядок ключей важен. Лечится сортировкой ключей перед сериализацией.
2. **Канонический ID.** Если у объектов есть уникальный \`id\`, используйте его как ключ.
3. **Композитный ключ.** Шаблонная строка из значимых полей: \`\\\`\${user.id}:\${user.role}\\\`\`.

На собеседовании уточняющий вопрос: «А что если поля объекта могут содержать другие объекты?» Ответ — рекурсивная сериализация с сортировкой ключей, или специализированная библиотека типа \`@nodutilus/object-hash\`.`,
      followUps: [
        'Чем SameValueZero отличается от SameValue?',
        'Как реализовать Map с ключом-объектом по содержимому без сериализации?',
      ],
      relatedChapterId: 'pitfalls',
    },
    {
      id: 'hm-iq7',
      question: 'Когда стоит выбирать Map для подсчёта частот, а когда — обычный объект?',
      shortAnswer:
        'Map — когда ключи нестроковые, динамические, или нужен предсказуемый .size и порядок. Object — когда ключи известны и являются строками, особенно если результат сериализуется в JSON.',
      fullAnswer: `**Map выигрывает в трёх случаях:**

1. **Ключи не строки.** Подсчёт уникальных пользователей по объекту \`user\`, частот чисел \`1\` и \`"1"\` как разных, ключей-функций (редко, но бывает).
2. **Ключи неизвестны заранее.** Динамический набор: имена полей, входящие данные. После многих \`delete\` Object в V8 переходит в dictionary mode и теряет оптимизации.
3. **Нужна метаинформация.** \`.size\` за O(1), итерация в порядке вставки, прямая поддержка \`for...of\`.

**Object выигрывает в:**

1. **Известных строковых ключах.** \`{ a: 0, b: 0, c: 0 }\` для подсчёта 26 букв — V8 хранит это с hidden class, доступ быстрее, чем у Map.
2. **JSON-совместимости.** \`JSON.stringify(obj)\` работает из коробки, для Map нужен \`Object.fromEntries(map)\` или \`Array.from(map)\`.
3. **Деструктуризации.** \`const { count } = freq\` элегантнее, чем \`const count = freq.get('count')\`.

**Бенчмарк.** Для 1М вставок строковых ключей разница между Map и Object в V8 — ~10-20%, в зависимости от паттерна доступа. На уровне здравого смысла это незаметно. **Не выбирайте структуру по микробенчмаркам — выбирайте по семантике задачи.**

**Практическое правило.** Если вы пишете \`if (key in obj)\` или \`hasOwnProperty\` — это Map disguised as Object. Перепишите на Map: код станет короче и безопаснее.`,
      followUps: [
        'Как влияет dictionary mode V8 на производительность Object?',
        'Когда стоит использовать `Object.create(null)`?',
      ],
      relatedChapterId: 'map-vs-object',
    },
  ],

  nextTopics: [
    {
      slug: 'sliding-window',
      reason: 'Скользящее окно почти всегда комбинируется с хеш-таблицей: Map для частот символов в окне, Set для уникальности. Освоив один шаблон, второй даётся естественно.',
    },
    {
      slug: 'trees',
      reason: 'Деревья — следующий шаг по структурам данных. Многие задачи на деревьях используют хеш-таблицы для маркировки посещённых узлов и кэширования поддеревьев.',
    },
  ],

  related: [
    { kind: 'pattern', id: 'frequency-count', label: 'Паттерн: подсчёт частот' },
    { kind: 'pattern', id: 'two-sum-lookup', label: 'Паттерн: lookup-таблица (Two Sum)' },
  ],
};

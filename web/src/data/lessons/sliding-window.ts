import type { Lesson } from '../../types/lesson';
import { slidingWindowQuiz } from '../quizzes/sliding-window';

// Index existing quiz questions for reuse as checkpoints.
const Q = Object.fromEntries(slidingWindowQuiz.questions.map((q) => [q.id, q]));

// Questions used as in-chapter checkpoints (must NOT appear in finalQuiz).
const CHECKPOINT_IDS = new Set(['sw-q2', 'sw-q5', 'sw-q4', 'sw-q10', 'sw-q6']);

export const slidingWindowLesson: Lesson = {
  topicId: 'sliding-window',

  intro: {
    whyItMatters: `Скользящее окно — техника, которая превращает квадратичный перебор подмассивов и подстрок в линейный обход за O(n). Идея проста: вместо того чтобы каждый раз пересчитывать агрегат (сумму, количество уникальных, частоты символов) для нового положения окна, мы обновляем его инкрементально — добавляем элемент справа и убираем слева.

В реальной разработке вы встречаете её чаще, чем кажется. Метрики уровня «среднее за последние N запросов», rate-limiting в API, скользящие минимумы/максимумы в финансовых графиках, поиск аномалий в логах, оконные функции SQL (\`OVER (ROWS BETWEEN ...)\`), потоковая обработка событий в Kafka — всё это сводится к одной и той же схеме «двух указателей, идущих в одну сторону».

На собеседованиях задачи на скользящее окно — must-have для middle и senior. Их любят за то, что они проверяют сразу несколько навыков: умение распознать паттерн в формулировке («непрерывный подотрезок», «минимальная длина», «не более k уникальных»), умение выбрать **fixed-size** или **dynamic-size** вариант, аккуратность с границами (off-by-one — главный враг), понимание, когда техника **не** работает (отрицательные числа в задачах на сумму, подпоследовательности вместо подмассивов).

Разница между двумя вариантами окна принципиальна. **Fixed-size**: размер \`k\` известен заранее, окно сдвигается строго на один элемент за итерацию — \`+arr[i] -arr[i-k]\`. **Dynamic-size**: размер заранее неизвестен, окно расширяется правым указателем и сжимается левым, пока выполняется (или нарушается) условие. После этого урока вы сможете на лету решать классические задачи: maximum sum subarray of size k, longest substring without repeating characters, minimum window substring, fruit into baskets — и объяснять, почему сложность именно O(n), а не O(n²).`,
    estimatedMinutes: 30,
    interviewAngle:
      'Скользящее окно проверяют, чтобы услышать аргументацию, а не шаблон. Сильный кандидат сначала классифицирует задачу (fixed vs dynamic), формулирует инвариант окна, объясняет амортизированную O(n)-сложность через «каждый элемент входит в окно и выходит из него ровно один раз» и сразу называет ограничения (отрицательные числа, подпоследовательности).',
    prerequisites: [{ slug: 'two-pointers', title: 'Два указателя' }],
  },

  resources: {
    videos: [
      {
        source: 'youtube',
        id: 'MK-NZ4hN7rs',
        title: 'Sliding Window Technique - Algorithmic Mental Models',
        channel: 'Ryan Schachte',
        language: 'en',
        durationSec: 12 * 60,
        description: 'Спокойный разбор обоих вариантов окна с акцентом на ментальную модель, а не на конкретные задачи.',
      },
      {
        source: 'youtube',
        id: 'jM2dhDPYMQM',
        title: 'Sliding Window Technique + 4 Questions',
        channel: 'QuanticDev',
        language: 'en',
        durationSec: 14 * 60,
        description: 'Четыре классические задачи, разобранные пошагово: фиксированное и динамическое окно на одном видео.',
      },
    ],
    links: [
      {
        url: 'https://leetcode.com/problems/longest-substring-without-repeating-characters/',
        title: 'Longest Substring Without Repeating Characters — LeetCode 3',
        source: 'article',
        language: 'en',
        note: 'Каноничная dynamic-size задача — must-solve перед собеседованием.',
      },
      {
        url: 'https://leetcode.com/problems/minimum-window-substring/',
        title: 'Minimum Window Substring — LeetCode 76',
        source: 'article',
        language: 'en',
        note: 'Hard-задача на динамическое окно с подсчётом символов; часто спрашивают на FAANG.',
      },
      {
        url: 'https://neetcode.io/courses/dsa-for-beginners/15',
        title: 'Sliding Window — NeetCode DSA course',
        source: 'article',
        language: 'en',
        note: 'Бесплатный курс с шаблонами и подборкой задач, отсортированных по сложности.',
      },
      {
        url: 'https://developer.mozilla.org/ru/docs/Web/JavaScript/Reference/Global_Objects/Map',
        title: 'Map — MDN',
        source: 'mdn',
        language: 'ru',
        note: '\`Map\` и \`Set\` — основные структуры для подсчёта в окне; полезно освежить интерфейс.',
      },
      {
        url: 'https://learn.javascript.ru/array',
        title: 'Массивы — learn.javascript.ru',
        source: 'learn-js',
        language: 'ru',
        note: 'Базовая глава Ильи Кантора по массивам — нужный фундамент.',
      },
    ],
  },

  chapters: [
    {
      id: 'idea',
      title: 'Идея скользящего окна',
      estimatedMinutes: 5,
      blocks: [
        {
          type: 'text',
          content:
            '**Скользящее окно** — техника, при которой мы поддерживаем непрерывный подотрезок массива (или подстроку) и двигаем его слева направо, обновляя агрегат **инкрементально**. Ключевой выигрыш: вместо O(n·k) или O(n²) перебора получаем O(n).',
        },
        {
          type: 'visualization',
          content: '',
          vizId: 'sliding-window',
        },
        {
          type: 'text',
          content:
            'Окно описывают двумя указателями \`left\` и \`right\`. Оба двигаются **только вправо** — в этом отличие от двух указателей, идущих навстречу. Каждый элемент массива попадает в окно один раз и покидает его один раз — отсюда амортизированная линейная сложность.',
        },
        {
          type: 'list',
          content: `- **Fixed-size**: длина окна \`k\` фиксирована и задана условием.
- **Dynamic-size**: длина окна меняется в зависимости от условия — мы расширяем его, пока условие выполняется, и сжимаем, когда нарушается.
- **Агрегат** окна: сумма, количество уникальных элементов, максимум, частоты символов в \`Map\`/\`Set\` — то, что мы пересчитываем дёшево при сдвиге.`,
        },
        {
          type: 'callout',
          calloutType: 'info',
          content:
            'Скользящее окно — частный случай двух указателей, где оба движутся в одну сторону. Если в задаче указатели идут навстречу друг другу — это не sliding window, а классические two-pointers.',
        },
      ],
      flashcardIds: ['sw-f1', 'sw-f2'],
      checkpoint: [Q['sw-q5']!],
    },

    {
      id: 'fixed-window',
      title: 'Фиксированное окно',
      estimatedMinutes: 6,
      blocks: [
        {
          type: 'visualization',
          content: '',
          vizId: 'fixed-window',
        },
        {
          type: 'text',
          content:
            'Если длина окна \`k\` известна заранее, шаблон состоит из двух фаз: **набираем** первое окно (первые \`k\` элементов), затем на каждом следующем шаге **добавляем** правый элемент и **убираем** левый, который вышел за границу.',
        },
        {
          type: 'code',
          language: 'javascript',
          content: `function maxSumSubarray(arr, k) {
  if (arr.length < k) return 0;

  // Фаза 1: сумма первых k элементов
  let sum = 0;
  for (let i = 0; i < k; i++) {
    sum += arr[i];
  }
  let maxSum = sum;

  // Фаза 2: сдвиг окна на 1 вправо за O(1)
  for (let i = k; i < arr.length; i++) {
    sum = sum + arr[i] - arr[i - k];
    maxSum = Math.max(maxSum, sum);
  }

  return maxSum;
}`,
        },
        {
          type: 'callout',
          calloutType: 'tip',
          content:
            'Запомните формулу сдвига: новый правый — \`arr[i]\`, ушедший левый — \`arr[i - k]\`. Не \`arr[i - k + 1]\`, не \`arr[i - 1]\`. Когда \`right === i\`, окно — это \`[i - k + 1; i]\`, а \`arr[i - k]\` — элемент **до** левой границы, который только что её покинул.',
        },
        {
          type: 'text',
          content:
            'Пример: \`maxSumSubarray([2, 1, 5, 1, 3, 2], 3)\`. Окна: \`[2,1,5]=8\`, \`[1,5,1]=7\`, \`[5,1,3]=9\`, \`[1,3,2]=6\`. Ответ — 9.',
        },
        { type: 'heading', content: 'Когда применим fixed-size' },
        {
          type: 'list',
          content: `- Размер окна явно задан в условии: «среди всех подмассивов длины k...».
- Все средние/максимумы/суммы подмассивов фиксированной длины.
- Скользящие метрики в реальном времени: «среднее за последние 60 секунд».
- Подсчёт уникальных в окне фиксированной длины (с \`Map\` или \`Set\`).`,
        },
      ],
      flashcardIds: ['sw-f3', 'sw-f4'],
      checkpoint: [Q['sw-q2']!],
    },

    {
      id: 'dynamic-window',
      title: 'Динамическое окно',
      estimatedMinutes: 8,
      blocks: [
        {
          type: 'visualization',
          content: '',
          vizId: 'dynamic-window',
        },
        {
          type: 'text',
          content:
            'Когда размер окна заранее неизвестен, его границы определяются **условием**. Базовый шаблон: внешний цикл двигает \`right\`, внутренний \`while\` — \`left\`. Один и тот же элемент рассматривается O(1) раз слева и O(1) раз справа, значит общая сложность — O(n).',
        },
        { type: 'heading', content: 'Шаблон: расширяй — сжимай' },
        {
          type: 'code',
          language: 'javascript',
          content: `function dynamicWindow(arr) {
  let left = 0;
  let agg = 0;          // агрегат: сумма, счётчик и т.д.
  let answer = 0;       // ответ: длина, количество, экстремум

  for (let right = 0; right < arr.length; right++) {
    // 1. Расширяем окно: добавляем arr[right] в агрегат
    agg += arr[right];

    // 2. Сжимаем, пока окно НЕ удовлетворяет условию
    while (/* условие нарушено */ false) {
      agg -= arr[left];
      left++;
    }

    // 3. Обновляем ответ для текущего валидного окна
    answer = Math.max(answer, right - left + 1);
  }

  return answer;
}`,
        },
        { type: 'heading', content: 'Пример: кратчайший подмассив с суммой ≥ target' },
        {
          type: 'code',
          language: 'javascript',
          content: `function minSubarrayLen(target, nums) {
  let left = 0;
  let sum = 0;
  let minLen = Infinity;

  for (let right = 0; right < nums.length; right++) {
    sum += nums[right];

    // Сжимаем, пока окно ВСЁ ЕЩЁ удовлетворяет условию,
    // фиксируя минимальную длину
    while (sum >= target) {
      minLen = Math.min(minLen, right - left + 1);
      sum -= nums[left];
      left++;
    }
  }

  return minLen === Infinity ? 0 : minLen;
}`,
        },
        {
          type: 'callout',
          calloutType: 'tip',
          content:
            'Различайте две формулировки сжатия. **«Сжимаем, пока условие выполняется»** — для задач на минимальную длину (Minimum Size Subarray Sum). **«Сжимаем, пока условие нарушено»** — для задач на максимальную длину при ограничении (Longest Substring Without Repeating Characters, Fruit Into Baskets). От этого зависит, **где** обновлять ответ — внутри \`while\` или после него.',
        },
        { type: 'heading', content: 'Пример: длиннейшая подстрока без повторов' },
        {
          type: 'code',
          language: 'javascript',
          content: `function lengthOfLongestSubstring(s) {
  const set = new Set();
  let left = 0;
  let maxLen = 0;

  for (let right = 0; right < s.length; right++) {
    // Сжимаем, пока условие НАРУШЕНО (есть дубликат)
    while (set.has(s[right])) {
      set.delete(s[left]);
      left++;
    }
    set.add(s[right]);
    // После цикла окно валидно — обновляем ответ
    maxLen = Math.max(maxLen, right - left + 1);
  }
  return maxLen;
}`,
        },
        {
          type: 'callout',
          calloutType: 'info',
          content:
            'Несмотря на вложенный \`while\`, сложность остаётся O(n). \`left\` за всё время цикла суммарно сдвигается не более чем на \`n\` шагов — это и есть классический амортизационный анализ.',
        },
      ],
      playground: {
        starterCode: `// Реализуйте: найдите длину длиннейшей подстроки без повторов.
// Ожидаемый вывод для "abcabcbb" — 3 (подстрока "abc").
// Шаблон с Set уже частично написан — допишите.

function lengthOfLongestSubstring(s) {
  const set = new Set();
  let left = 0;
  let maxLen = 0;

  for (let right = 0; right < s.length; right++) {
    // ваш код: сжимаем окно при дубликате,
    // затем добавляем s[right] и обновляем maxLen
  }

  return maxLen;
}

console.log(lengthOfLongestSubstring("abcabcbb"));`,
        expectedOutput: '3',
        description:
          'Допишите шаблон динамического окна с использованием \`Set\`. Цель — увидеть, что условие сжатия здесь — «пока окно НЕ валидно», и убедиться, что вывод — 3.',
      },
      flashcardIds: ['sw-f5', 'sw-f6', 'sw-f7'],
      checkpoint: [Q['sw-q4']!, Q['sw-q10']!],
    },

    {
      id: 'when-applies',
      title: 'Когда техника применима — и когда нет',
      estimatedMinutes: 5,
      blocks: [
        { type: 'heading', content: 'Сигналы «здесь sliding window»' },
        {
          type: 'list',
          content: `- В условии — **непрерывный** подмассив или подстрока (не подпоследовательность!).
- Спрашивают **минимум/максимум/количество** среди подотрезков с агрегатным условием.
- Наивное решение даёт O(n·k) или O(n²), а ожидается O(n).
- Условие на агрегат: сумма, количество уникальных, максимум, частоты символов.
- Для задач на сумму — массив **неотрицательный** (для dynamic-size).`,
        },
        { type: 'heading', content: 'Свойство монотонности' },
        {
          type: 'text',
          content:
            'Динамическое окно работает, когда расширение окна вправо **монотонно меняет** агрегат в одну сторону, а сжатие слева — в противоположную. Если это свойство нарушено, жадное сжатие пропустит оптимальное окно.',
        },
        { type: 'heading', content: 'Когда техника НЕ работает' },
        {
          type: 'list',
          content: `- **Подпоследовательность вместо подмассива.** Элементы подпоследовательности не обязаны быть соседними — окно не подойдёт.
- **Отрицательные числа в задачах на сумму.** Удаление отрицательного элемента *увеличивает* сумму — монотонность ломается. Используйте prefix sums + хеш-таблицу или deque.
- **Произвольная переставимость элементов в окне.** Скользящее окно сохраняет порядок; если порядок не важен и можно сортировать — это другая задача.
- **Окно зависит от глобального состояния всего массива.** Если для каждого положения окна нужно знать что-то о всём массиве, инкрементальное обновление невозможно.`,
        },
        {
          type: 'callout',
          calloutType: 'warning',
          content:
            'Классическая ловушка: «найдите кратчайший подмассив с суммой ≥ target» с массивом \`[-1, 2, 3, -2, 1]\`. Для отрицательных чисел стандартный шаблон даст неверный ответ — нужно prefix sums + monotonic deque.',
        },
      ],
      flashcardIds: ['sw-f8', 'sw-f9'],
      checkpoint: [Q['sw-q6']!],
    },

    {
      id: 'pitfalls',
      title: 'Типичные ошибки',
      estimatedMinutes: 5,
      blocks: [
        { type: 'heading', content: 'Off-by-one в формуле сдвига' },
        {
          type: 'code',
          language: 'javascript',
          content: `// АНТИПАТТЕРН: вычитаем не тот элемент
for (let i = k; i < arr.length; i++) {
  sum = sum + arr[i] - arr[i - k + 1]; // ❌ off-by-one
  maxSum = Math.max(maxSum, sum);
}

// КОРРЕКТНО:
for (let i = k; i < arr.length; i++) {
  sum = sum + arr[i] - arr[i - k];     // ✓
  maxSum = Math.max(maxSum, sum);
}`,
        },
        {
          type: 'callout',
          calloutType: 'warning',
          content:
            'Когда \`i\` стало \`k\`, окно — это индексы \`[1; k]\`. Элемент, который только что вышел из окна, имеет индекс \`0 = i - k\`, а **не** \`1 = i - k + 1\`.',
        },
        { type: 'heading', content: 'Забыли сжать окно' },
        {
          type: 'code',
          language: 'javascript',
          content: `// АНТИПАТТЕРН: сравниваем длину, но не двигаем left
function lengthOfLongestSubstring(s) {
  const set = new Set();
  let left = 0, maxLen = 0;
  for (let right = 0; right < s.length; right++) {
    if (set.has(s[right])) {
      // ❌ забыли while + set.delete + left++
    }
    set.add(s[right]);
    maxLen = Math.max(maxLen, set.size);
  }
  return maxLen;
}
// Для "abcabcbb" вернёт 3, но логика сломается на "pwwkew" → 4 вместо 3.`,
        },
        { type: 'heading', content: 'Забыли сдвинуть right' },
        {
          type: 'code',
          language: 'javascript',
          content: `// АНТИПАТТЕРН: while с условием на right без инкремента
let left = 0, right = 0, sum = 0;
while (right < arr.length) {
  sum += arr[right];
  if (sum >= target) {
    // ❌ обработали окно, но right не сдвинули → бесконечный цикл
    sum -= arr[left];
    left++;
  }
  // забыли right++
}`,
        },
        {
          type: 'callout',
          calloutType: 'warning',
          content:
            'Шаблон с \`for (let right = 0; ...; right++)\` защищает от этой ошибки: правый указатель сдвигается автоматически. Шаблон с \`while\` требует ручного \`right++\` — и это самый частый источник зависаний.',
        },
        { type: 'heading', content: 'Где обновлять ответ' },
        {
          type: 'list',
          content: `- **Минимальная длина**: ответ — *внутри* \`while\` сжатия, перед сдвигом \`left\`. Так фиксируется длина каждого валидного окна перед уменьшением.
- **Максимальная длина**: ответ — *после* \`while\`, когда окно стало валидным. Так фиксируется длина каждого валидного окна после восстановления инварианта.`,
        },
        { type: 'heading', content: 'Не обработали граничные случаи' },
        {
          type: 'list',
          content: `- Пустой массив: \`arr.length === 0\` — большинство шаблонов корректно вернёт 0/Infinity, но проверьте явно.
- \`k > arr.length\`: окно не помещается. Возвращайте \`0\` или \`null\` — главное, не уходите в \`NaN\`.
- \`k === 0\`: бессмысленный размер окна. Лучше падать с понятной ошибкой.`,
        },
      ],
      playground: {
        starterCode: `// Найдите баг в коде ниже. Ожидается: 9 (сумма [5,1,3]).
// Подсказка: проверьте формулу сдвига окна.

function maxSumSubarray(arr, k) {
  let sum = 0;
  for (let i = 0; i < k; i++) sum += arr[i];
  let maxSum = sum;
  for (let i = k; i < arr.length; i++) {
    sum = sum + arr[i] - arr[i - k + 1]; // тут что-то не так
    maxSum = Math.max(maxSum, sum);
  }
  return maxSum;
}

console.log(maxSumSubarray([2, 1, 5, 1, 3, 2], 3));`,
        expectedOutput: '9',
        description:
          'Это off-by-one: вычитается не тот элемент, который покинул окно. Замените \`arr[i - k + 1]\` на \`arr[i - k]\` и убедитесь, что результат — 9.',
      },
      flashcardIds: ['sw-f10', 'sw-f11'],
    },

    {
      id: 'patterns',
      title: 'Канонические паттерны и задачи',
      estimatedMinutes: 5,
      blocks: [
        { type: 'heading', content: 'Maximum sum subarray of size k' },
        {
          type: 'text',
          content:
            'Базовый fixed-size паттерн. Тренирует формулу сдвига и обработку \`arr.length < k\`. Решение — выше в главе про fixed-size.',
        },
        { type: 'heading', content: 'Longest substring without repeating characters' },
        {
          type: 'text',
          content:
            'LeetCode 3, dynamic-size с \`Set\`. Можно ускорить, заменив \`Set\` на \`Map\` «символ → индекс»: при дубликате \`left\` сразу прыгает к \`map.get(s[right]) + 1\`, а не сдвигается по одному.',
        },
        {
          type: 'code',
          language: 'javascript',
          content: `function lengthOfLongestSubstring(s) {
  const map = new Map(); // символ → последний индекс
  let left = 0, maxLen = 0;

  for (let right = 0; right < s.length; right++) {
    if (map.has(s[right])) {
      // Math.max защищает от «отката» left назад,
      // если старое вхождение уже за пределами окна.
      left = Math.max(left, map.get(s[right]) + 1);
    }
    map.set(s[right], right);
    maxLen = Math.max(maxLen, right - left + 1);
  }
  return maxLen;
}`,
        },
        { type: 'heading', content: 'Minimum window substring' },
        {
          type: 'text',
          content:
            'LeetCode 76, hard. Шаблон с двумя \`Map\`: \`need\` (что нужно) и счётчик \`have\` (сколько символов уже в нужном количестве). Как только \`have === required\` — пробуем сжимать слева. Часто спрашивают на FAANG.',
        },
        { type: 'heading', content: 'Fruit into baskets / Longest substring with K distinct' },
        {
          type: 'text',
          content:
            'Dynamic-size с ограничением «не более K уникальных». \`Map\` хранит частоту, при превышении \`map.size > K\` — сжимаем слева. Удаляем ключ из \`Map\`, когда счётчик опустился до 0.',
        },
        { type: 'heading', content: 'Subarrays product less than K' },
        {
          type: 'text',
          content:
            'LeetCode 713. Тонкий момент: для каждого \`right\` число подмассивов с произведением < K, заканчивающихся в \`right\`, равно \`right - left + 1\`. Это общая идея для подсчёта подмассивов в окне.',
        },
        {
          type: 'callout',
          calloutType: 'tip',
          content:
            'Запомните три «семьи» паттернов: (1) fixed-size — формула сдвига; (2) dynamic-size на минимум — сжимаем, пока валидно; (3) dynamic-size на максимум — сжимаем, пока невалидно. 80% задач собеседования решаются комбинацией этих трёх шаблонов.',
        },
      ],
      flashcardIds: ['sw-f12'],
    },
  ],

  // Все вопросы из старого квиза, кроме тех, что ушли в checkpoint.
  finalQuiz: slidingWindowQuiz.questions.filter((q) => !CHECKPOINT_IDS.has(q.id)),

  flashcards: [
    {
      id: 'sw-f1',
      question: 'Что такое скользящее окно и в чём его выигрыш?',
      answer:
        'Техника поддержания непрерывного подотрезка с инкрементальным обновлением агрегата. Сводит O(n·k) и O(n²) перебор к O(n), поскольку каждый элемент попадает в окно ровно один раз и выходит из него один раз.',
      keyPoints: [
        'Непрерывный подмассив или подстрока',
        'Инкрементальное обновление агрегата при сдвиге',
        'Амортизированная сложность O(n)',
      ],
    },
    {
      id: 'sw-f2',
      question: 'Чем sliding window отличается от двух указателей?',
      answer:
        'Sliding window — частный случай two pointers, где оба указателя движутся в **одну** сторону (слева направо). Классические two pointers идут навстречу или вообще по разным массивам.',
      keyPoints: [
        'Sliding window: оба указателя только вправо',
        'Two pointers: указатели могут идти навстречу',
        'Окно сохраняет инвариант непрерывности',
      ],
    },
    {
      id: 'sw-f3',
      question: 'Как выглядит шаблон fixed-size окна?',
      answer:
        'Сначала набираем сумму первых k элементов. Затем для каждого \`i\` от \`k\` до \`arr.length - 1\` обновляем агрегат за O(1): \`sum = sum + arr[i] - arr[i - k]\`.',
      keyPoints: [
        'Две фазы: набор окна и сдвиг',
        'Формула сдвига: +arr[i] -arr[i-k]',
        'Не arr[i-k+1] и не arr[i-1] — это off-by-one',
      ],
      code: `let sum = 0;
for (let i = 0; i < k; i++) sum += arr[i];
let maxSum = sum;
for (let i = k; i < arr.length; i++) {
  sum = sum + arr[i] - arr[i - k];
  maxSum = Math.max(maxSum, sum);
}`,
      codeLanguage: 'javascript',
    },
    {
      id: 'sw-f4',
      question: 'Почему в формуле сдвига именно \`arr[i - k]\`?',
      answer:
        'Когда \`i\` дошло до значения \`i\`, окно содержит индексы \`[i - k + 1; i]\`. Элемент, который только что покинул окно — \`arr[i - k]\` (он был левой границей до сдвига).',
      keyPoints: [
        'Окно после сдвига: [i - k + 1; i]',
        'Покинувший элемент: i - k',
        'Проверьте на маленьком примере: i=k → окно [1; k], выпал arr[0]',
      ],
    },
    {
      id: 'sw-f5',
      question: 'Опишите шаблон dynamic-size окна.',
      answer:
        'Внешний цикл двигает \`right\` (расширяет окно), внутренний \`while\` двигает \`left\` (сжимает окно), пока выполняется или нарушается условие. Ответ обновляется в зависимости от типа задачи.',
      keyPoints: [
        'Расширяй right → обновляй агрегат',
        'Сжимай left во вложенном while',
        'Каждый элемент входит и выходит ровно один раз → O(n)',
      ],
      code: `let left = 0, agg = 0, answer = 0;
for (let right = 0; right < arr.length; right++) {
  agg += arr[right];
  while (/* условие сжатия */) {
    agg -= arr[left];
    left++;
  }
  answer = Math.max(answer, right - left + 1);
}`,
      codeLanguage: 'javascript',
    },
    {
      id: 'sw-f6',
      question: 'Где обновлять ответ — внутри while или после?',
      answer:
        'Зависит от типа задачи. Для **минимальной длины** — внутри \`while\`, перед сдвигом \`left\` (фиксируем длину перед сжатием). Для **максимальной длины** — после \`while\`, когда окно снова валидно.',
      keyPoints: [
        'min length → ответ внутри while',
        'max length → ответ после while',
        'Подсказка: чего хочется — больше валидных или меньше?',
      ],
    },
    {
      id: 'sw-f7',
      question: 'Как решить «longest substring without repeating characters» за O(n)?',
      answer:
        'Используйте \`Set\` или \`Map\`. Расширяем окно вправо. При обнаружении дубликата \`s[right]\` — сжимаем слева, удаляя символы, пока дубликат не исчезнет. Затем добавляем \`s[right]\` и обновляем максимум.',
      keyPoints: [
        'Set хранит символы текущего окна',
        'while (set.has(s[right])) — сжимаем',
        'O(n): каждый символ +1 раз add и +1 раз delete',
      ],
      code: `const set = new Set();
let left = 0, maxLen = 0;
for (let right = 0; right < s.length; right++) {
  while (set.has(s[right])) {
    set.delete(s[left++]);
  }
  set.add(s[right]);
  maxLen = Math.max(maxLen, right - left + 1);
}`,
      codeLanguage: 'javascript',
    },
    {
      id: 'sw-f8',
      question: 'Какие сигналы в формулировке задачи говорят «sliding window»?',
      answer:
        '«Непрерывный подмассив/подстрока», «минимум/максимум среди всех подотрезков», «не более k уникальных», «сумма ≥ target» — при условии неотрицательных чисел и ожидаемой O(n) сложности.',
      keyPoints: [
        'Ключевые слова: непрерывный, подряд, окно',
        'Агрегат: сумма, частоты, количество уникальных',
        'Ожидаемая сложность лучше наивной O(n²)',
      ],
    },
    {
      id: 'sw-f9',
      question: 'Когда sliding window НЕ работает?',
      answer:
        'На подпоследовательностях (элементы не обязаны быть соседними), на массивах с отрицательными числами в задачах на сумму (нарушается монотонность), на задачах, где порядок элементов в окне можно произвольно менять.',
      keyPoints: [
        'Subsequence ≠ subarray',
        'Отрицательные числа ломают сжатие',
        'Альтернатива: prefix sums + hash map / deque',
      ],
    },
    {
      id: 'sw-f10',
      question: 'Назовите три самых частых ошибки при реализации sliding window.',
      answer:
        '(1) Off-by-one в формуле сдвига \`arr[i - k]\` vs \`arr[i - k + 1]\`. (2) Забытое сжатие — окно растёт, но \`left\` не двигается. (3) Забытый \`right++\` в \`while\`-шаблоне — бесконечный цикл.',
      keyPoints: [
        'Off-by-one в формуле сдвига',
        'Забытый while + delete + left++',
        'Забытый right++ в while-цикле',
      ],
    },
    {
      id: 'sw-f11',
      question: 'Какова сложность sliding window по времени и памяти?',
      answer:
        'Время — O(n) амортизированно: каждый элемент входит и выходит из окна ровно один раз. Память — O(1) для задач на сумму или O(k) / O(\|alphabet\|) для задач со структурой (Set, Map) внутри окна.',
      keyPoints: [
        'Время: O(n) — каждый элемент дважды (вход/выход)',
        'Память: O(1) для агрегатов; O(k) для Set/Map',
        'Вложенный while не делает алгоритм O(n²)',
      ],
    },
    {
      id: 'sw-f12',
      question: 'Как подсчитать количество подмассивов с произведением меньше K?',
      answer:
        'Dynamic-size окно: расширяем \`right\`, делим \`product\` на \`nums[left]\` пока произведение ≥ K. Для каждого \`right\` добавляем к ответу \`right - left + 1\` — это количество валидных подмассивов, заканчивающихся в \`right\`.',
      keyPoints: [
        'product *= nums[right] на расширении',
        'product /= nums[left] на сжатии',
        'count += right - left + 1 — все подмассивы, оканчивающиеся в right',
      ],
    },
  ],

  cheatsheet: `### Шпаргалка по скользящему окну

- **Идея**: непрерывный подотрезок + инкрементальное обновление агрегата → O(n) вместо O(n·k).
- **Fixed-size**: набираем первые \`k\`, затем \`sum = sum + arr[i] - arr[i - k]\`.
- **Dynamic-size**: внешний \`for right\`, внутренний \`while left\`. Сжимаем «пока валидно» (min) или «пока невалидно» (max).
- **Где обновлять ответ**: внутри \`while\` для минимальной длины, после \`while\` для максимальной.
- **Время**: O(n), память: O(1) или O(k).
- **Не работает**: подпоследовательности, отрицательные числа в задачах на сумму.
- **Тесты**: пустой массив, \`k > n\`, \`k === 0\`, отрицательные/нулевые элементы, дубликаты.`,

  interviewQA: [
    {
      id: 'sw-iq1',
      question: 'Чем sliding window отличается от двух указателей?',
      shortAnswer:
        'Sliding window — частный случай two pointers, где оба указателя движутся в одну сторону (слева направо). Классические two pointers идут навстречу. Окно поддерживает непрерывный подотрезок, два указателя — пару элементов или общий инвариант.',
      fullAnswer: `Эти техники часто путают, потому что обе используют пару указателей. Различия принципиальны.

**Sliding window** — \`left\` и \`right\` стартуют слева, оба двигаются только вправо. Окно \`[left; right]\` — это непрерывный подотрезок. Цель — поддерживать инвариант на этом подотрезке и обновлять агрегат при сдвиге за O(1).

**Two pointers** — указатели могут идти навстречу (например, \`left = 0\`, \`right = arr.length - 1\` в задаче 2Sum для отсортированного массива) или в одном направлении, но **без концепции окна** — их используют, например, для in-place удаления дубликатов.

**Когда применять что.**
- «Найдите кратчайший подмассив с суммой ≥ target» — sliding window: ответ — длина непрерывного отрезка.
- «Найдите пару чисел с суммой target в отсортированном массиве» — two pointers навстречу: ответ — пара индексов, окно не нужно.
- «Удалите дубликаты на месте» — two pointers slow/fast в одну сторону: окна нет, есть указатель «куда писать».

**Граница размытая.** Например, «контейнер с максимальной площадью» (LeetCode 11) формально решается двумя указателями, но идея «сжимаемого окна» там тоже присутствует. На собеседовании важнее объяснить инвариант, чем спорить о терминах.`,
      followUps: [
        'Может ли в задаче 2Sum on sorted array помочь sliding window?',
        'В каких задачах указатели идут с разной скоростью (slow/fast)?',
      ],
      relatedChapterId: 'idea',
    },
    {
      id: 'sw-iq2',
      question: 'Когда применима техника sliding window? Какое ключевое свойство задачи?',
      shortAnswer:
        'Когда ответ зависит от непрерывного подотрезка и агрегат окна меняется монотонно при расширении и сжатии. Если расширение нарушает условие, а сжатие восстанавливает (или наоборот) — окно работает. Если монотонность нарушена (отрицательные числа, подпоследовательности) — нужен другой подход.',
      fullAnswer: `Два условия применимости:

1. **Непрерывный подотрезок.** В условии прямо или косвенно — «подмассив», «подстрока», «окно». Если речь о подпоследовательности — окно не подойдёт.
2. **Монотонность агрегата.** При сдвиге \`right\` вправо агрегат меняется в одну сторону, при сдвиге \`left\` вправо — в противоположную. Это позволяет жадно сжимать или расширять окно.

**Пример работающего случая.** «Кратчайший подмассив с суммой ≥ target» на массиве неотрицательных чисел. Расширение \`right\` строго увеличивает сумму. Сжатие \`left\` — строго уменьшает. Если сумма ≥ target и мы убрали \`nums[left]\`, новая сумма — это лучший кандидат для следующего шага сжатия.

**Пример не работающего случая.** Тот же вопрос на массиве \`[-2, 1, -3, 4, -1, 2, 1]\`. Сжатие может **увеличить** сумму (если \`nums[left] < 0\`), и жадная остановка \`while (sum >= target)\` пропускает оптимальное окно. Здесь нужны prefix sums + monotonic deque или другие техники.

**Чек-лист на собеседовании.** Перед написанием кода произнесите вслух: «Окно — непрерывный подмассив. Агрегат — сумма / счётчик. Расширение увеличивает / уменьшает агрегат. Сжатие — наоборот. Монотонность сохраняется». Если на каком-то пункте споткнулись — техника, скорее всего, не работает.`,
      followUps: [
        'Как доказать монотонность для конкретной задачи?',
        'Какие альтернативы sliding window для немонотонных случаев?',
      ],
      relatedChapterId: 'when-applies',
    },
    {
      id: 'sw-iq3',
      question: 'Опишите шаблон dynamic-size окна. Где обновлять ответ?',
      shortAnswer:
        'Внешний \`for (let right = 0; right < n; right++)\` расширяет окно, внутренний \`while\` сжимает. Ответ для минимальной длины обновляется внутри \`while\` перед сдвигом \`left\`, для максимальной — после \`while\`, когда окно снова валидно.',
      fullAnswer: `Универсальный шаблон:

\`\`\`js
let left = 0, agg = 0, answer = INIT;
for (let right = 0; right < arr.length; right++) {
  agg = updateOnExpand(agg, arr[right]);

  while (windowConditionViolated(agg)) {
    agg = updateOnShrink(agg, arr[left]);
    left++;
  }

  answer = combine(answer, currentWindow(left, right));
}
\`\`\`

**Минимальная длина (Minimum Size Subarray Sum).**

\`\`\`js
while (sum >= target) {
  minLen = Math.min(minLen, right - left + 1); // ответ ВНУТРИ
  sum -= nums[left++];
}
\`\`\`

Здесь окно валидно **внутри** \`while\`. Каждое сжатие может дать более короткое валидное окно, поэтому ответ фиксируется до сдвига \`left\`.

**Максимальная длина (Longest Substring Without Repeating Characters).**

\`\`\`js
while (set.has(s[right])) {
  set.delete(s[left++]); // сжимаем, пока невалидно
}
set.add(s[right]);
maxLen = Math.max(maxLen, right - left + 1); // ответ ПОСЛЕ
\`\`\`

Здесь \`while\` восстанавливает инвариант. Внутри \`while\` окно невалидно — обновлять ответ нельзя. После цикла окно валидно — фиксируем длину.

**Мнемоника.** «Сжимаем, пока валидно» → ответ внутри. «Сжимаем, пока невалидно» → ответ снаружи. Это правило закрывает 90% задач.`,
      followUps: [
        'Что произойдёт, если перепутать местами обновление ответа?',
        'Как изменится шаблон, если нужно подсчитать количество окон, а не их длину?',
      ],
      relatedChapterId: 'dynamic-window',
    },
    {
      id: 'sw-iq4',
      question: 'Какова временная и пространственная сложность sliding window? Почему вложенный while не делает её O(n²)?',
      shortAnswer:
        'Время — O(n) амортизированно. Память — O(1) для задач на скаляр, O(k) или O(\|alphabet\|) для Set/Map. Вложенный \`while\` не нарушает O(n), поскольку \`left\` за всё время работы суммарно сдвигается не более чем на \`n\` шагов.',
      fullAnswer: `**Время.** В шаблоне dynamic-size есть внешний \`for\` (n итераций) и внутренний \`while\`. Кажется, что в худшем случае \`while\` может на каждой итерации внешнего цикла пройти до n шагов — итого O(n²). Это иллюзия.

Аккуратный анализ: \`left\` инициализируется нулём и **никогда не уменьшается**. Каждое выполнение тела \`while\` увеличивает \`left\` на 1. Значит, суммарное число выполнений тела \`while\` за всё время работы алгоритма — не более \`n\` (когда \`left\` достигает \`n\`, \`while\` больше не сработает, потому что \`left ≤ right < n\`).

Итого: внешний цикл — \`n\` итераций, внутренний — \`n\` итераций суммарно. Общее число операций — \`2n = O(n)\`. Это классический амортизационный анализ.

**Память.**
- Для скалярных агрегатов (сумма, произведение, счётчик) — O(1).
- Для \`Set\` или \`Map\` с символами окна — O(размер окна) в худшем случае. Для алфавита из 26 латинских букв — O(1) с большой константой.
- Для прецифициро-сумм + хеш-таблицы — O(n).

**Сравнение с наивным решением.** Наивный перебор всех подмассивов — O(n²) подмассивов, для каждого вычисление агрегата — O(k) или O(n). Итого O(n²) или O(n³). Sliding window даёт квадратичный или кубический выигрыш.

**На собеседовании.** Объяснение амортизации — то, что отделяет middle от senior. Просто сказать «O(n)» недостаточно, особенно если интервьюер дотошный — он будет ждать аргумента «каждый элемент попадает и покидает окно ровно один раз».`,
      followUps: [
        'Как оценивается сложность с использованием Map против Set?',
        'Почему Math.max в формуле \`left = Math.max(left, map.get(c) + 1)\` важен для корректности O(n)?',
      ],
      relatedChapterId: 'dynamic-window',
    },
    {
      id: 'sw-iq5',
      question: 'Приведите пример задачи, на которой sliding window НЕ работает. Почему?',
      shortAnswer:
        'Минимальный подмассив с суммой ≥ target на массиве с отрицательными числами. Сжатие окна слева может увеличить сумму (если \`nums[left] < 0\`), и жадное условие \`while (sum >= target)\` пропустит оптимальное окно. Альтернатива — prefix sums + monotonic deque.',
      fullAnswer: `**Задача-обманка.** «Найдите минимальный по длине подмассив с суммой ≥ target» с входом \`nums = [2, -1, 2, -3, 5]\`, \`target = 4\`.

**Что делает sliding window.** Расширяем окно: \`[2]=2\`, \`[2,-1]=1\`, \`[2,-1,2]=3\`, \`[2,-1,2,-3]=0\`, \`[2,-1,2,-3,5]=4\` ≥ 4. Пробуем сжать: убрали \`2\`, сумма стала \`2\`, \`< 4\`. Жадно остановились на длине 5. Но реальный ответ — \`[5]\` длиной 1 (последний элемент).

**Почему сломалось.** Удаление \`nums[left] = 2\` уменьшило сумму. Удаление \`nums[left] = -1\` (если бы дошли) — увеличило бы. Монотонность нарушена: расширение и сжатие больше не меняют сумму в фиксированном направлении. Жадный шаблон не работает.

**Что использовать.**

1. **Prefix sums + minimum prefix.** Для каждого \`i\` ищем \`j < i\` такое, что \`prefix[i] - prefix[j] >= target\` и \`i - j\` минимально. Это сводится к задаче «для каждого \`i\` найти ближайший \`j\` с \`prefix[j] <= prefix[i] - target\`», решаемой за O(n log n) сортировкой или O(n) с monotonic deque.

2. **Monotonic deque.** Поддерживаем deque индексов с возрастающим \`prefix\`. Для каждого нового \`prefix[i]\` сначала вытаскиваем из головы все \`j\` с \`prefix[j] <= prefix[i] - target\` (фиксируя минимум \`i - j\`), затем чистим хвост, поддерживая монотонность.

**Другие задачи-обманки.**
- «Длиннейшая подпоследовательность с условием X» — подпоследовательность, не подмассив. Sliding window не применим вообще.
- «Найдите k-й элемент в подмассиве» — порядок внутри окна важен в нетривиальном виде, обычная техника не подходит. Нужно или сортировать окно (медленно), или мульти-дерево, или quickselect.

**Урок собеседования.** Прежде чем писать код, проговорите вслух: «работает ли это на отрицательных числах? на пустом массиве? на дубликатах?». Это часто ловит баги ещё на этапе проектирования.`,
      followUps: [
        'Как реализовать monotonic deque за O(n)?',
        'Почему prefix sums + хеш-таблица не подходит, когда нужно «≥», а не «=»?',
      ],
      relatedChapterId: 'when-applies',
    },
    {
      id: 'sw-iq6',
      question: 'Какие граничные случаи нужно тестировать в реализации sliding window?',
      shortAnswer:
        'Пустой массив, \`k > n\`, \`k === 0\`, массив длины ровно \`k\`, дубликаты, нулевые и отрицательные элементы (если они допустимы), один элемент, target недостижим. Эти случаи покрывают типичные off-by-one и ошибки инициализации.',
      fullAnswer: `**Минимальный набор для fixed-size.**

1. Пустой массив: \`maxSumSubarray([], 3)\` — обычно возвращают \`0\` или \`null\`. Проверьте, что код не падает с \`NaN\` (вычисление суммы 0 элементов).
2. \`k > n\`: \`maxSumSubarray([1, 2], 3)\` — окно не помещается. Возврат \`0\`/\`null\`/спецзначение.
3. \`k === 0\`: бессмысленный размер окна, лучше явно выбрасывать ошибку.
4. \`k === n\`: одно окно — весь массив. Проверяет, что цикл сдвига не выполнится (i < n начинается с i = k = n).
5. \`k === 1\`: каждый элемент — окно. Ответ — максимум массива.

**Минимальный набор для dynamic-size.**

1. Пустой массив: \`minSubarrayLen(7, [])\` — возвращаем 0/Infinity/null.
2. Массив из одного элемента: \`minSubarrayLen(5, [5])\` — должен вернуть 1.
3. Target недостижим: \`minSubarrayLen(100, [1, 1, 1])\` — возвращаем 0 (по контракту).
4. Дубликаты в подстроке: \`lengthOfLongestSubstring("aaaa")\` — должен вернуть 1.
5. Все элементы одинаковые: важно, чтобы \`while\`-сжатие корректно отрабатывало.
6. Условие выполняется на всём массиве: \`minSubarrayLen(1, [1, 1, 1])\` — окно сразу сужается до длины 1.

**Дополнительно.**
- Отрицательные/нулевые элементы — если по условию они допустимы, проверьте, что монотонность не нарушается.
- Очень длинный массив — для проверки производительности (не O(n²)).
- Массивы из чисел больше 2^31 — для проверки переполнения в других языках (в JS не проблема).

**Tip собеседования.** Назовите тест-кейсы вслух **до** написания кода. Это ловит до 80% багов на этапе проектирования.`,
      followUps: [
        'Как организовать property-based тесты для sliding window?',
        'Что должна вернуть функция при `arr === null`? Как это специфицировать?',
      ],
      relatedChapterId: 'pitfalls',
    },
  ],

  nextTopics: [
    {
      slug: 'hash-map',
      reason:
        'Sliding window часто комбинируется с хеш-таблицей для подсчёта частот символов или элементов в окне (longest substring without repeating characters, minimum window substring).',
    },
    {
      slug: 'two-pointers',
      reason:
        'Скользящее окно — частный случай двух указателей. После закрепления окна полезно вернуться к классическим two-pointers задачам и увидеть общие паттерны.',
    },
  ],
};

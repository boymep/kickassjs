import type { Lesson } from '../../types/lesson';
import { twoPointersQuiz } from '../quizzes/two-pointers';

// Index existing quiz questions for reuse as checkpoints.
const Q = Object.fromEntries(twoPointersQuiz.questions.map((q) => [q.id, q]));

// Questions used as in-chapter checkpoints (must NOT appear in finalQuiz).
const CHECKPOINT_IDS = new Set(['tp-q1', 'tp-q5', 'tp-q3', 'tp-q11', 'tp-q14']);

export const twoPointersLesson: Lesson = {
  topicId: 'two-pointers',

  intro: {
    whyItMatters: `Техника двух указателей — один из самых частых приёмов на алгоритмических собеседованиях во фронтенд- и бэкенд-разработке. Она встречается в задачах поиска пары с заданной суммой в отсортированном массиве (Two Sum II), удаления дубликатов или нулей in-place, проверки палиндрома, объединения двух отсортированных списков, реализации алгоритма Trapping Rain Water и партиционирования при quicksort. На практике вы используете её каждый раз, когда сливаете отсортированные потоки данных, нормализуете коллекцию без аллокации нового массива или ищете границы подмножества в отсортированной коллекции.

Главная ценность техники — переход от наивного решения O(n²) к линейному O(n) без дополнительной памяти. Это достигается за счёт инварианта: каждый из указателей движется только в одном направлении, поэтому суммарное число шагов не превышает длину массива. В задачах с тремя элементами (3Sum) два указателя комбинируются с предварительной сортировкой и снижают сложность с O(n³) до O(n²).

На собеседовании интервьюер проверяет не знание шаблона, а умение распознать паттерн в незнакомой формулировке, корректно записать инвариант цикла и обработать граничные случаи: пустой массив, дубликаты, перекрытие указателей. В этом уроке вы научитесь различать два вида техники (converging и parallel), увидите, чем она отличается от скользящего окна, и потренируетесь решать задачи без подсказок.`,
    estimatedMinutes: 32,
    interviewAngle:
      'Двух указателей спрашивают почти на каждом алгоритмическом собеседовании. Сильный кандидат не пишет шаблон по памяти, а аргументирует, почему техника применима: где именно проявляется монотонность и почему сдвиг указателя не теряет ни одного потенциального ответа.',
    prerequisites: [{ slug: 'binary-search', title: 'Бинарный поиск' }],
  },

  resources: {
    videos: [
      {
        source: 'youtube',
        id: 'jzZsG8n2R9A',
        title: '3Sum — LeetCode 15',
        channel: 'NeetCode',
        language: 'en',
        durationSec: 12 * 60,
        description: 'Образцовый разбор того, как сортировка плюс два указателя превращают O(n³) в O(n²).',
      },
      {
        source: 'youtube',
        id: '-gjxg6Pln50',
        title: 'How to Use the Two Pointer Technique',
        channel: 'Errichto Algorithms',
        language: 'en',
        durationSec: 18 * 60,
        description:
          'Errichto на конкретных задачах показывает, когда два указателя дают O(n) и почему сдвиг указателя обоснован. Хороший широкий обзор паттерна, дополняющий узкий разбор 3Sum.',
      },
    ],
    links: [
      {
        url: 'https://cp-algorithms.com/others/15-puzzle.html',
        title: 'cp-algorithms — статьи по технике двух указателей',
        source: 'article',
        language: 'en',
        note: 'Раздел e-maxx с обзором классических задач, где встречается приём двух указателей.',
      },
      {
        url: 'https://neetcode.io/courses/dsa-for-beginners/9',
        title: 'Two Pointers — NeetCode DSA course',
        source: 'article',
        language: 'en',
        note: 'Бесплатный курс с пошаговым разбором шаблона converging-pointers и задач Two Sum II, 3Sum, Trapping Rain Water.',
      },
      {
        url: 'https://leetcode.com/articles/two-pointer-technique/',
        title: 'Two Pointer Technique — LeetCode editorial',
        source: 'article',
        language: 'en',
        note: 'Каноническая статья с классификацией: указатели навстречу, в одном направлении, slow/fast.',
      },
      {
        url: 'https://learn.javascript.ru/array-methods',
        title: 'Методы массивов — learn.javascript.ru',
        source: 'learn-js',
        language: 'ru',
        note: 'Глава Ильи Кантора по работе с массивами — нужный фундамент перед освоением алгоритма.',
      },
      {
        url: 'https://developer.mozilla.org/ru/docs/Web/JavaScript/Reference/Global_Objects/Array',
        title: 'Array — MDN',
        source: 'mdn',
        language: 'ru',
        note: 'Справочник по индексному доступу и in-place-операциям над массивами.',
      },
    ],
  },

  chapters: [
    {
      id: 'how-it-works',
      title: 'Как работают два указателя',
      estimatedMinutes: 5,
      blocks: [
        {
          type: 'text',
          content:
            '**Техника двух указателей** — это паттерн обхода массива или строки с помощью двух индексов вместо вложенных циклов. На каждой итерации вы решаете, какой из указателей сдвинуть, и за один проход обрабатываете задачу, которая в наивном решении требует O(n²).',
        },
        {
          type: 'visualization',
          content: '',
          vizId: 'two-pointers',
        },
        { type: 'heading', content: 'Два варианта техники' },
        {
          type: 'list',
          content: `- **Converging pointers** (сходящиеся): \`left\` стартует в начале, \`right\` — в конце. Указатели движутся навстречу друг другу. Применяется на отсортированных массивах и в задачах симметрии (палиндром).
- **Parallel pointers** (параллельные, slow/fast): оба указателя идут в одну сторону. \`fast\` читает каждый элемент, \`slow\` отмечает позицию для записи. Применяется для in-place-модификаций и для обнаружения цикла в связном списке.`,
        },
        {
          type: 'callout',
          calloutType: 'info',
          content:
            'Ключевое условие применимости — монотонность. У сходящихся указателей это отсортированность массива: сдвиг одного указателя гарантированно отбрасывает все «безнадёжные» пары. У параллельных — то, что элементы обрабатываются строго в порядке возрастания индекса.',
        },
        {
          type: 'text',
          content:
            'Сложность техники: O(n) по времени и O(1) по дополнительной памяти. Каждый указатель движется не более n раз и в одном направлении, поэтому суммарное число итераций ограничено линейно.',
        },
      ],
      flashcardIds: ['tp-f1', 'tp-f2'],
      checkpoint: [Q['tp-q1']!, Q['tp-q5']!],
      docsLink: { url: 'https://learn.javascript.ru/array-methods', title: 'Методы массивов — learn.javascript.ru' },
    },

    {
      id: 'converging-pointers',
      title: 'Сходящиеся указатели',
      estimatedMinutes: 7,
      blocks: [
        { type: 'visualization', content: '', vizId: 'converging-pointers' },
        {
          type: 'text',
          content:
            'Сходящиеся указатели — это пара \`left\` и \`right\`, которые начинают с противоположных концов массива и сдвигаются навстречу. Условие выхода — \`left < right\`: при равенстве указатели встретились и пространство поиска исчерпано.',
        },
        { type: 'heading', content: 'Шаблон: пара с заданной суммой' },
        {
          type: 'code',
          language: 'javascript',
          content: `function twoSumSorted(nums, target) {
  let left = 0;
  let right = nums.length - 1;

  while (left < right) {
    const sum = nums[left] + nums[right];

    if (sum === target) return [left, right];
    if (sum < target) left++;   // нужно число побольше
    else right--;               // нужно число поменьше
  }

  return [-1, -1];
}`,
        },
        {
          type: 'callout',
          calloutType: 'tip',
          content:
            'Почему сдвиг корректен: при \`sum < target\` элемент \`nums[left]\` не может быть частью ответа ни с одним элементом из \`(left; right]\` — все они уже учтены через \`right\`. Поэтому \`left\` можно сдвинуть, не теряя ни одной потенциальной пары.',
        },
        { type: 'heading', content: 'Шаблон: проверка палиндрома' },
        {
          type: 'code',
          language: 'javascript',
          content: `function isPalindrome(s) {
  let left = 0;
  let right = s.length - 1;

  while (left < right) {
    if (s[left] !== s[right]) return false;
    left++;
    right--;
  }

  return true;
}`,
        },
        {
          type: 'text',
          content:
            'Тот же приём применяется в задачах Container With Most Water и Trapping Rain Water — там монотонность строится не на сортировке, а на свойстве «более низкая стенка ограничивает объём воды».',
        },
      ],
      flashcardIds: ['tp-f3', 'tp-f4'],
      checkpoint: [Q['tp-q11']!],
    },

    {
      id: 'parallel-pointers',
      title: 'Параллельные указатели (slow/fast)',
      estimatedMinutes: 7,
      blocks: [
        { type: 'visualization', content: '', vizId: 'parallel-pointers' },
        {
          type: 'text',
          content:
            'Параллельные указатели идут в одну сторону. \`fast\` читает массив элемент за элементом. \`slow\` указывает на позицию, в которую следует записать следующий «нужный» элемент. После обхода первые \`slow\` элементов содержат отфильтрованную последовательность.',
        },
        { type: 'heading', content: 'Шаблон: удаление дубликатов in-place' },
        {
          type: 'code',
          language: 'javascript',
          content: `function removeDuplicates(nums) {
  if (nums.length === 0) return 0;

  let slow = 0;

  for (let fast = 1; fast < nums.length; fast++) {
    if (nums[fast] !== nums[slow]) {
      slow++;
      nums[slow] = nums[fast];
    }
  }

  return slow + 1; // длина уникального префикса
}`,
        },
        {
          type: 'callout',
          calloutType: 'info',
          content:
            'Инвариант: после каждой итерации первые \`slow + 1\` элементов \`nums[0..slow]\` уникальны и совпадают с уникальными значениями подмассива \`nums[0..fast]\` исходного входа. Доказывается индукцией по \`fast\`.',
        },
        { type: 'heading', content: 'Шаблон: слияние двух отсортированных массивов' },
        {
          type: 'code',
          language: 'javascript',
          content: `function merge(a, b) {
  const result = [];
  let i = 0;
  let j = 0;

  while (i < a.length && j < b.length) {
    if (a[i] <= b[j]) result.push(a[i++]);
    else result.push(b[j++]);
  }

  while (i < a.length) result.push(a[i++]);
  while (j < b.length) result.push(b[j++]);

  return result;
}`,
        },
        {
          type: 'text',
          content:
            'Это базовая операция merge sort. Два указателя гарантируют, что каждый элемент посещается ровно один раз — итого O(n + m) времени.',
        },
      ],
      playground: {
        starterCode: `// Реализуйте moveZeroes: переместите все нули в конец массива,
// сохранив относительный порядок ненулевых элементов. Решите за O(n)
// и O(1) дополнительной памяти.

function moveZeroes(nums) {
  // ваш код
  return nums;
}

console.log(JSON.stringify(moveZeroes([0, 1, 0, 3, 12])));`,
        expectedOutput: '[1,3,12,0,0]',
        description:
          'Используйте slow/fast: slow — позиция для следующего ненулевого элемента, fast — текущая позиция чтения. При \`nums[fast] !== 0\` сделайте swap и сдвиньте slow.',
      },
      flashcardIds: ['tp-f5', 'tp-f6'],
      checkpoint: [Q['tp-q3']!, Q['tp-q14']!],
    },

    {
      id: 'when-to-apply',
      title: 'Когда применять и чем отличается от sliding window',
      estimatedMinutes: 6,
      blocks: [
        { type: 'heading', content: 'Сигналы задачи' },
        {
          type: 'list',
          content: `- Массив **отсортирован** или его можно отсортировать без потери индексов.
- Нужна **пара** или **тройка** элементов с заданным свойством (сумма, разность, произведение).
- Требуется **in-place**-модификация без аллокации нового массива.
- В формулировке упоминаются **палиндром**, **слияние**, **разделение** или **симметрия**.`,
        },
        { type: 'heading', content: 'Чем two-pointers отличается от sliding window' },
        {
          type: 'text',
          content:
            'Скользящее окно — частный случай параллельных указателей, где \`left\` и \`right\` ограничивают **подмассив**, а внутри окна поддерживается агрегат (сумма, счётчик, частота). Two-pointers в общем смысле не требует поддерживать состояние внутри отрезка — каждый шаг решается локально, сравнением \`arr[left]\` и \`arr[right]\` или \`arr[fast]\` и \`arr[slow]\`.',
        },
        {
          type: 'callout',
          calloutType: 'tip',
          content:
            'Если задача звучит как «найдите подмассив максимальной длины с условием X» или «минимальное окно, удовлетворяющее Y» — это sliding window. Если «найдите пару/тройку» или «сделайте in-place» — это two-pointers.',
        },
        { type: 'heading', content: 'Когда техника не работает' },
        {
          type: 'list',
          content: `- Массив **не отсортирован**, а сортировка теряет нужные индексы. Пример: классический Two Sum (LeetCode 1), где требуются исходные индексы. Решение — хеш-таблица за O(n).
- Свойство **немонотонно**: сдвиг указателя может пропустить ответ. Здесь нужны другие техники — динамическое программирование, рекурсия, BFS/DFS.
- Требуется **произвольный доступ** к удалённым элементам. Two-pointers идёт строго вперёд или к центру и не возвращается назад.`,
        },
      ],
      flashcardIds: ['tp-f7', 'tp-f8'],
    },

    {
      id: 'pitfalls',
      title: 'Типичные ошибки',
      estimatedMinutes: 5,
      blocks: [
        { type: 'heading', content: 'Off-by-one в условии цикла' },
        {
          type: 'text',
          content:
            'Для converging pointers стандартное условие — \`while (left < right)\`. Запись \`left <= right\` приведёт к тому, что один и тот же элемент будет использован дважды (например, в Two Sum получится пара \`[i, i]\`).',
        },
        {
          type: 'code',
          language: 'javascript',
          content: `// АНТИПАТТЕРН: при left === right элемент посчитается с самим собой
while (left <= right) {
  const sum = nums[left] + nums[right];
  // при left === right здесь sum === 2 * nums[left]
}`,
        },
        { type: 'heading', content: 'Забытый сдвиг указателя' },
        {
          type: 'code',
          language: 'javascript',
          content: `// АНТИПАТТЕРН: бесконечный цикл
while (left < right) {
  if (nums[left] + nums[right] === target) return [left, right];
  if (nums[left] + nums[right] < target) {
    // забыли left++
  } else {
    right--;
  }
}`,
        },
        {
          type: 'callout',
          calloutType: 'warning',
          content:
            'Правило: каждая ветка цикла должна **строго** сдвигать хотя бы один указатель. Это инвариант, гарантирующий завершение цикла за O(n).',
        },
        { type: 'heading', content: 'Дубликаты в задачах с тройками' },
        {
          type: 'text',
          content:
            'В 3Sum после нахождения подходящей тройки нужно пропустить все дубликаты \`nums[left]\` и \`nums[right]\`, иначе одна и та же тройка попадёт в ответ несколько раз.',
        },
        {
          type: 'code',
          language: 'javascript',
          content: `if (sum === 0) {
  result.push([nums[i], nums[left], nums[right]]);
  while (left < right && nums[left]  === nums[left + 1])  left++;
  while (left < right && nums[right] === nums[right - 1]) right--;
  left++;
  right--;
}`,
        },
        { type: 'heading', content: 'Выход за границу при односторонней проверке' },
        {
          type: 'callout',
          calloutType: 'warning',
          content:
            'В алгоритмах с параллельными указателями (например, merge) забытая проверка \`i < a.length\` приведёт к чтению \`undefined\`. В JavaScript это не выбросит ошибку, но даст некорректное сравнение и тихий баг.',
        },
      ],
      playground: {
        starterCode: `// Перед вами буггованный шаблон. Найдите ошибку и исправьте её.
// Ожидаемый вывод: true (строка "level" — палиндром).

function isPalindrome(s) {
  let left = 0;
  let right = s.length;     // ← подсказка: что-то не так

  while (left < right) {
    if (s[left] !== s[right]) return false;
    left++;
    right--;
  }
  return true;
}

console.log(isPalindrome('level'));`,
        expectedOutput: 'true',
        description:
          'Указатель \`right\` должен начинаться с \`s.length - 1\`, а не \`s.length\`. Иначе на первой итерации сравнивается \`s[0]\` с \`s[length]\`, что даёт \`undefined\` и ломает логику.',
      },
      flashcardIds: ['tp-f9', 'tp-f10'],
    },

    {
      id: 'advanced',
      title: 'Усложнения: 3Sum и Dutch Flag',
      estimatedMinutes: 5,
      blocks: [
        { type: 'heading', content: '3Sum: три указателя через сортировку' },
        {
          type: 'text',
          content:
            'Задача: найти все уникальные тройки \`(a, b, c)\` в массиве, такие что \`a + b + c = 0\`. Наивное решение O(n³). С сортировкой и двумя указателями — O(n²).',
        },
        {
          type: 'code',
          language: 'javascript',
          content: `function threeSum(nums) {
  nums.sort((a, b) => a - b);
  const result = [];

  for (let i = 0; i < nums.length - 2; i++) {
    if (i > 0 && nums[i] === nums[i - 1]) continue; // пропускаем дубль

    let left = i + 1;
    let right = nums.length - 1;

    while (left < right) {
      const sum = nums[i] + nums[left] + nums[right];
      if (sum === 0) {
        result.push([nums[i], nums[left], nums[right]]);
        while (left < right && nums[left]  === nums[left + 1])  left++;
        while (left < right && nums[right] === nums[right - 1]) right--;
        left++;
        right--;
      } else if (sum < 0) left++;
      else right--;
    }
  }

  return result;
}`,
        },
        { type: 'heading', content: 'Dutch National Flag: три указателя за один проход' },
        {
          type: 'text',
          content:
            'Алгоритм Дейкстры сортирует массив из трёх различных значений (0, 1, 2) за O(n) времени и O(1) памяти. Используются три указателя: \`low\` (граница нулей), \`mid\` (текущий), \`high\` (граница двоек).',
        },
        {
          type: 'code',
          language: 'javascript',
          content: `function sortColors(nums) {
  let low = 0;
  let mid = 0;
  let high = nums.length - 1;

  while (mid <= high) {
    if (nums[mid] === 0) {
      [nums[low], nums[mid]] = [nums[mid], nums[low]];
      low++;
      mid++;
    } else if (nums[mid] === 1) {
      mid++;
    } else {
      [nums[mid], nums[high]] = [nums[high], nums[mid]];
      high--; // mid не сдвигаем — пришедший элемент ещё не обработан
    }
  }
}`,
        },
        {
          type: 'callout',
          calloutType: 'tip',
          content:
            'Обратите внимание: при swap с \`high\` указатель \`mid\` не сдвигается. Это нужно, потому что элемент, пришедший с правого конца, ещё не классифицирован — он может быть нулём, единицей или ещё одной двойкой.',
        },
      ],
      flashcardIds: ['tp-f11'],
    },
  ],

  // Все вопросы из старого квиза, кроме тех, что ушли в checkpoint.
  finalQuiz: twoPointersQuiz.questions.filter((q) => !CHECKPOINT_IDS.has(q.id)),

  flashcards: [
    {
      id: 'tp-f1',
      question: 'Что такое техника двух указателей?',
      answer:
        'Алгоритмический паттерн, в котором массив или строка обрабатываются за один проход с помощью двух индексов вместо вложенных циклов. Снижает сложность с O(n²) до O(n).',
      keyPoints: [
        'Два индекса вместо вложенного цикла',
        'O(n) времени, O(1) памяти',
        'Каждый указатель движется только в одном направлении',
      ],
    },
    {
      id: 'tp-f2',
      question: 'Какие два основных вида техники существуют?',
      answer:
        'Converging (сходящиеся): left и right движутся навстречу с противоположных концов. Parallel (slow/fast): оба указателя идут в одну сторону, slow отмечает позицию для записи.',
      keyPoints: [
        'Converging — для отсортированных массивов и палиндромов',
        'Parallel — для in-place-фильтрации и слияния',
        'Slow/fast — частный случай parallel, иногда с разной скоростью',
      ],
    },
    {
      id: 'tp-f3',
      question: 'Как выглядит шаблон converging-pointers для пары с заданной суммой?',
      answer:
        'Установите left=0 и right=n-1. В цикле while(left<right) сравнивайте sum с target и сдвигайте указатель в нужную сторону: меньше — left++, больше — right--.',
      keyPoints: [
        'Условие цикла: `left < right` (строгое неравенство)',
        '`sum < target` → `left++`',
        '`sum > target` → `right--`',
      ],
      code: `let left = 0, right = nums.length - 1;
while (left < right) {
  const sum = nums[left] + nums[right];
  if (sum === target) return [left, right];
  if (sum < target) left++;
  else right--;
}`,
      codeLanguage: 'javascript',
    },
    {
      id: 'tp-f4',
      question: 'Почему сдвиг указателя не теряет ни одной потенциальной пары?',
      answer:
        'Если sum < target, то nums[left] слишком мал, чтобы образовать пару с любым элементом в [left; right]. Все эти пары уже неявно «отброшены» через right. Аналогично для sum > target.',
      keyPoints: [
        'Опирается на отсортированность',
        'Все пары вида (left, x) для x ≤ right отбрасываются обоснованно',
        'Без сортировки рассуждение не работает',
      ],
    },
    {
      id: 'tp-f5',
      question: 'Как работает шаблон slow/fast для удаления дубликатов?',
      answer:
        'fast идёт по массиву и сравнивает nums[fast] с nums[slow]. Если они различаются — увеличивает slow и записывает nums[fast] в новую позицию. После прохода первые slow+1 элементов уникальны.',
      keyPoints: [
        'fast — читатель, slow — писатель',
        'Возвращаемое значение: длина префикса = `slow + 1`',
        'Работает только на отсортированном массиве',
      ],
      code: `let slow = 0;
for (let fast = 1; fast < nums.length; fast++) {
  if (nums[fast] !== nums[slow]) {
    slow++;
    nums[slow] = nums[fast];
  }
}
return slow + 1;`,
      codeLanguage: 'javascript',
    },
    {
      id: 'tp-f6',
      question: 'Какова сложность техники двух указателей?',
      answer:
        'Время — O(n), потому что каждый указатель движется не более n шагов в одном направлении. Дополнительная память — O(1): хранятся только сами индексы.',
      keyPoints: [
        'Время O(n)',
        'Память O(1)',
        'Для 3Sum с фиксированным первым элементом — O(n²)',
      ],
    },
    {
      id: 'tp-f7',
      question: 'Чем two-pointers отличается от sliding window?',
      answer:
        'Sliding window — это частный случай parallel-pointers, где между left и right поддерживается агрегат (сумма, счётчик символов). В общем two-pointers агрегат не нужен: каждый шаг решается локально.',
      keyPoints: [
        'Sliding window: «найдите подмассив с условием»',
        'Two-pointers: «найдите пару/тройку» или «in-place»',
        'Sliding window поддерживает состояние внутри окна',
      ],
    },
    {
      id: 'tp-f8',
      question: 'Когда техника двух указателей не применима?',
      answer:
        'Если массив не отсортирован и сортировка теряет нужные индексы (классический Two Sum по индексам). Если свойство немонотонно. Если требуется произвольный доступ к уже пройденным элементам.',
      keyPoints: [
        'Без сортировки — рассмотрите хеш-таблицу',
        'Без монотонности — DP или поиск в ширину',
        'Связные списки без random access — частично работает (slow/fast)',
      ],
    },
    {
      id: 'tp-f9',
      question: 'Какие ошибки чаще всего ломают шаблон двух указателей?',
      answer:
        'Перепутали `<` и `<=` в условии цикла. Забыли сдвинуть указатель в одной из веток — бесконечный цикл. Не пропустили дубликаты в 3Sum — повторяющиеся ответы. Вышли за границу через `s.length` вместо `s.length - 1`.',
      keyPoints: [
        '`while (left < right)` — строгое неравенство',
        'Каждая ветка должна сдвигать указатель',
        'В 3Sum пропускайте дубликаты после успеха',
      ],
    },
    {
      id: 'tp-f10',
      question: 'Как удалить дубликаты в 3Sum, чтобы тройки не повторялись?',
      answer:
        'Внешний цикл: пропустить i, если nums[i] === nums[i-1] и i>0. Внутри: после успешного push сделать while-сдвиг left и right через все равные соседние элементы, потом обычный left++/right--.',
      keyPoints: [
        'Внешний пропуск: `i > 0 && nums[i] === nums[i - 1]`',
        'Внутренний пропуск дубликатов left и right',
        'Сравнение с предыдущим элементом, не со следующим',
      ],
    },
    {
      id: 'tp-f11',
      question: 'Что такое алгоритм Dutch National Flag?',
      answer:
        'Алгоритм Дейкстры для сортировки массива из трёх различных значений за O(n) и O(1). Использует три указателя: low (граница нулей), mid (текущий элемент), high (граница двоек).',
      keyPoints: [
        'Три указателя, один проход',
        'При swap с high mid не сдвигается',
        'Применение: sort colors, partition в quicksort',
      ],
    },
    {
      id: 'tp-f12',
      question: 'Зачем сортировать массив перед применением 3Sum?',
      answer:
        'Сортировка делает предикат «sum < 0 → нужно увеличить» монотонным. Без сортировки сдвиг указателя не гарантирует отсечение ответов, и приходится перебирать все пары — O(n²) на каждый i, итого O(n³).',
      keyPoints: [
        'Сортировка — O(n log n), поглощается O(n²)',
        'Включает монотонность для двух указателей',
        'Альтернатива без сортировки — хеш-таблица',
      ],
    },
  ],

  cheatsheet: `### Шпаргалка по двум указателям

- **Идея**: два индекса вместо вложенных циклов → O(n) вместо O(n²).
- **Converging**: \`left=0\`, \`right=n-1\`, \`while (left < right)\`. Для отсортированных массивов и палиндромов.
- **Parallel (slow/fast)**: оба идут вправо, \`slow\` пишет, \`fast\` читает. Для in-place-фильтрации.
- **Сдвиг**: при \`sum < target\` → \`left++\`; при \`sum > target\` → \`right--\`.
- **Условие цикла**: всегда строгое \`<\` для converging, иначе элемент посчитается с самим собой.
- **3Sum**: сортируйте, фиксируйте \`i\`, применяйте two-pointers к подмассиву \`(i; n)\`. Не забудьте пропуск дубликатов.
- **Не работает**: если нужны исходные индексы и сортировка их теряет → берите хеш-таблицу.
- **Тесты**: пустой массив, один элемент, все одинаковые, дубликаты, target меньше/больше всех сумм.`,

  interviewQA: [
    {
      id: 'tp-iq1',
      question: 'В чём идея техники «двух указателей» и почему она даёт O(n)?',
      shortAnswer:
        'Два индекса заменяют вложенный цикл. Каждый из них движется только в одном направлении, поэтому суммарное число шагов не превышает n. Итого — O(n) вместо O(n²).',
      fullAnswer: `Техника двух указателей — это паттерн, в котором массив или строка обходятся одним внешним циклом с двумя индексами вместо вложенных циклов.

Линейная сложность достигается благодаря инварианту: на каждой итерации сдвигается **ровно один** из указателей, и он движется только в **одном** направлении. У сходящихся указателей \`left\` идёт только вправо, \`right\` — только влево; они встречаются ровно один раз. У параллельных указателей \`fast\` идёт от 0 до n-1, \`slow\` — от 0 до значения, не превышающего \`fast\`.

Суммарное число итераций ограничено длиной массива \`n\`. Каждая итерация выполняет O(1) работы — два чтения, сравнение и сдвиг. Итого — O(n) времени и O(1) дополнительной памяти.

Корректность опирается на монотонность. Для converging pointers это либо отсортированность массива (Two Sum II), либо физический инвариант задачи (Container With Most Water: более низкая стенка ограничивает объём). Для parallel pointers — то, что элементы обрабатываются строго в порядке возрастания индекса.`,
      followUps: [
        'Что произойдёт, если сдвигать оба указателя одновременно?',
        'Как обосновать корректность сдвига при `sum < target` без ссылки на интуицию?',
      ],
      relatedChapterId: 'how-it-works',
    },
    {
      id: 'tp-iq2',
      question: 'Чем сходящиеся указатели отличаются от parallel-pointers (slow/fast)?',
      shortAnswer:
        'Сходящиеся идут навстречу с противоположных концов; применяются для пар, троек и палиндромов на отсортированных данных. Параллельные идут в одну сторону; применяются для in-place-фильтрации и слияния.',
      fullAnswer: `**Сходящиеся указатели.** \`left\` стартует в начале, \`right\` — в конце. Условие цикла \`while (left < right)\`. На каждой итерации мы сравниваем \`arr[left]\` и \`arr[right]\` (или их сумму) с целью и решаем, какой указатель сдвинуть. Применяется в задачах:

- Two Sum II на отсортированном массиве.
- Проверка палиндрома.
- Container With Most Water.
- Trapping Rain Water.
- Внутренний цикл в 3Sum после фиксации одного элемента.

**Параллельные указатели (slow/fast).** Оба указателя идут слева направо. \`fast\` читает каждый элемент, \`slow\` отмечает позицию для записи. Условие цикла обычно \`for (let fast = 1; fast < n; fast++)\` или \`while (i < a.length && j < b.length)\` для двух массивов. Применяется в задачах:

- Удаление дубликатов из отсортированного массива in-place.
- Move Zeroes — перемещение всех нулей в конец.
- Partition в quicksort и Dutch National Flag.
- Слияние двух отсортированных массивов или связных списков.
- Floyd's cycle detection (черепаха и заяц) — частный случай со скоростями 1 и 2.

Главная разница — куда смотрит инвариант. У сходящихся он связывает левый и правый конец массива. У параллельных — состояние префикса \`[0; slow]\`, обычно «уже отфильтрованного».`,
      followUps: [
        'В каких задачах slow и fast двигаются с разной скоростью?',
        'Можно ли свести задачу о палиндроме к slow/fast и стоит ли это делать?',
      ],
      relatedChapterId: 'parallel-pointers',
    },
    {
      id: 'tp-iq3',
      question: 'Когда техника двух указателей не работает без предварительной сортировки?',
      shortAnswer:
        'Если задача требует вернуть исходные индексы, а сортировка их теряет. Например, классический Two Sum (LeetCode 1): нужно вернуть индексы пары, и сортировка ломает соответствие. Решение — хеш-таблица за O(n).',
      fullAnswer: `Корректность двух указателей опирается на монотонный предикат: «при сдвиге указателя я не теряю ни одного потенциального ответа». Этот предикат обычно следует из отсортированности массива.

Если массив не отсортирован, у нас два варианта:

1. **Отсортировать.** Это работает, если ответ задачи — сами **значения** или **их количество**, а не исходные индексы. Пример: 3Sum возвращает уникальные тройки чисел, индексы не важны. Сортировка обходится в O(n log n) и поглощается общей сложностью O(n²).

2. **Не сортировать и взять другую технику.** Пример: классический Two Sum просит вернуть пару исходных индексов. Если отсортировать массив — соответствие \`значение → индекс\` нарушится. Решение: хеш-таблица \`{ значение: индекс }\` и один проход за O(n).

Также техника не применима, если предикат **немонотонен**. Например, в задаче «найдите пару с разностью ровно K на неотсортированном массиве» сдвиг указателя не отсекает корректные пары — нужно либо хеш-множество, либо сортировка плюс модифицированный шаблон.

Третий случай — задачи, требующие возврата к уже пройденным элементам. Two-pointers идёт строго вперёд (или к центру) и не возвращается. Если нужно «откатиться», подходят рекурсия с возвратом, DP или стек.`,
      followUps: [
        'Как реализовать классический Two Sum за O(n) без сортировки?',
        'Можно ли совместить хеш-таблицу с двумя указателями для задачи «3Sum closest»?',
      ],
      relatedChapterId: 'when-to-apply',
    },
    {
      id: 'tp-iq4',
      question: 'Объясните, как удалить дубликаты из отсортированного массива in-place. Каков инвариант цикла?',
      shortAnswer:
        'Используется slow/fast. fast читает массив, slow отмечает конец префикса с уникальными элементами. При nums[fast] !== nums[slow] увеличиваем slow и записываем nums[fast] туда. Возвращаем slow + 1.',
      fullAnswer: `\`\`\`js
function removeDuplicates(nums) {
  if (nums.length === 0) return 0;
  let slow = 0;

  for (let fast = 1; fast < nums.length; fast++) {
    if (nums[fast] !== nums[slow]) {
      slow++;
      nums[slow] = nums[fast];
    }
  }

  return slow + 1;
}
\`\`\`

**Инвариант цикла.** Перед каждой итерацией:

- \`nums[0..slow]\` — все уникальные значения, встретившиеся в \`nums[0..fast-1]\` исходного массива, в порядке первого появления.
- \`nums[slow]\` — последний уникальный элемент.

**Доказательство сохранения.** Рассматриваем \`nums[fast]\`:

1. Если \`nums[fast] === nums[slow]\` — это дубликат, ничего не делаем. Инвариант сохраняется.
2. Если \`nums[fast] !== nums[slow]\` — поскольку массив отсортирован, \`nums[fast] > nums[slow]\` и это новый уникальный элемент. Сдвигаем \`slow\` и записываем. Инвариант снова выполняется.

**На выходе.** \`fast === nums.length\`, поэтому \`nums[0..slow]\` содержит все уникальные значения. Длина уникального префикса — \`slow + 1\`.

**Сложность.** O(n) времени, O(1) дополнительной памяти. На несортированном массиве алгоритм даст неверный результат — дубликат может встретиться позже исходного значения.`,
      followUps: [
        'Как изменится решение, если разрешено оставлять до K вхождений каждого элемента?',
        'Что произойдёт, если массив не отсортирован?',
      ],
      relatedChapterId: 'parallel-pointers',
    },
    {
      id: 'tp-iq5',
      question: 'Чем two-pointers отличается от sliding window? Приведите задачи на оба паттерна.',
      shortAnswer:
        'Sliding window — это two-pointers с агрегатом внутри окна (сумма, счётчик символов). Применяется к задачам про подмассивы и подстроки. Two-pointers в общем смысле работает с парами и тройками, агрегат не нужен.',
      fullAnswer: `Sliding window формально является разновидностью parallel-pointers: оба указателя идут вправо, и \`right\` всегда не меньше \`left\`. Разница — в **что** хранится между указателями.

**Two-pointers** в общем смысле:
- Каждый шаг локален: сравнение \`arr[left]\` и \`arr[right]\` или \`arr[fast]\` и \`arr[slow]\`.
- Никакого состояния между указателями не поддерживается.
- Задачи: Two Sum II, 3Sum, Container With Most Water, remove duplicates, merge two sorted arrays.

**Sliding window:**
- Между \`left\` и \`right\` поддерживается агрегат: сумма элементов, счётчик различных символов, максимум, частоты.
- Расширение \`right\` обновляет агрегат, сжатие \`left\` — откатывает.
- Задачи: «максимальная сумма подмассива длины K», «минимальное окно с заданным набором символов», «длиннейшая подстрока без повторов».

**Признак в формулировке.**
- «Найдите **пару/тройку**, удовлетворяющую X» → two-pointers.
- «Найдите **подмассив/подстроку** с условием X» → sliding window.

**Связь с агрегатом.** Если на собеседовании вы видите «найдите подмассив с суммой ровно K» и массив может содержать отрицательные числа — sliding window не работает (агрегат немонотонен). Тогда используют префиксные суммы и хеш-таблицу.`,
      followUps: [
        'Как доказать, что sliding window работает за O(n), а не за O(n²) при суммарном числе сдвигов?',
        'В каких задачах sliding window не подходит, и почему?',
      ],
      relatedChapterId: 'when-to-apply',
    },
    {
      id: 'tp-iq6',
      question: 'Как алгоритм 3Sum использует два указателя и почему его сложность O(n²)?',
      shortAnswer:
        'Массив сортируется, затем внешний цикл фиксирует первый элемент i, а на подмассиве (i; n) применяется converging two-pointers для поиска пары с суммой -nums[i]. Внешний цикл O(n), внутренний O(n), итого O(n²).',
      fullAnswer: `\`\`\`js
function threeSum(nums) {
  nums.sort((a, b) => a - b);
  const result = [];

  for (let i = 0; i < nums.length - 2; i++) {
    if (i > 0 && nums[i] === nums[i - 1]) continue;

    let left = i + 1;
    let right = nums.length - 1;

    while (left < right) {
      const sum = nums[i] + nums[left] + nums[right];
      if (sum === 0) {
        result.push([nums[i], nums[left], nums[right]]);
        while (left < right && nums[left]  === nums[left + 1])  left++;
        while (left < right && nums[right] === nums[right - 1]) right--;
        left++;
        right--;
      } else if (sum < 0) left++;
      else right--;
    }
  }

  return result;
}
\`\`\`

**Идея.** Зафиксируем первый элемент тройки \`nums[i]\`. Тогда задача сводится к поиску пары \`(left, right)\` в подмассиве \`(i; n)\` с суммой \`-nums[i]\` — это классический Two Sum II на отсортированных данных, решается за O(n) двумя указателями.

**Сложность.**
- Сортировка: O(n log n).
- Внешний цикл: O(n).
- Внутренний two-pointers: O(n).
- Итого: O(n) × O(n) = O(n²). Сортировка O(n log n) поглощается.

**Дубликаты.** Их три источника:
1. Внешний \`i\`: пропускаем, если \`nums[i] === nums[i - 1]\`.
2. Внутренние \`left\` и \`right\` после успеха: пропускаем все равные соседние элементы.

Без этих проверок одна и та же тройка попадёт в ответ несколько раз.

**Альтернативы.** Хеш-множество даёт O(n²) времени, но O(n) памяти. Two-pointers выигрывает по памяти: O(1) (если не считать выходной массив и память сортировки).`,
      followUps: [
        'Как обобщить шаблон до 4Sum и kSum?',
        'Что произойдёт, если массив содержит большие числа и сумма переполняет Number?',
      ],
      relatedChapterId: 'advanced',
    },
  ],

  nextTopics: [
    {
      slug: 'sliding-window',
      reason:
        'Скользящее окно — частный случай параллельных указателей. После two-pointers удобно перейти к нему, чтобы научиться поддерживать агрегат внутри окна.',
    },
    {
      slug: 'hash-map',
      reason:
        'Хеш-таблица — основная альтернатива двум указателям там, где нужны исходные индексы или массив не отсортирован. Together они покрывают большинство задач на пары и тройки.',
    },
  ],
};
